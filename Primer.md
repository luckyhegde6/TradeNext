# Primer.md - Session Tracking

> Agent reads this at the start of every session to understand current state and progress
> ⚠️ IMPORTANT: After completing ANY task, you MUST update documentation (@AGENTS.md, @Primer.md, @agent-memory.md, @Lessons.md). See @Lessons.md Lesson 20 for details.
> 🔄 Handoff System: Read `@HANDOFF.md` for orchestration state and `.agents/handoffs/active/latest.md` for current session handoff.

## Last Updated

2026-09-18 (v3.40.0 pre-merge follow-up — **lint gate restored**: `npm run lint` had been broken repo-wide on Next 16 (`next lint` removed → bad directory) and `eslint .` died in the legacy `FlatCompat` bridge (`@eslint/eslintrc` circular-structure `TypeError`). Fix: `"lint": "eslint ."` + `eslint.config.mjs` rewritten to native `eslint-config-next` flat exports with plugins registered per config object (Lesson 123). 7 real findings fixed — 3 `react-hooks/rules-of-hooks` (`ingest-csv` effects hoisted above the admin guard **and `fetchHistory` hoisted** to avoid a TDZ crash in the loading state; `GoogleAnalytics` → unconditional effect gated internally), 2 `<a>`→`<Link>` breadcrumbs, 2 test `no-this-alias` → arrow. Legacy debt triaged to `warn` (documented): `no-require-imports` ×88, `react/no-unescaped-entities` ×21, react-hooks v6 compiler rules ×14 → **0 errors / 1139 warnings exit 0**; re-verified `tsc 46/0` exact, jest **92/92 (1269 pass)**, `quickbuild` **185/185 + 0 warns**, doc budget **73.8 KB**. Also confirmed before the PR: `feat/sqlite-durable-mirror` fully contained in HEAD and `fix/sqlite-upsert-worker-undefined-bind`'s unique commit `85c18d9` is present as `797e449` (byte-identical warm-endpoint code) → nothing to cherry-pick; PR-vs-main = 45 files +5365/−2009)
2026-09-18 (v3.40.0 — **Agentic context, orchestration & harness**: fixes the REMAINING compaction loop (v3.39.4 removed the injected-file cause; the **conversation transcript** — large tool outputs + long summaries re-sent every request — is what stays resident after compaction). **W1** NEW `.agents/rules/tool-output-protocol.md` + NEW `scripts/dev-checks/chunk-output.mjs` (+12 tests). **W2** NEW `.agents/rules/durable-memory.md` (two-tier `.remember/now.md` + memory MCP; documented defect: `search_nodes` is a literal contiguous substring match → empty result ≠ no memory). **W3** `compaction.reserved` 10000 → **30000** (user-approved). **W4** NEW `.agents/agents/orchestrator.md` + `.agents/commands/orchestrate.md` (`mode: primary`, not subagent; agents **16** / commands **11**; Tier A parallel / Tier B chunked-sequential). **W5+W6** NEW `.agents/agents/orchestrator-health.md` (budgets, liveness 50%/100%, max 1 retry, taxonomy `provider-blocked|timeout|stalled|error|completed`) + handoff `SCHEMA.md` **v1.1** (§8/§9, additive-only). **W7** NEW `check-tsc-baseline.mjs` + committed `tsc-baseline.json` `{total:46,prod:0}` + `check-doc-sizes.mjs` `checkInjectedBudget()`/`--json` + hook §8 + CI `context-budget`/`tsc-baseline` jobs + Turbopack warning gate. **Phase 7** 23 tests; a hardcoded script-path bug in the helper made `--update` overwrite the committed baseline → fixed via explicit `run(script, …)` + `afterAll` byte-identity guards (Lesson 120). Verified `tsc 46` exact, `quickbuild` **0 Turbopack warnings / 185/185**, doc budget **73.0 KB / 100 KB**, suites 23/23. Subagents unusable in this env (free-tier provider error) → Tier B is the live path. On `feat/agentic-context-orchestration` (stacked on `fix/turbopack-tracing-harness @ ca56a74`); commits `b6552ca`→`6a9135a`; **merge/PR pending user** — see Current Project Status below)
2026-09-17 (v3.39.1 — WCAG a11y contrast fixes + flat-change neutral state + index chart buttons open TradingView + mobile menu aria-label: `lib/charting.tsx` NEW `INDEX_TV_TICKERS` (NIFTY→NSE-NIFTY, BANKNIFTY→NSE-BANKNIFTY, NIFTYIT→NSE-CNXIT, SENSEX→BSE-SENSEX, FINNIFTY→NSE-CNXFINANCE) — index chart buttons open TradingView, stocks stay charting.nseindia.com/?symbol=<SYM>-EQ, unknown → ?symbol=<SYM>; `StockQuoteHeader.tsx` flat-0.00 neutral gray pill (no arrow); `NSEStockChart.tsx` NEW `INDICATOR_TEXT_CLASSES` MA20 amber-700/300 · MA50 violet-700/300 · MA200 cyan-700/300 (≥4.5:1) + "Price Chart" h3→h2; `Header.tsx` mobile-menu aria-label; contrast tweaks CorporateDataTabs/PiotroskiFScore/CompanyIntelligence/VerdictCard/UnifiedChart; diff 9 files +65/−32, committed `309deac` on `feat/sqlite-durable-mirror` on top of v3.39.0; full 89/89 suites / 1234 pass / 4 skip / 0 fail, tsc 46 = exact baseline, no migration — see Current Project Status below)
2026-09-15 (v3.39.0 — SQLite Durable Mirror — disk snapshot + Netlify Blobs upload (restart survival, Spec 12): Prisma plan-limit hold until **2026-10-02**; in-memory mirror + breaker-gated boot `syncFromPrisma()` lost hot-route SQLite data on EVERY restart → `lib/sqlite.ts` (+288/−14) `state.db.export()` → disk `logs/sqlite-mirror.sqlite` (getMirrorSnapshotPath 762 + os.tmpdir fallback) + boot restore disk → Blobs → fresh + schema replay + cadence piggybacked on existing 60s ops timer; dynamic `import("@netlify/blobs")` store tradenext-sqlite-mirror, 200 MB cap, digest gate, fail-open; sqlite.test.ts +239 (10 mirror) + NEW sqliteMirror.test.ts 6 golden REAL sql.js WASM; full 89/89 suites / 1234 pass / 4 skip / 0 fail, tsc 46 = exact baseline, no migration; committed `e4623a3` feat + `e9db4a7` docs on `feat/sqlite-durable-mirror` on top of unreleased v3.39.x base fixes `2182687`/`f470e6d`/`e84fb34` (PR #123) + PRs #124/#125 — see Current Project Status below)
2026-09-13 (Netlify deploy fix — `@netlify/blobs` RE-ADDED `^11.0.3` `e84fb34`: v3.38.2's removal of `@netlify/blobs` (zero source refs — 56 grep matches all in CHANGELOG) broke the Netlify build — the Edge bundler imports it internally → "Build script returned non-zero exit code: 2" (failed build `6aa6d681de472d000829d0eb`); identical precedent `c1b27cb` fixed the same failure with `^10.7.7`; fix re-added `^11.0.3` (2 files +380/−4, package.json + package-lock.json); PR #123 merged `9166601`; deploy GREEN (`#BEF9C6`); live site verified **0 console errors** (1 benign report-only CSP INFO notice — Next.js inline telemetry); GitHub-Action changelog push `86595f0` (+515 CHANGELOG.md lines); main = `86595f0` clean; incident docs committed on new branch `docs-blobs-lessons`; lesson — ALWAYS run the full build before commits (`npx prisma generate && npm run quickbuild` — local builds do NOT exercise Netlify's Edge bundling) + keep `@netlify/blobs` in package.json)
2026-09-13 (v3.38.2 — Dependabot vulnerability-fix batch — next 16.3.1→16.3.5 / eslint-config-next 16.3.5 / @next/third-parties 16.3.5 / csv-parse 7.0.2 / morgan 1.12.1 / nodemailer 9.1.1 bumps + mysql2 override 3.24.4 + @netlify/blobs removal: npm audit 0 critical / 3 high (deepmerge-ts 7.1.5 via @prisma/config — no 7.x patch, do NOT override), npm ls clean, full **88/88 suites / 1207 pass / 4 skip / 0 fail exit 0**, tsc 46 = exact baseline, npm run quickbuild OK, no migration, no new packages, diff package.json + package-lock.json ONLY on `fix/db-health-ops-count-manual-sync` on top of pushed v3.38.1 docs `36f2c9b`; committed + pushed; **PR #121 OPEN — merge/deploy PENDING USER** — see Current Project Status below)
2026-09-13 (v3.38.1 — Swing generatedAt push-sink NULL guard (Prisma 23502/23503) + AI-monitoring "Last hour" timeframe filter: full **88/88 suites / 1207 pass / 4 skip / 0 fail**, tsc 46 = exact baseline, no migration, no new packages, diff 6 files +157/−3 on `fix/db-health-ops-count-manual-sync` on top of committed v3.38.0 `d5645a9`; live :3000 "Last hour" clean + 0 console errors; committed `d35343e` (feature) + `36f2c9b` (docs); pushed; **PR #121 OPEN — merge/deploy PENDING USER** — see Current Project Status below)
2026-09-12 (v3.37.0 — Worker engine monitoring fixes 1–5 (issue #119): long-task BUSY heartbeat (`TASK_HEARTBEAT_MS = 240_000`) so a long task can never be reaped mid-flight (5-min liveness wrote SQLite ONLY → Prisma `lastHeartbeat` stale → cross-instance reaper killed the healthy `run_daily_recommendations` worker mid-batch); NEW worker-logs route (dead since v3.14.0); Scheduler Leadership card; cron-ledger running-guard on `recordSystemRunOutcome`; daemon leader cross-check on the cron route; full 88 suites / 1192 pass / 4 skip / 0 fail, tsc 46 = exact baseline; on `fix/workers-tasks-monitoring` on top of PR #118 merge `10e2a00`; see Current Project Status below)
2026-09-11 (v3.36.0 — Swing steady-state serve fix: done/failed analysis jobs served INDEFINITELY — no auto-regen on plain load; `force=1` is the ONLY regeneration trigger (supersedes pending/running + kicks `checkSwingPerformance()`); liveness stale semantics (`staleBefore = now − SWING_JOB_STALE_MS`, anchor by status) + newest-TERMINAL fallback + Prisma fallback + durable pending job; swing 57/57, tsc 46 = exact baseline; on `fix/workers-tasks-monitoring` on top of PR #118 merge `10e2a00`; see Current Project Status below)
2026-09-11 (v3.35.0 — Flaky `intelligence.test.ts` CI fix: prisma mock isolates the fire-and-forget `IntelligenceCache` upsert — zero real-DB writes in tests; un-awaited `upsert` (`cache.ts` :101-124) raced the `beforeEach` `deleteMany` teardown → stale row → spurious `INTELLIGENCE_CACHE_HIT` flaky fails at :187/:246/:279; test-only +21, targeted 13/13, FIRST fully-green full run 86/86 suites / 1170 pass / 4 skip / 0 fail, tsc 46 exact baseline; on PR #118 branch on top of v3.34.1 merge `05b91e8`; see Current Project Status below)
2026-09-11 (v3.34.1 — sql.js WASM async-load gate fix: synchronous `wasmBinary` load + memoized `getSqlJs()` eliminates "Cannot log after tests are done" noise; commit `d91fb01` on `feat/db-health-monthly-ops`, merged into PR #118 branch; see Current Project Status below)
2026-09-11 (v3.34.0 — Monthly Query Consumption — db-health monthly-ops window (deferred Plan 09 rev-v3 c/d): NEW pure `lib/services/opsMonthly.ts` ledger on globalThis `__opsMonthly`, SQLite `_backup_meta` `"ops_monthly"` persistence + db-health "Monthly Query Consumption" card; on `feat/db-health-monthly-ops` on top of committed v3.30.0 `653b617`; see Current Project Status below)
2026-09-10 (v3.33.1 — Swing performance touch-tracking fix: intraday HIGH/LOW touches count as target/stop hits; see Current Project Status below)
2026-09-10 (v3.33.0 — Leader Watchdog Self-Heal: NEW `watchLeaderRole` replaces the one-shot boot election so a dead scheduler self-heals with zero manual intervention — "don't let this happen again"; see Current Project Status below)
2026-09-10 (v3.32.1 — db-health POST double-`req.json()` hotfix: parse the body ONCE and reuse `requestBody` — restores `restore` (broken since v3.21.2) + honest `set_time_correction` zod validation; post-merge of v3.32.0 PR #117 `38a27bf`, on `main` working tree; see Current Project Status below)
2026-09-10 (v3.32.0 — Admin Time Synchronisation: on-demand Postgres NOW() probe + persisted server-clock offset fixing IST-as-UTC cron nextRun corruption; **MERGED to `main` via PR #117** `38a27bf`; see Current Project Status below)
2026-09-09 (v3.31.0 — SQLite-first NSE read architecture + low-frequency Prisma sync; Plan 09 on `fix/v3.29.1-header-watchlist` on top of committed v3.30.0; see Current Project Status below)

2026-09-07 (v3.30.0 — Daemon control-plane cadence + SQLite mirror touch-freshness + `upsertCronJob` Date-binding fix + Netlify WASM staging; on `fix/v3.29.1-header-watchlist` on top of committed v3.29.2 `1e907f1`; see Current Project Status below)

2026-09-07 (v3.29.2 — Admin AI model management: true built-ins permanently locked — `openrouter/free` + `openrouter/auto` can NEVER be removed (API 400 + UI), catalog (8) + custom models stay removable; "Model Library" UI + 3-optgroup active-model select; on `fix/v3.29.1-header-watchlist` on top of committed v3.29.1 `6e8db23`; see Current Project Status below)

2026-09-06 (v3.29.1 — Header overflow fix + Watchlist logged-out infinite-skeleton fix; browser/DevTools visual-verification batch on `main`, on top of merged v3.29.0 `d7e54cf`; see Current Project Status below)

2026-09-05 (v3.29.0 — UI/UX audit fixes: backtest symbol-gate softening + AI-failure error surfacing + mobile-nav Alerts + `[object Object]` throw-site fix, on top of v3.28.5 `6700076` on branch `fix/v3.28.1-sqlite-self-heal`; see Current Project Status below)

2026-09-05 (v3.28.5 — NSE scrip-list constant — Securities Available for Trading (Equity) CSV → generated `lib/services/nseScripList.ts` + `scripts/fetch-nse-scrips.ts` + 11 tests; see Current Project Status below)

2026-09-05 (v3.28.4 — Read-first recommendations route + edge-cache the heavy latest-run reads, on top of v3.28.3 `a1dd094` on branch `fix/v3.28.1-sqlite-self-heal`). User-approved "Both" fix for two compounding issues: **(1) key collision** — `app/api/recommendations/route.ts` wrote its serialized `responseBody` under the **service's** `LATEST_KEY` (`"recommendations:latest"`), clobbering the `LatestCacheEntry {runId, newestRunId, data}` → `cached.runId === undefined` → the cross-instance fingerprint check re-ran the heavy stocks-include query (95 rows) on EVERY request (db-health read-tier `recommendations.prisma` 14/14 huge-query misses); **(2) heavy reads unedge-cached** despite v3.27.0's `withAccelerateCache`. Fix: route gains `ROUTE_CACHE_KEY = "recommendations:api:latest"` + `ROUTE_CACHE_TTL_SECONDS = 60` + typed `RouteRecommendationsCacheBody`, with a read-first memory fast path after the breaker block (`servedFrom: "memory_cache"`, `recordRead("recommendations.memory", {hit:true})`, zero Prisma); all three legacy `"recommendations:latest"` refs (breaker fallback, `set`, DB-error fallback) switched to the route key so the service key is never touched by the route; `getLatestRecommendations`' two heavy `findFirst` reads (latestRun stocks-include + newestRun lightweight select) wrapped in `withAccelerateCache({ttl:60, swr:30})`; fingerprint probes stay **uncached** (cross-instance staleness guard). Tests: factory gains the pure `withAccelerateCache` stub + NEW regression → recs **34/34**. tsc 46 = exact baseline (0 new); full suite **1004 pass / 4 skip / 2 fail** (2 = documented pre-existing `intelligence.test.ts` flake — excluding it 72 suites / 1004 / 4 / 0); no schema change → no migration. Docs complete (incl. TODO.md row + orphaned duplicate `# v3.28.2` header removed at EOF of versions-v3.28.md); **commit pending user (no push/merge without approval)**.

---

## Current Project Status

### v3.40.0 — Agentic context, orchestration & harness (Sep 18 2026) — on `feat/agentic-context-orchestration` (stacked on `fix/turbopack-tracing-harness @ ca56a74`); commits `b6552ca`→`6a9135a`; **merge/PR PENDING USER**

**Why**: v3.39.4 killed the injected-file refill source (358 → 72 KB); the residual compaction loop is the **conversation transcript** itself — large tool outputs + long summaries are re-sent on every request and stay resident after compaction (`compaction.reserved` was only 10000). The epic adds the four cures (chunk outputs, durable memory, compaction headroom, delegation) plus the requested orchestrator, subagent health monitor, better handoff contract, and a code/test/build harness.

- **W1 — tool-output protocol** (`1f73d3c`): NEW `.agents/rules/tool-output-protocol.md` (never land large output in-context; redirect → narrow slice/grep; chunk when too big) + NEW `scripts/dev-checks/chunk-output.mjs` (`splitLines`/`planChunks`/`renderChunk`/`renderIndex`/`chunkOutput`; line-ranges preserved; stale parts pruned) + NEW `lib/__tests__/chunk-output.test.ts` (12 tests).
- **W2 — durable memory** (`40181c4`, `db08255`): NEW `.agents/rules/durable-memory.md` (two-tier `.remember/now.md` + memory MCP; 7-value `entityType`; query-before-read); `.remember/now.md` normalised. **Documented MCP defect**: `search_nodes` is a literal contiguous substring match over the WHOLE query (not tokenised/ranked/fuzzy) — `"Injected context budget doc-size"` → 0 hits but `"Injected context budget"` → 1 — so an empty result ≠ no memory.
- **W3 — compaction headroom** (`f758e67`): `.opencode/opencode.json` `compaction.reserved` **10000 → 30000** (user-approved sensitive op).
- **W4 — orchestrator** (`ac91571`): NEW `.agents/agents/orchestrator.md` + NEW `.agents/commands/orchestrate.md`; `mode: "primary"` (NOT subagent — subagents cannot dispatch subagents), `tools` omitted so it inherits; `.agents/RULES.md` §0 normative parallel-dispatch rule; **agents 16** (primary 3: build/plan/orchestrator; subagents 13), **commands 11**. Tiers: **A** parallel / **B** chunked-sequential.
- **W5+W6 — subagent health + handoff upgrade** (`ac90b53`): NEW `.agents/agents/orchestrator-health.md` (per-kind time budgets; liveness checks at 50 %/100 %; **max 1 retry**; taxonomy `provider-blocked | timeout | stalled | error | completed`) + `.agents/handoffs/SCHEMA.md` **v1.1** (§8 `Subagent Status`, §9 `Handoff Summary`, additive-only BC clause) + `agent-to-agent.md` orchestrator dispatch + **Tier Downgrade Path** + `latest.md` rewritten under v1.1.
- **W7 — harness** (`973cae5`): NEW `scripts/dev-checks/check-tsc-baseline.mjs` (total-vs-prod classification, regression → exit 1, `--json`, `--update`, exit 2 when tsc never ran; documented test seam `TSC_BASELINE_CMD`) + NEW `scripts/dev-checks/tsc-baseline.json` `{ total: 46, prod: 0, recorded: "2026-09-18" }` + `check-doc-sizes.mjs` gains `checkInjectedBudget()`/`--json`/`.context/out` retention advisory + `.githooks/pre-commit` §8 advisory + `.github/workflows/quality-gate.yml` NEW `context-budget` + `tsc-baseline` jobs and the Build step is now a Turbopack warning gate.
- **Phase 7 — tests** (`6a9135a`): 23 tests (`check-tsc-baseline.test.ts` 9 incl. the 1-prod+2-test classification guard; `check-doc-sizes.test.ts` 14 incl. real-repo read-only + failure modes + wiring).
- **Incident (Lesson 120)**: the test helper hardcoded the repo script path, so "harness" tests ran the REAL script — and the `--update` case **overwrote the committed `tsc-baseline.json`** with `{total:2,prod:1}`. Fixed via explicit `run(script, …)` + `afterAll` byte-identity guards on the protected real files; recovered via `git checkout`. Post-run `git status --short` shows only the intended entries.
- **Verified**: `npx tsc --noEmit` **46 = exact baseline (prod 0)** · `check-tsc-baseline.mjs` OK exit 0 · `npm run quickbuild` **0 Turbopack warnings / 185/185 pages** · doc budget **73.0 KB / 100 KB** · new suites **23/23** · `quality-gate.yml` parses. No migration; no new packages (Node built-ins only).
- **Honest limits**: all four `task` subagent types fail with `Error from provider (Console): OpenCode's free tier can only be used from within OpenCode` → Tier A cannot be demoed here; orchestration is capability-detected and **no workstream is blocked on it** (D3/D11). Remaining: Phase 9 final verification; **merge/PR pending user**.

### v3.39.4 — Context-optimisation: slim injected instruction files (358 KB → 72 KB/session) + modular read-on-demand docs + Turbopack tracing fixes → 0 warnings (Sep 18 2026) — on `fix/turbopack-tracing-harness` (on top of `a7e3709` = origin/main); **commit pending user**
Root cause of the session compaction loop: `.opencode/opencode.json` → `instructions` injects the FULL contents of 5 files every request ≈ **358 KB ≈ 90K tokens**, re-injected instantly after each compaction (`compaction.reserved` was only 10000). **Slimmed** via NEW `.context/slim-docs.mjs` (marker-based, EOL-preserving): `AGENTS.md` 191,017 → **20,260 B** (version history → `.agents/changelog/versions-index.md`), `TODO.md` 134,054 → **20,792 B** (Quick Reference → `.agents/changelog/todo-quick-reference-archive.md`) — injected total **72.2 KB**. **NEW `.agents/INDEX.md`** topic→file manifest. **NEW `.agents/session-archive/`** (`Primer.md` 1159 → 213 lines, `agent-memory.md` 1092 → 373 lines) via NEW `.context/chunk-history.mjs`; `Lessons.md` deliberately NOT age-chunked (live rulebook). **Guards**: context-budget rule (`.agents/rules/session-memory-rules.md` §7) + `scripts/dev-checks/check-doc-sizes.mjs`. **Turbopack**: all 33 `/*turbopackIgnore: true*/` sites applied (`lib/logger.ts` 9, `lib/services/ingestService.ts` 2, `lib/services/worker/worker-logger.ts` 14, `lib/sqlite.ts` 8) → `npm run quickbuild` **0 Turbopack warnings** (was 33), 185/185 pages; `npx tsc --noEmit` **46 = exact baseline**. No migration; no new packages.

### v3.39.0 + v3.39.1 — SQLite Durable Mirror (Spec 12) + WCAG/UI batch (Sep 15–17 2026) — on `feat/sqlite-durable-mirror`, push/PR/deploy PENDING USER

**Context**: the Prisma plan-limit hold runs until **2026-10-02**; hot routes (recommendations / screener / corp-actions / swing) served from the in-memory SQLite mirror, which died on EVERY restart because the boot `syncFromPrisma()` is breaker-gated.

**v3.39.0 — SQLite Durable Mirror (disk snapshot + Netlify Blobs upload)** — committed `e4623a3` (feat) + `e9db4a7` (docs), on top of unreleased v3.39.x base fixes `2182687` (toIsoVal) + `f470e6d` (set_ops_counter queryConsumption) + PR #123 `@netlify/blobs` re-add `e84fb34` + PRs #124/#125.
- `lib/sqlite.ts` (+288/−14): `state.db.export()` → disk `logs/sqlite-mirror.sqlite`; NEW `getMirrorSnapshotPath()` (762) with `os.tmpdir()` fallback when `resolveLogsDir()` returns `''` (root-causes the repo-root stray artifact in jsdom — `lib/logger.ts:5` `isServer` false).
- Boot restore order: **disk → Blobs → fresh DB**, with the existing schema replay upgrading older snapshots.
- Cadence: eager after each `syncFromPrisma()` + piggybacked on the EXISTING 60s `OPS_PERSIST_INTERVAL_MS` timer (1979, 1987) → non-leader instances keep fresh mirrors, **no new timers**.
- Netlify Blobs upload: NEW `uploadMirrorSnapshotToBlobs`/`downloadMirrorSnapshotFromBlobs` — dynamic `import("@netlify/blobs")` (no static import), store `tradenext-sqlite-mirror`, key `sqlite-mirror.sqlite`, 200 MB hard cap, digest gate skips identical bytes, memoized `getMirrorBlobsStore()` (908) on globalThis, **fail-open** (never throws / never blocks boot).
- Tests: `sqlite.test.ts` +239 (durable-mirror describe 10 + FakeBlobsStore) + NEW `sqliteMirror.test.ts` 6 golden REAL sql.js WASM (separate file because `sqlite.test.ts` hoists the `sql.js` mock); `.gitignore` +4.

**v3.39.1 — WCAG a11y contrast + flat-change neutral state + index charts → TradingView + mobile-menu aria-label** — committed `309deac` (9 files +65/−32).
- `lib/charting.tsx`: NEW `INDEX_TV_TICKERS` (NIFTY→`NSE-NIFTY`, BANKNIFTY→`NSE-BANKNIFTY`, NIFTYIT→`NSE-CNXIT`, SENSEX→`BSE-SENSEX`, FINNIFTY→`NSE-CNXFINANCE`) — index chart buttons open TradingView (NSE Charting links 404'd for indices); stocks stay `charting.nseindia.com/?symbol=<SYM>-EQ`; unknown → `?symbol=<SYM>`.
- `app/components/StockQuoteHeader.tsx`: flat 0.00 → neutral gray pill (no arrow) instead of green.
- `app/components/NSEStockChart.tsx`: NEW `INDICATOR_TEXT_CLASSES` — MA20 amber-700/300, MA50 violet-700/300, MA200 cyan-700/300 (≥4.5:1 both themes); "Price Chart" h3→h2.
- `app/Header.tsx`: mobile-menu `aria-label="Toggle mobile menu"`; contrast tweaks in CorporateDataTabs / PiotroskiFScore / CompanyIntelligence / VerdictCard / UnifiedChart.

**Verification**: full suite **89/89 suites / 1234 pass / 4 skip / 0 fail**; `npx tsc --noEmit` **46 = exact baseline (0 new)**; no migration; no new packages. Spec `.agents/specs/12-sqlite-durable-mirror.md` + plan `.agents/plans/12-sqlite-durable-mirror.md`.

**⚠️ All of the above is committed on `feat/sqlite-durable-mirror` — push/PR/deploy PENDING USER.**

### Netlify deploy fix — `@netlify/blobs` RE-ADDED `^11.0.3` (Sep 13 2026) — ✅ RESOLVED + LIVE-VERIFIED (docs committed on new branch `docs-blobs-lessons`; PR to open)
**User directive context**: production deploy failed after the v3.38.2 dependency cleanup was merged. **Root cause**: `07f42fe` (v3.38.2) removed `@netlify/blobs` from `package.json` — zero source refs (56 grep matches all in CHANGELOG) — but the Netlify **Edge bundler** imports it internally → "Build script returned non-zero exit code: 2" (failed build `6aa6d681de472d000829d0eb`). Local `npm run quickbuild` does NOT exercise the Edge bundler — final confirmation is the Netlify deploy. **Fix**: `e84fb34` re-added `"@netlify/blobs": "^11.0.3"` (2 files +380/−4 — package.json + package-lock.json, blobs-11.0.3.tgz); identical precedent `c1b27cb` fixed the same failure with `^10.7.7` (⇒ the package was re-added twice — MUST stay).
**Verification**: tsc 46 = exact baseline (0 new) + full build OK pre-fix-commit; PR #123 MERGED (`9166601`) → Netlify deploy GREEN (`#BEF9C6`) → live `https://tradenext6.netlify.app/` serving dashboard → **0 console errors** (1 INFO-level, report-only CSP notice @ `/_next/static/chunks/3zfpuu3o318py.js:5` — Next.js inline telemetry with nonce present — benign, no action). GitHub Action pushed changelog commit `86595f0` (+515 CHANGELOG.md lines — CHANGELOG now 10,773 + 515); `main` now at `86595f0`, working tree clean.
**Lessons (recorded)**: (1) **ALWAYS run the full build (`npx prisma generate && npm run quickbuild`) + tsc baseline before EVERY commit** — v3.38.2 was committed without a build check and only the DEPLOY caught the Edge-bundle break; (2) `@netlify/blobs` must stay in package.json (re-added twice: `c1b27cb` ^10.7.7, `e84fb34` ^11.0.3); (3) pending optional observation — Accelerate Error 1102 / 30s-wall note for HANDOFF.md.
**Status**: RESOLVED + DEPLOYED + LIVE-VERIFIED. PR #121 (v3.38.x) and PR #118 remain merge/deploy PENDING USER.

### v3.38.2 — Dependabot vulnerability-fix batch — next/third-parties/nodemailer/morgan/csv-parse bumps + mysql2 override + @netlify/blobs removal (Sep 13 2026) — ✅ CODE + TESTS VERIFIED (full **88/88 suites / 1207 pass / 4 skip / 0 fail exit 0**; tsc **46 = exact baseline (0 new)**); npm audit **0 critical / 3 high** (all = deepmerge-ts 7.1.5 via @prisma/config — no 7.x patch, do NOT override); npm ls clean; npm run quickbuild OK; no migration; no new packages; diff package.json + package-lock.json ONLY — COMMITTED + PUSHED; **PR #121 OPEN — MERGE/DEPLOY PENDING USER** (on `fix/db-health-ops-count-manual-sync`, on top of pushed v3.38.1 docs commit `36f2c9b`)
**User directive**: run `npm audit --fix` and fix what can be fixed. **(1) Bumps** (`package.json`): `next` 16.3.1→16.3.5, `eslint-config-next` 16.3.5, `@next/third-parties` 16.2.0→16.3.5, `csv-parse` 6.1.0→7.0.2, `morgan` 1.11.0→1.12.1, `nodemailer` 9.0.3→9.1.1. **(2) Removal**: `@netlify/blobs` (v3.11.3 full-serverless-purge leftover — zero refs; only `@netlify/streaming-storage` referenced). **(3) NEW `overrides` block**: `mysql2` → 3.24.4 (GHSA-rgwj-5xj2-c3m3 HIGH; prisma pins exact 3.22.0 → override wins; `npm ls mysql2` = 3.24.4), `@eslint/eslintrc` → {js-yaml 4.3.2} + `@istanbuljs/load-nyc-config` → {js-yaml 3.15.2} (GHSA-2rj7-9f4x-w5w5 code-injection via `!!`), `@humanfs/node` → 0.16.8, `fast-uri` → 3.1.6 (CVE-2025-6613, via `@fastify/ajv-compiler`). **(4) Verification**: `npm ls` clean; `npm audit` → **0 critical / 3 high** (all remaining = `deepmerge-ts` 7.1.5 via `@prisma/config` — GHSA-ggr8-5vv4-36mx <8.0.0, NO 7.x patch exists; only fix = Prisma 6 downgrade which breaks the app → **documented, do NOT override**); full suite **88/88 / 1207 pass / 4 skip / 0 fail exit 0**; tsc **46 = exact baseline (0 new)**; `npm run quickbuild` OK; no migration; no new packages. Timeline in `.agents/changelog/versions-v3.38.md`.
### v3.38.1 — Swing generatedAt push-sink NULL guard (Prisma 23502/23503) + AI-monitoring "Last hour" timeframe filter (Sep 13 2026) — ✅ CODE + TESTS VERIFIED (full 88/88 suites / 1207 pass / 4 skip / 0 fail; tsc 46 = exact baseline); LIVE-VERIFIED; COMMITTED `d35343e` (6 files +157/−3) + DOCS `36f2c9b` (9 files +206/−3); PUSHED; **PR #121 OPEN — MERGE/DEPLOY PENDING USER** (on `fix/db-health-ops-count-manual-sync`, on top of committed v3.38.0 `d5645a9`)
**User directive**: db-health "Recent DB Errors" showed repeating Prisma 23502 NOT NULL violations on `swing_signals.create` every ~6h (the 6h PUSH-cadence sink) + the AI-monitoring "Last hour" filter appeared broken (listed calls older than an hour).
**Root cause (swing)**: `upsertSwingAnalysisJob` bound `created_at` ONLY inside the `payload` object → the SQLite mirror's `generated_at` column stayed NULL → every `pushSqliteToPrisma` swing sink call passed `swing_signals.create` with `generatedAt: :undefined` (prop dropped) → **23502 NOT NULL** (swing cards' target/stop blanked in the catch branch) + **23503 FK cascade (victory rows DESTROYED)**; the Prisma 23502 reset → ResolverFailed → `runScheduledOps` `.finally` recoverySync → `recordDbError` ring entries.
**Fix 1** (`lib/services/swingRecommendationService.ts` ~L1227): NEW top-level `generatedAt: new Date()` binding on `upsertSwingAnalysisJob` — the mirror `generated_at` column is now populated at source.
**Fix 2** (`lib/sqlitePushSinks.ts` ~L497): the swing sink gains a `generatedAt` fallback `() => (row.generatedAt as string) ?? row.created_at` (cast via `tbl.date(...)`) — heals PRE-FIX mirror rows too; no more 23502/23503.
**Root cause (AI-monitoring)**: `getAiCallsMerged` never filtered the in-memory ring by timeframe — only `getAiStatsMerged` re-filtered after merging → "Last hour" showed ~ALL buffered calls.
**Fix 3** (`lib/services/ai/ai-monitoring.ts` L331-340): NEW memory-ring cutoff `cutoff = Date.now() - timeframeMinutes*60*1000` (skip `entry.timestamp < cutoff`) + passes `timeframeMinutes` through to `getPersistedAiCalls`.
**Tests**: `ai-monitoring.test.ts` +86 (NEW "memory-buffer timeframe filter (v3.38.1)" describe — 5/5 incl. the `enqueueWriteBehind` mock); `sqlite.test.ts` +30 (23502 regression: asserts the `params[3]` created_at binding + a `job-null-gen` row); `swingRecommendationService.test.ts` +21 (top-level Date `generatedAt` + string in payload — **40/40**). Full **88/88 suites / 1207 pass / 4 skip / 0 fail**; tsc **46 = exact baseline (0 new)**; no migration; no new packages; diff 6 files +157/−3.
**Live-verified** (:3000): AI-monitoring "Last hour" shows only fresh calls + 0 console errors. Timeline in `.agents/changelog/versions-v3.38.md`.
**Status**: COMMITTED `d35343e` (feature) + `36f2c9b` (docs); PUSHED; **PR #121 OPEN — MERGE/DEPLOY PENDING USER.**

### v3.38.0 — Admin SET_OPS_COUNTER authority fix + db-health "Sync Operations Count" UI (Sep 13 2026) — COMMITTED `d5645a9`; **merge/deploy PENDING USER** (on `fix/db-health-ops-count-manual-sync`)
**User directive**: "In admin — update / set the monthly ops counter manually if needed" — the admin is the authority on actual Prisma-dashboard consumption, so the admin must be able to MANUALLY correct the ops-usage date + total used for plan display.
**(1) `lib/services/opsMonthly.ts` +42**: NEW `setOpsMonthlyDay(day)` / `setOpsMonthlyTotal(total)` mutators on the `__opsMonthly` ledger + `readOpsLedger()` refresh + `resetOpsMonthlyForTests()` flush.
**(2) `lib/audit.ts` +1**: NEW `ADMIN_DB_SET_OPS_COUNTER`.
**(3) db-health POST `set_ops_counter`** (`app/api/admin/db-health/route.ts` +75): zod `setOpsCounterPayload` validates `action` + `set-day`/`set-val` TOGETHER (action-only → 400); day via `Date.parse`/`new Date(s)` fallback to `getIstDayKey()` + IST `YYYY-MM-DD` zod check; `setOpsMonthlyTotal` → `dbOpsCounter.total = round(abs(total))` with `.reset = false` (live counter NOT zeroed — display-only override until the next fold); fail-open `.catch` → 500.
**(4) db-health UI second card** (`app/admin/utils/db-health/page.tsx` +110): local day/total inputs + Update/Reset + authority tooltip "affects the ops-usage UI display and monthly plan usage — NOT the live counter itself" + `warn('unsafe')`; `handleSetOpsCounter` → POST `set`/`reset`.
**Tests**: `dbHealthRoute.test.ts` +69, `opsMonthly.test.ts` +51 (10/10), `audit.test.ts` +3/−1. Full **88 / 1207 pass / 4 skip / 0 fail**; tsc **46 = exact baseline (0 new)**; no migration; diff 7 files +349/−2. Timeline in `.agents/changelog/versions-v3.38.md`. **Do not commit/push/merge without explicit user approval.**

### v3.37.0 — Worker engine monitoring fixes 1–5 (Sep 12 2026) — ✅ CODE + TESTS VERIFIED (worker-service.test.ts 7/7 + cronDaemonRoute.test.ts 4/4 new; worker-engine.test.ts 23/23; full 88/88 suites / 1192 pass / 4 skip / 0 fail; tsc 46 = exact baseline); LIVE-VERIFIED; COMMIT/PUSH PENDING USER (on `fix/workers-tasks-monitoring`, on top of PR #118 merge `10e2a00`; merge/deploy pending user)
**User directive**: issue #119 — fix the worker-engine monitoring groundwork: long tasks were reaped mid-flight, the Logs sub-tab was dead, the Scheduler card was missing, cron-ledger entries could double-count, and the cohort gate ignored leader-liveness.
**Root cause (Fix 1)**: the 5-min liveness heartbeat (`writeHeartbeat("live")`) wrote SQLite ONLY → Prisma `worker_status.lastHeartbeat` went stale ~10 min in → a cross-instance reaper (`worker-169.254.80.183`) legitimately-but-wrongly reaped the `run_daily_recommendations` worker mid-batch (recovery via `checkScheduledJobs` re-claim masked the symptom).
**Fix 1** (`lib/services/worker/worker-engine.ts`): NEW `TASK_HEARTBEAT_MS = 240_000` per-task **BUSY heartbeat** (`updateHeartbeat("busy", task.id)`; interval unref'd; cleared in the task `finally`; also fires at the 60s async `setTimeout` poll) — a task alive up to the `TASK_TIMEOUT_MS = 240 min` last-resort cap can never appear dead; `TASK_TIMEOUT_MS` 40→**240 min**.
**Fix 2**: NEW `app/api/admin/workers/logs/route.ts` — GET list `{files}` (direct fs read on `logs/`, zero DB), GET `?taskId=` `{content}` (`MAX_TASK_ID_LEN = 128` guard), DELETE `?taskId=` `{deleted}`; `auth()+admin();` → 401; route never existed since v3.14.0 → the Workers Logs tab 404'd.
**Fix 3** (`app/admin/utils/workers/page.tsx`): `leader-*` rows surfaced in a NEW **"Scheduler Leadership"** card (role + id + holding = `lastHeartbeat < 10 min`) and filtered OUT of the Active Workers grid (`activeWorkers = workers.filter(w => !w.workerId.startsWith("leader-"))`).
**Fix 4** (`lib/services/worker/worker-service.ts` L148-167): `recordSystemRunOutcome` now EXPORTED + guarded to `task?.cronJobId && task.status === "running"` → a terminating/timeout/reaped task can NEVER double-count nor amend a completed ledger row; timeout path + stale-task reaper record the ledger entry BEFORE the failed-status write; try/catch → `logger.warn` non-fatal.
**Fix 5** (`app/api/admin/cron/daemon/route.ts`): daemon ALSO refreshes the shared `leader-cron-daemon` row; the route cross-checks BOTH rows via `Promise.all` + `leaderFresh` within `LEADER_STALENESS_MS` (10 min) so the cron tab chip reports "Scheduler Live" even if the watchdog's own renewal lands just inside the window.
**Tests**: `worker-service.test.ts` **7/7** (new) + `cronDaemonRoute.test.ts` **4/4** (new) + `worker-engine.test.ts` **23/23** (+254). Full **88/88 suites / 1192 pass / 4 skip / 0 fail**; tsc **46 = exact baseline (0 new)**; no migration; no new packages; diff 6 files +407/−53 + 2 new test files.
**Live-verified** (:3000): Workers tab Active grid + Logs tab file list; Scheduler Leadership 3/3 roles holding on `LAPTOP-HM25SVAR-19820`; `GET /api/admin/cron/daemon` → 200 `running:true` daemonId `cron-daemon-LAPTOP-HM25SVAR-19820` heartbeat ~42s fresh; `GET /api/admin/workers/logs?type=check` → 200 `{files:[…]}`; cron chip "Scheduler Live" + heartbeat 102s; route `registeredJobs` "0 job(s)" = Turbopack dev module-graph artifact (`localhost.cron_job` HAS 4 active rows; prod `next start` single bundle registers 4); 0 console errors.
**Status**: CODE + TESTS + LIVE-VERIFIED; **commit/push pending user — merge/deploy PENDING USER.**

### v3.36.0 — Swing steady-state serve fix (Sep 11 2026) — ✅ CODE + TESTS VERIFIED (swing **57/57** incl. 5 new; tsc 46 = exact baseline); LIVE-VERIFIED 5/5; COMMIT/PUSH PENDING USER (on `fix/workers-tasks-monitoring`, on top of PR #118 merge `10e2a00`; merge/deploy pending user)
**User directive**: plain `/api/recommendations/swing` loads were RE-RUNNING the full screener+AI pipeline at steady state (after a done/in-flight job existed) — burning NSE requests + OpenRouter tokens with no new data; Swing tab slow again.
**Root cause**: v3.13.0+ DB-job model (`SwingAnalysisJob`) still gates the DB fast-path on age → every plain load past the gate re-scanned and re-analyzed even when a terminal/fresh job already existed.
**Fix** (`lib/services/swingRecommendationService.ts` serve path ~L988-1135): existing **done/failed result served INDEFINITELY** (no age gate, no auto-regeneration on a plain load); **`force=1` is the ONLY regeneration trigger** (supersedes pending/running jobs AND kicks `checkSwingPerformance()` for the superseded job); new liveness semantics (`staleBefore = Date.now() - SWING_JOB_STALE_MS`; staleness anchor = `pending→createdAt`, `running→startedAt`, else `NaN`); **newest-TERMINAL (done/failed) fallback** when the newest job is fresh in-flight; **Prisma fallback** when the SQLite mirror is empty (serve a done job without re-scanning); **durable pending job on plain first load** (mirror+Prisma both empty — NO in-memory-only run).
**Tests**: swing **57/57** (5 new: old-done-served-indefinitely, newest-done-behind-stale-pending, Prisma-fallback-serve-without-rescan, durable-pending-on-first-load, force-perf-kick); tsc **46 = exact baseline (0 new)**; no migration; no new packages; diff 2 files +266/−12.
**Live-verified** (:3000): all 5 behavior points + console 64 msgs 0 errors (timeline in `.agents/changelog/versions-v3.36.md`).
**Status**: CODE + TESTS + LIVE-VERIFIED; **commit/push pending user — merge/deploy PENDING USER.**

### v3.35.0 — Flaky `intelligence.test.ts` CI fix (Sep 11 2026) — ✅ CODE + TESTS VERIFIED (targeted 13/13; full **86/86 suites / 1170 pass / 4 skip / 0 fail** — FIRST fully-green full run; tsc 46 = exact baseline); TEST-ONLY +21 — COMMITTED `2036724` + PUSHED to PR #118 branch `fix/leader-watchdog-self-heal` (on top of v3.34.1 merge `05b91e8`); MERGE/DEPLOY PENDING USER
**Root cause**: `setIntelligenceCache` (`lib/services/intelligence/cache.ts` :101-124) fires `prisma.intelligenceCache.upsert(...)` UN-AWAITED → the real-Postgres `beforeEach` `deleteMany` teardown (`intelligence.test.ts` :73-80) races the in-flight upsert → stale row → spurious `INTELLIGENCE_CACHE_HIT` → flaky failures at :187/:246/:279 (the "documented pre-existing flake" since v3.25.0, now FIXED).
**Fix**: NEW full prisma mock factory (`{ __esModule: true, default: { intelligenceCache: { findUnique/upsert/delete/deleteMany/count/findMany → jest.fn() } } }`) inserted between the `beforeEach` close and the Tests header; `jest.clearAllMocks()` clears call history but keeps impls.
**Verification**: targeted `intelligence.test.ts` **13/13 PASS**; full **86/86 suites / 1170 pass / 4 skip / 0 fail** — FIRST fully-green full run (no more pre-existing-flake asterisk); tsc **46 = exact baseline (0 new)**; no migration; no new packages. **Committed `2036724` + pushed; PR #118 merge/deploy pending user.**

### v3.34.1 — sql.js WASM async-load gate fix (Sep 11 2026) — ✅ CODE + TESTS VERIFIED (sqlite + cron-daemon 88/88 under CI=true --runInBand with zero "Cannot log after tests are done" noise; tsc 46 = exact baseline); COMMITTED `d91fb01` on `feat/db-health-monthly-ops` (merged into PR #118 branch `fix/leader-watchdog-self-heal`)
**Root cause**: `getSqlJs()` loaded `sql-wasm.wasm` via async `fs.readFile`/stream → sql.js init + stray logs landed after a Jest file finished.
**Fix**: synchronous `wasmBinary` load (`fs.readFileSync`, try/catch per candidate path); `getSqlJs()` memoized.
**Verification**: sqlite + cron-daemon **88/88** (CI=true --runInBand), zero noise; full **1154 pass / 4 skip / 1 fail** (pre-existing `intelligence.test.ts` flake); tsc **46 = exact baseline (0 new)**; no migration; no new packages. **Push/merge/deploy pending user.**

### v3.34.0 — Monthly Query Consumption — db-health monthly-ops window (Sep 11 2026) — ✅ CODE + TESTS VERIFIED (sqlite 83/83, targeted 94/94, tsc 46 = exact baseline); DOCS PHASE ~COMPLETE — COMMIT/PR PENDING USER
**User directive** (v3.31.0): plan limit **MONTHLY 200K ops/mo (resetting 2nd)** + Prisma calls only at 3 moments — the db-health panel only showed TODAY's ops; with a monthly plan that's the wrong unit and a restarted instance loses the month's history → **implements the deferred Plan 09 rev-v3 c/d monthly-ops window; `query_cache` STAYS deferred.**
**(1) Ledger module (pure)**: NEW `lib/services/opsMonthly.ts` — `OpsMonthlyState {monthKey, days}` on globalThis `__opsMonthly`; month key = `getIstDayKey().slice(0, 7)` (YYYY-MM); `getOpsMonthlyState()` lazily seeds + fresh ledger at month rollover; `foldOpsCounterIntoMonthly()` idempotent `Math.max` high-water merge (re-persisting a restored snapshot can never shrink); `buildQueryConsumption()` pure aggregation (live day merged over persisted — restarted instance keeps high-water, current-day replaced before summing, no double count; perDay newest-first ≤ 31; `DB_PLAN_LIMIT_OPS_MONTHLY` default 200_000). PURE module: only imports `getIstDayKey` from `@/lib/prisma`, NEVER invoked at module load (several suites mock `@/lib/prisma` without named exports).
**(2) Persistence** (`lib/sqlite.ts` +64): `SqliteFallback.persistOpsMonthly()`/`restoreOpsMonthly()` (iface :244-250) snapshot under `_backup_meta` key `"ops_monthly"` (`OPS_MONTHLY_KEY` :1653); restored in `initSqliteBackup()` (:1421) + persisted after initial sync (:1440) + 60s `startOpsCounterPersistence()` tick folds+persists (:1705); `restoreOpsMonthly` discards stale previous-month snapshots (never leaks prior-month numbers into a new month).
**(3) API/UI**: db-health GET `queryConsumption` block (+12) + "Monthly Query Consumption" card (+77: month key, MTD reads/writes/total, plan bar + remaining, today merged, per-day table); `.env.example` +5.
**(4) No hot-path change**: `$allOperations` keeps bumping the live counter; `lib/prisma.ts` diff = 6-line pointer comment only.
**Test-trap fixed (Lesson #114)**: sqlite.test.ts test-3 `resetSqliteStateForTests()` nulls state IN PLACE → next test `TypeError … 'persistTimeCorrection'`; fix = test-3 ends `await ensureSqliteBackup();` before `resetOpsMonthlyForTests()`.
**Tests**: sqlite **83/83**; targeted **94/94** across 7 suites (opsMonthly, dbHealthRoute, timeCorrection, backtestDataService, worker-engine, recommendationPerformanceService, recommendationCronService); tsc **46 = exact baseline (0 new)**; no schema change → no migration; diff 7 files +310/−2 + 2 new.
**Deferred**: `query_cache` (Plan 09 rev-v3 c/d). Branch `feat/db-health-monthly-ops` (work authored on committed v3.30.0 `653b617`, branch on `main` tip `7e21569`); **MERGED into PR #118 branch `fix/leader-watchdog-self-heal` per user-approved plan; PR #118 merge/push/deploy pending USER.**

### v3.33.1 — Swing performance touch-tracking fix (Sep 10 2026) — ✅ CODE + TESTS + VERIFIED (swing **27/27**; full **1154 pass / 4 skip / 1 fail**; tsc 46 = exact baseline); COMMITTED as own workstream commit (push/merge/deploy pending user)
**Root cause**: `checkSwingPerformance` evaluated target/stop hits using the LATEST CLOSE only → intraday touches that closed back inside the range never counted (missed exits / wrong "still open").
**Fix**: NEW `maxHighSincePosting`/`minLowSincePosting` on `SwingSignalStatusInput` (omit/null → close-only preserved); `checkSwingPerformance` builds `windowByTicker` from ONE `$queryRaw` over `daily_prices` (`ticker = ANY(${symbols}) AND "tradeDate" >= MIN(postedAt)`, ASC; per-signal JS filter); live-quote bridge captures `dayHigh`/`dayLow`; BUY intraday-touch target-wins tie; reason strings `touched … intraday (high/low X, close Y)` vs `crossed`; audit metadata +2.
**Tests**: `swingPerformanceService.test.ts` **27/27**; full **1154 pass / 4 skip / 1 fail** (pre-existing flake); tsc **46 = exact baseline (0 new)**; no migration; no new packages. Commit 2 files ONLY: `lib/services/swingPerformanceService.ts` + `lib/__tests__/swingPerformanceService.test.ts`. **Push/merge/deploy pending user.**

### v3.33.0 — Leader Watchdog Self-Heal (Sep 10 2026) — ✅ CODE + TESTS + VERIFIED (targeted 125/125, full **1154 pass / 4 skip / 1 fail**, tsc 46 = exact baseline); COMMITTED as own workstream commit (push/merge/deploy pending user)
**User directive**: "don't let this happen again" — prod scheduler dead since ~2026-09-08 07:06 UTC with NO automatic recovery (only manual admin "Start Engine" recovered).
**Root cause**: leadership was ONE-SHOT — `acquireLeaderLock()` at boot + `startLeaderHeartbeat(role, onLost)`; v3.28.2 STOPPED worker/cron engines on `onLost` but NOTHING ever re-acquired → after a lost/stale claim every instance stays a standby poller forever.
**Design**: NEW `watchLeaderRole(role, handlers)` (`lib/services/leader.ts`) — standby → adaptive probe (fresh foreign row = SLOW re-probe `LEADER_CLAIM_SLOW_MS` 300s; stale/absent = claim via `updateMany` count>0 | `create` | P2002 → false | `isDbUnavailableError` → fail-open) → re-probe OWN row (null/not-ours → `failOpenEvents++`) → **leader** + heartbeat `LEADER_HEARTBEAT_MS` 300s; renewal 0 → internal `onLost` → standby + `handlers.onLost` + FAST re-probe `LEADER_CLAIM_FAST_MS` 60s; `stop()` clears timers; phase-guard (no double `onAcquired`); steady-state = 1 `findUnique`/300s. `LEADER_STALENESS_MS` 15→**10 min**; NEW `LeaderWatchStatus`/`getLeaderWatchStatuses()` (globalThis `__leaderWatchStatus`, zero-Prisma). Wiring: worker/cron-daemon `onAcquired`/`onLost` (idempotent starts), sqlite-sync log-only. db-health: 7 leader imports at :8, GET leader block :207–225, POST :242; page `leaderWatch`/`leaderTuning` (no server-only import).
**Tests**: NEW `leaderWatch.test.ts` 8 + `instrumentation.test.ts` 7 rewritten + `dbHealthRoute.test.ts` +1 + `cron-daemon.test.ts` +1 (engine restart); constant fixes `leader.test.ts:68`/`sqlite.test.ts:282`; targeted **125/125**; full **1154 pass / 4 skip / 1 fail** (1 = documented pre-existing `intelligence.test.ts` flake); tsc **46 = exact baseline (0 new)**; no migration; no new packages. Spec/plan 11 (`.agents/specs/11-scheduler-self-heal.md` + `.agents/plans/11-scheduler-self-heal.md`) + ALL docs (versions-v3.33.md, AGENTS.md rows, CHANGELOG, TODO, Primer, agent-memory, Lessons #113, session-todos, HANDOFF, latest.md) committed with commit 1. **Push/merge/deploy pending user.**

### v3.32.1 — db-health POST double-`req.json()` hotfix (Sep 10 2026) — ✅ COMMITTED `bcde7ae` (+ docs `7e21569`); NEW `dbHealthRoute.test.ts` 5/5, tsc 46 = exact baseline — merged into PR #118 branch
**User directive**: none (post-merge triage of the v3.32.0 time-correction card — restore + set_time_correction both 400'd).
**Root cause**: POST `app/api/admin/db-health/route.ts` parses `req.json()` at the top (~:238) for `action`, then `restore` (~:272) and `set_time_correction` (~:434) RE-READ the body — a Web `Request` body stream is single-use (`bodyUsed` after the first `json()`) → the second read throws → both actions 400'd (`"Invalid restore payload"` — broken since v3.21.2; `"Invalid payload"` — inherited in v3.32.0, masking the real zod error).
**Fix (surgical, route only)**: hoisted `let action = "sync_sqlite"; let requestBody = {}; try { requestBody = (await req.json()) ... }` at POST top with `// v3.32.1 fix: parse the body ONCE here and reuse requestBody`; `restore` reuses `requestBody as { data?: string; file?: string }` (unparseable → `{}` → existing `"Missing base64 sqlite data"` 400), `set_time_correction` reuses `requestBody as { istDateTime?: string }` (missing → zod `"istDateTime is required"`).
**Tests**: NEW `lib/__tests__/dbHealthRoute.test.ts` **5/5** (node env; real `Request` via `jsonPost` helper enforcing `bodyUsed` — fails pre-fix); tsc **46 = exact baseline (0 new)**; no migration; no new packages.
**Live-verified** (Playwright :3000 admin db-health): Save Correction → `"Correction saved: server clock is 1 min SLOW (offset 1)"` + chip + footnote `"Active offset: 1 min"`; Clear → `"No correction saved — using the raw server clock"`; 0 console errors.
**Deferred**: live `probe_time` DB check (local Postgres not running); durable fix = correct `TZ`/`UTC` env on Netlify (v3.32.0).

### v3.32.0 — Admin Time Synchronisation (Sep 10 2026) — ✅ MERGED to `main` via PR #117 (`38a27bf`; `e74ae54` feat · `df7959d` docs · `b75deb0` docs update); post-merge hotfix in v3.32.1 (commit pending user)
**User directive**: "in admin db health screen display server time and db time. and also admin can enter ist time so if server time is misaligned it can be corrected through admin entered input for timezone corrections."
**Root cause**: Netlify host clock treats IST wall-clock as UTC → stored cron `nextRun` skew ≈ +5.5h; `calculateNextRun` (UTC-correct) takes `from` from the server clock.
**(1) DB probe + persistence** (`lib/sqlite.ts`): NEW `probeDbTimeNow()` (`SELECT NOW()` via `$queryRawUnsafe`, 10s timeout, `IST_OFFSET_MINUTES = 330`, `TIME_ALIGN_TOLERANCE_MS = 60_000`) + `persistTimeCorrection`/`deleteTimeCorrection`/`restoreTimeCorrection`/`persistTimeProbe`/`restoreTimeProbe` (`_backup_meta` keys `time_correction`/`time_probe_db`; types `:1753-1789`, interface `:247-257`, fallback `:5978-5983`).
**(2) Correction engine**: NEW `lib/services/timeCorrection.ts` (`offsetMinutes = trueNow − serverNow`; `applyOffset` identity@0; `getCorrectedNow()`/`getCronFrom()` lazy read-through, no cache; `getTimeDiagnostics()`); **REAL BUG FIXED**: `parseIstDateTimeLocal` round-trip guard → `toIstIso(parsed).slice(0,16) === input` (epoch = `Date.UTC(y,m-1,d,h,min) − offset`).
**(3) Device API/UI**: db-health POST `probe_time` (`route.ts:395-425`, 30s throttle → 200-throttled; PG-down → 200 `available:false`; audit `ADMIN_DB_SYNC`/`time-probe`) + `set_time_correction`/`clear_time_correction`; GET zero-Prisma `time` block; **Time Synchronisation card** (`page.tsx:247/505-515/1540`); `lib/audit.ts` +`ADMIN_DB_TIME_CORRECTION_SET`/`ADMIN_DB_TIME_CORRECTION_CLEARED`.
**(4) Scheduling wiring**: `getCronFrom()` (`recommendationCronService.ts` + `worker-engine.ts` nextRun sites `:598`/`:631`); `getCorrectedNow()` at due-claim `:645`/`:657`.
**Tests**: NEW `timeCorrection.test.ts` 20/20 (full manual `@/lib/sqlite` mock — no `...actual` spread, no `process.env.TZ`); sqlite.test.ts **71/71**; targeted **118/118**; full **1106 pass / 4 skip / 1 fail** (1 = documented pre-existing `intelligence.test.ts` flake); tsc **46 = exact baseline (0 new)**; no migration; no new packages; diff 7 modified +565/−10.
**Deferred**: live `probe_time` DB check (local Postgres not running); durable fix = correct `TZ`/`UTC` env on Netlify. **Merged to `main` via PR #117 (`38a27bf`; `e74ae54` feat · `df7959d` docs · `b75deb0` docs update); post-merge hotfix in v3.32.1 (commit pending user).**

### v3.31.0 — SQLite-first NSE read architecture + low-frequency Prisma sync (Sep 09 2026) — ✅ CODE + TESTS + VERIFIED, COMMITTED `9303bd7`→`653b617` (9 commits) + DOC COMMIT PENDING
**User directive**: plan limit is now **monthly 200K ops/mo (resetting 2nd)** + **Prisma calls ALLOWED ONLY at 3 moments — boot hydration, 6h SQLite→Prisma push, ONE hourly ops-usage write — zero between**.
**(1) sync_history ledger** (`9303bd7`): durable `sync_history` table (`direction` `prisma_to_sqlite`|`sqlite_to_prisma`, `trigger`, `leaderGated`, `rowsSynced`, `durationMs`, `error?`) + `recordSyncHistory()` (prune-100) + `recentSyncs` in health; also fixed the SCHEMA_SQL stray-`;`-in-`--`-comment sql.js parse error (root cause of v3.30.0 boot noise).
**(2) Boot hydration on every instance** (`d9bda6b`): `syncFromPrisma(opts)` `reason`=`boot|probe|admin` / `skipReconcile` / `leaderBypass` / `force`; `initSqliteBackup()` → `{reason:"boot", skipReconcile:true, leaderBypass:true}` — every instance pulls, reconcile only on probe/admin/force.
**(3) NSE rate guard** (`56ee538`): NEW `lib/services/nseRateGuard.ts` (single-flight, throttle, burst cooldown) + 6 tests; wired `nse-client.ts` + `market-cache.ts`.
**(4) `_sync_outbox` + 6h PUSH engine** (`a681a48`+`63736f5`): `_sync_outbox` table + `pushSqliteToPrisma` (grouped latest-op-wins, chunk-200 sinks, `reconcileControlToPrisma`); NEW `lib/sqlitePushSinks.ts`; 6h probe pivots PULL→PUSH. **Prod bug fixed**: `if (!isLeader("sqlite-sync"))` never fired (isLeader returns Promise) → awaited.
**(5) NSE captures SQLite+outbox only** (`f7e56b5`): auto-promote off, env-gated `NSE_PROMOTE_ENABLED=1`.
**(6) Jobs write SQLite-first (recs/swing/perf)** (`ae44431`+`0013dae`): 5 mirror tables + 7 write-through helpers; pipelines mirror-first with Prisma fallback.
**(7) Admin long-lived datasets SQLite-first** (`a2bffd9`): +7 `SqliteFallback` helpers + 4 admin CRUD routes flipped.
**(8) db-health wiring** (`653b617`): GET spreads `outboxPending`/`derivedCounts`/sync-history; POST `push_to_prisma`; db-health UI Outbox + Derived-counts cards + Push button + Direction/Trigger columns; 5 hot-route header comments document the read chain.
**Tests**: sqlite.test.ts **68/68** (11 new P8); instrumentation +1; NEW nseRateGuard 6; full **1105 pass / 4 skip / 1 fail** (1 = documented pre-existing `intelligence.test.ts` flake); tsc **46 = exact baseline (0 new)**; no schema change → no migration. **Deferred (plan rev-v3 c/d)**: `query_cache` + db-health monthly-ops window NOT implemented.

## Historical Log (moved out — read on demand)

> Version entries (v3.30.0 and older) and the old Session History (Sessions 1–23) moved to
> **`.agents/session-archive/primer-history-archive.md`** so this file stays readable.
> Recent status + recent sessions stay inline below.

---

## Pending Actions

1. [ ] Deploy and check Netlify Function logs
2. [ ] Set DATABASE_URL in Netlify environment variables
3. [ ] Verify site works after database connection fix
4. [ ] Check logs show ">>> FATAL: No valid DATABASE_URL" error

---

## CRITICAL - Database Setup Required

The app CANNOT work without a valid PostgreSQL database URL.

**Option 1: Set in Netlify Dashboard**
- Go to: Site Settings → Environment Variables
- Add: DATABASE_URL=postgresql://user:password@host:port/database

**Option 2: Use Prisma Postgres**
- Install Prisma Postgres extension in Netlify
- It will automatically set DATABASE_URL

**Option 3: Use a free PostgreSQL service**
- Neon, Supabase, Railway, etc.

---

## Notes

- Logger now exports named functions (info, warn, error, debug)
- Build command: `npx prisma generate && npm run quickbuild`
- Prisma 7 requires driver adapter or accelerateUrl
- Early logging added: check Netlify Function logs for `>>>` prefix


