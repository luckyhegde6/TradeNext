# Agent Memory - Activity Log

> This file tracks all agent activities. Use git hooks to automatically append activity logs.

---

## Git Hook Setup (v1.15.0)

The post-commit hook has been created automatically as part of the Handoff File System:

- **Location**: `.git/hooks/post-commit`
- **Function**: Logs commit checkpoints to `.agents/handoffs/checkpoint.log` (non-tracked file)
- **Automation**: Runs on every `git commit` automatically
- **⚠️ Important**: Post-commit hook writes to a NON-TRACKED file only to avoid infinite loop. Update `agent-memory.md` manually for meaningful activity entries.

---

### 2026-09-24 | v3.41.0 — Spec 16 Decision Engine core + POC A/B (Laya-only)

**Request**: user approved spec/plan 16 "Build engine core + POC A/B" (no SDK, no live smoke — P6003 plan-limit hold).

**Execution**: `lib/services/decision/` engine core (types/provider/gate/layaProvider/client/fusion; `DECISION_PROVIDER=none|laya`, unknown→warn+coerce none — D8; confidence-gated ACT/REVIEW via `shapeConfidence`; retry ≤3; inert evaluate → null) · audit `DECISION_EVALUATED` + `DECISION_GATE` only · POC A screener scoring + POC B Swing `gateAutoGenerate` (`DECISION_POC_ENABLED`-gated, off = byte-identical) · `POST /api/decision/evaluate` + `GET /api/admin/decision/ping` + OpenAPI (tag 'Decision Engine', header "v3.41.0, spec 16") + `/admin/decision` panel + nav · `typesafeProvider.ts` deleted (Jev docs-only) · `check-tsc-baseline` harness ENOENT fix (Lesson 135: `makeHarness()` stub tsconfig) · 7 NEW test files + harness test update.

**Verification**: `tsc` **46 = exact baseline** (0 decision-file errors) · lint **0** · `npm run test` **107/107 suites (1401 pass / 4 skip / 0 fail)** · quickbuild **188/188**. Docs pass done (AGENTS.md v3.41.0 row + pointer, CHANGELOG index, versions-index, NEW versions-v3.41.md, TODO.md, session-todos, Primer, Lessons 135 + Update Log, handoff latest.md, session flow/decisions). **Commit as v3.41.0 pending user approval (no push/PR).**

### 2026-09-22 | v3.40.5 — Laya / System-One decision-engine RESEARCH (docs-only, DONE)
- **Request**: research Laya (HF `convaiinnovations/laya`), Jev 1.13.0 (verify "published"), the Medium comparison article, and ALL TypeSafe.AI docs; create a detailed doc + durable `memory.md` (budget-preserving) so future sessions never re-scrape; afterwards build a decision engine from it. Session: docs-only (no spec/plan — research phase precedes the build).
- **Execution**: 5 parallel research subagents (foundations, primitives, patterns+demo, JS SDK, Laya/Jev/Medium). Findings collapsed into **`memory.md`** (durable cross-session reference — NOT an injected instruction file; `check-doc-sizes.mjs` verified 75.6/100 KB injected).
- **Key findings**: **Laya** = Apache-2.0, non-autoregressive "System 1" decision model, RLCD-trained, ~33 ms/pass, 100+ langs, 843 MB safetensors, NEVER generates text. **Jev 1.13.0 premise CORRECTED**: `github.com/answers-ai/jev` = **404** — Jev is TypeSafe's proprietary hosted API (`POST https://api.typesafe.ai/v1/systemone`, model `jev-latest`); NOT an OSS repo. **TypeSafe primitives**: Choice `{choice, probabilities[], confidence}`, Score `{score, probabilities[], confidence}`, Noul `{noul:0–1}`; confidence = f(probability shape); atomicy via backticked state paths; decision logic in code. **SDK**: `@typesafe-ai/sdk` v0.6.0 (breaking: `Score.criteria` = ordered tuple), Node 20+, env `TYPESAFE_API_KEY`/`TYPESAFE_BASE_URL=https://api.typesafe.ai`/`TYPESAFE_DEFAULT_MODEL=jev-latest`; `new TypeSafeClient().systemOne({state, questions})` → `res.answers["..."]`. **Medium article → HTTP 403** (recovered via search snippets only; gap recorded — Lesson 134).
- **New files**: `memory.md`; `docs/laya.md`; `docs/designDoc/ph22-laya-decision-engine-design.md` (research + proposed engine: provider-agnostic `lib/services/decision/` — Laya local + TypeSafe cloud, Choice/Score/Noul, confidence-gated ACT/REVIEW/ESCALATE, audit `DECISION_EVALUATED`/`DECISION_PROVIDER_FALLBACK`, admin panel; POC wiring = screener composite-scoring + Swing AI gatecheck; spec→plan→human approval REQUIRED before implementation).
- **Verification**: `check-doc-sizes.mjs` OK (75.6/100 KB); no code, no migration, no new packages. **Docs**: `AGENTS.md` v3.40.5 row, `.agents/changelog/versions-v3.40.md` §v3.40.5, `.agents/changelog/versions-index.md` row, `.agents/CHANGELOG.md` addenda, Lesson 134, Primer, this entry, session-todos, handoff. Next per user intent: spec + plan for the decision-engine build.

---

### 2026-09-23 | Laya → JS code-extraction plan + WIKI: Laya-based decision engine pages (docs/Wiki, DONE; engine code blocked)
- **Request**: (1) "mainly focus on the https://github.com/NandhaKishorM/laya its code extraction to js"; (2) follow-up: "jev is just a doc only, but want the implementation based of the laya and create a whole guide, the porting guide what reference what and how you did it and what all were there everything into wiki and add the reference of the https://github.com/NandhaKishorM/laya on the wiki as well and also add the architecture of the decision engine and how will help in taking decision for the new daily recommendation engine, swing and ipo analysis, stock ai analysis and for watchlist as well".
- **Execution (1)**: fetched the ENTIRE canonical repo source (`NandhaKishorM/laya` v0.3.6, main `c7527708`) module-by-module (`common.py`, `agent.py`, `router.py`, `lang.py`, `shortlist.py`, `presets.py`, `__init__.py`, `pyproject.toml`). Finding: repo = **inference/decode layer** (no NN; lives on HF) → ~84% pure-Python 1:1 TS port; only 2 runtime deps (same `tokenizer.json` via WASM; whole-model ONNX export → onnxruntime-node, laya-coreml-proven 189/189). NEW `docs/designDoc/ph22-laya-js-extraction-plan.md` (P0–P6 + fidelity gates) + `docs/laya.md` §5b + `memory.md` §1 + open-question #7 answered at research level.
- **Execution (2)**: **Wiki pushed `51c0db6`** (16 pages): NEW **Decision-Engine.md** (Laya-based architecture, confidence-gated ACT/REVIEW/ESCALATE, 5 surfaces — daily recommendation engine, Swing auto-generate, IPO analysis, stock AI analysis, watchlist/alerts) + NEW **Laya-Porting-Guide.md** (what references what module map, deps, parity, P0–P6, risks, NandhaKishorM/laya reference) + Home.md table + What's New; **Jev demoted to docs-only reference** (cross-links).
- **Verification**: wiki pages live-verified via webfetch (Decision-Engine + Home render, mermaid blocks OK). Repo docs updated: session-todos, handoff addendum, memory.md header. **Engine implementation re-opened by user (Laya-based)** but still blocked: spec 16 + plan 16 approval + P0 spike (model download + ONNX toolchain = sensitive ops requiring explicit permission).

---
- **Origin / scope (reviewed + approved)**: P6003 plan-limit hold until 2026-10-02 — a production deploy during the hold cold-starts a fresh instance with NO build-time guarantee that the live SQLite mirror (outbox, snapshot, Blobs backups) is preserved or pending mirror rows drain to Prisma → same served-empty class as v3.40.1 (Spec 14). Spec `.agents/specs/14-predeploy-mirror-preserve.md`, plan `.agents/plans/14-predeploy-mirror-preserve.md`, session `.agents/sessions/2026-09-19-predeploy-guard/`. Branch created from `main` @ `27c0770` (v3.40.2 MERGED via PR #129 `38ee7da`).
- **Design — build-time guard calls the live app**: `netlify.toml` build = `node scripts/predeploy/preserve-mirror.mjs && npx prisma generate && npm run quickbuild`. The script self-gates production (`CONTEXT === "production"` OR `--force`), soft-fails (warn + exit 0 — a deploy is NEVER blocked by preservation), 20 s timeout, URL chain `DEPLOY_PRIME_URL` → `URL` → `NEXT_PUBLIC_BASE_URL` → fallback, ONE summary line, token never logged.
- **Route ordering contract** (`app/api/admin/predeploy/preserve/route.ts`, `runtime="nodejs"`, `dynamic="force-dynamic"`): `getOutboxPending()` → snapshot (`persistMirrorSnapshot()` + `exportSqliteBackup()` + `uploadMirrorSnapshotToBlobs()`) → `createMirrorBackup(bytes)` → **breaker-closed only** `pushSqliteToPrisma({reason: "deploy", leaderGate: false})` → `createAuditLog({action: "ADMIN_DB_SYNC", resource: "predeploy-preserve"})`. Modes `"pushed" | "backed_up" | "skipped"` — on the P6003 hold the breaker is OPEN → deliberately `backed_up`; push throw → 500 `{error:"push_failed", mode:"skipped", snapshotBytes, backupKey, pendingBefore, detail}`; GET read-only `{success, breakerOpen, sqliteReady, pending, backups, keep}`.
- **Auth**: `x-deploy-guard-token` vs `DEPLOY_GUARD_TOKEN` (64-hex, length-checked `crypto.timingSafeEqual`; 503 `guard_token_not_configured` when unset + presented) + admin-session fallback `authorize(req)`; deployed (production) env var scopes `["builds","runtime"]`, site `78401e5d-b137-4b6d-94bb-ad1ec8de6b05`, temp file deleted — token NEVER echoed.
- **New code**: `lib/services/mirrorBackup.ts` (keys `backups/sqlite-mirror-<ISO>.sqlite`, `MIRROR_BACKUP_KEEP=5`, `MIRROR_BACKUP_MAX_BYTES=200 MiB`, `createMirrorBackup` fail-open `key|null`, `listMirrorBackups`/`pruneMirrorBackups` error-tolerant); `scripts/predeploy/preserve-mirror.mjs`; `lib/sqlite.ts` Phase 1 (`SyncTrigger "deploy"`, `MirrorBlobsStoreLike` exported/widened with `list?`/`delete?`, `getMirrorBlobsStore()` exported); `package.json` `predeploy:preserve`; `.env.example` `DEPLOY_GUARD_TOKEN=`; OpenAPI block (tags `Admin - Deploy`, `x-deploy-guard-token` header param).
- **Tests — 2 NEW suites / 25 passing**: `lib/__tests__/mirrorBackup.test.ts` (14) + `predeployPreserveRoute.test.ts` (11, GET diagnostics + POST auth 401/503 + breaker-open `backed_up` + push-fail 500 `push_failed`). Full `npm run test` **99/99 suites / 1334 pass / 4 skip / 0 fail** (v3.40.2 baseline 97/1309 + 25).
- **Verification**: `check-tsc-baseline.mjs` → **46/46, prod 0 → OK**; `npm run lint` → **0 errors** (1139 pre-existing warnings, 4 new files clean); `check-doc-sizes.mjs --json` → **ok, 74.9 / 100 KB**; `npm run quickbuild` → **185/185 COMPILED OK**.
- **Legacy finding (record-only)**: `.github/workflows/deploy.yml` publishes `./out` via `nwtgck/actions-netlify@v3.0` — NOT the real netlify.toml → quickbuild → `.next` deploy path; recorded in §v3.40.3 + CHANGELOG; DO NOT modify.
- **Docs**: Lessons **132/133** (ISO 2-digit-day `padStart` + deploy-time guard order/soft-fail/auth rules) + Update Log; `versions-v3.40.md` §v3.40.3 appended + §v3.40.1/§v3.40.2 statuses → **MERGED via PRs #128 (`15fa0a3`) / #129 (`38ee7da`)**; `versions-index.md` v3.40.3 row + PR fixes; `.agents/CHANGELOG.md` row 11 addenda (v3.40.2 + v3.40.3); `AGENTS.md` pointer verified correct (no edit); Primer + this entry + session-todos + handoff + session `decisions.md`/`flow.md`; TODO.md quick-reference refresh. No migration, no new packages.

---

### 2026-09-18 | v3.40.2 — mirror-contract fixes for BUGS 15/16/17 (branch `fix/mirror-contract-fixes`) — CODE + TESTS + DOCS DONE, commit/push/PR/deploy PENDING USER
- **Scope (user-approved)**: the 3 bugs found in the v3.40.1 live-site verification — "**15 + 16 + high-impact 17**" plus "**add auth to POST too**" (the workers/status heartbeat). Spec `.agents/specs/13-mirror-contract-fixes.md`, plan `.agents/plans/13-mirror-contract-fixes.md`, session `.agents/sessions/2026-09-18-v340ctx/`. Branch created from `main` @ `e183a3a` (v3.40.0 merge).
- **Bug 16 — a fallback branch is a second implementation of the contract**: NEW `lib/services/corpActionMirror.ts` `mapMirrorCorporateAction(row)` (+ `MirrorCorporateAction`) — tolerant snake→camel mapping (`company_name`→`companyName`, `ex_date`→`exDate`, `dividend_per_share`→`dividendPerShare`, …), idempotent for already-camelCase rows, numeric coercion, `null` for blanks, never throws. Applied to **BOTH** mirror branches of `app/api/corporate-actions/combined/route.ts` (`sqlite_mirror` + `sqlite_backup`) so the fallback renders the same shape as the primary Prisma path. `app/markets/calendar/page.tsx` — module-level `toDayKey()` (local Y-M-D, `""` on invalid) replaces `toISOString().split("T")[0]` at 3 key sites (22-Sep-2026 00:00 IST ex-date previously keyed `2026-09-21`, now `2026-09-22`; UTC-midnight unchanged).
- **Bug 15 — `/alerts` blank page**: `app/api/alerts/route.ts` NEW `getMirrorAlerts(userId)` (mirror rows camelCase-aliased + `condition` JSON-parsed, but span ALL users → filtered to the session user; `triggered`/`seen` coerced to booleans) wired into the list branch AND `action=count` behind `isDbUnavailableError` (non-hold errors still re-throw). `app/alerts/page.tsx` — `fetchAlerts` now `Array.isArray`-guards the response, sets `fetchError`, and renders an error branch with a Retry button before the empty state.
- **Bug 17-high — Prisma-only admin surfaces**: `app/api/admin/workers/status/route.ts` — GET **and POST** now require an admin session (401; POST was previously open/writable), GET falls back to `getSqliteFallback().getWorkerStatuses()` via NEW `mapMirrorWorkerStatus()` + `filterWorkers()` (5-min staleness, `Date | ISO string`, `?includeOffline=true`). `app/admin/utils/workers/page.tsx` — the 10 s `setInterval` poll became a self-scheduling backoff (10 s healthy → 20 → 40 → 60 s cap while failing) driven by `fetchData()`'s `Promise<boolean>` (`tasksRes.ok || workersRes.ok`) + a `pollPaused` amber "retrying with backoff" hint at ≥3 failures; the fixed interval had produced 186+ console errors per visit. `lib/services/dividendCalendarService.ts` — `fetchDividends` catch falls back to NEW `fetchMirrorDividends(startDate, endDate, limit)` (DIVIDEND-only, date-windowed, mapped, `currentPrice: null`) instead of `totalDividends: 0`.
- **Out of scope (BUGS.md row 17 follow-up)**: `/api/admin/users` (mirror has **no `user` table** → no fallback possible), `/api/admin/monitoring` (5 types), `/api/admin/workers` list, `/api/admin/cron`, `/api/screener/saved`.
- **Tests — 4 NEW suites / 26 passing**: `lib/__tests__/corpActionMirror.test.ts` (5), `dividendCalendarMirror.test.ts` (5, fake-timer-frozen clock + P6003/mirror/empty/non-hold/healthy paths), `alertsMirrorFallback.test.ts` (6, incl. 401 + session scoping + `action=count` + non-hold 500), `workersStatusRoute.test.ts` (10, GET/POST auth + mirror camelCase + `includeOffline` + stale filter).
- **Verification**: full `npm run test` **97/97 suites, 1309 pass / 4 skip / 0 fail** (baseline 93/1283 + these 4/26); `node scripts/dev-checks/check-tsc-baseline.mjs` → **46/46, prod 0 → OK (exit 0)**; `node scripts/dev-checks/check-doc-sizes.mjs --json` → **ok, 76,669 / 102,400 B**; `npm run lint` → **0 errors** (1,139 pre-existing warnings). Live `:3000`: `/alerts` signed-in renders tabs + "No alerts configured" with **0 console errors**; `/markets/calendar` SSR 200 (1.97 s); `/admin/utils/workers` SSR 200; IST day-key semantics proven by direct evaluation. The jest `FAIL: TypeScript regression` / `FAIL: injected context over budget` lines are the guard suites' negative-path output (test seam) — both guards run directly exit 0.
- **Docs**: `BUGS.md` rows 15/16 → ✅ Fixed (pending deploy), row 17 → 🟡 Partial + follow-up; `Lessons.md` **129/130/131** + Update Log; `app/api/openapi/route.ts` `/api/admin/workers/status` GET+POST entry (`securityAdmin`); `versions-v3.40.md` §v3.40.2 + `versions-index.md` row; Primer + this entry + session-todos + handoff + session `decisions.md`/`flow.md`. No migration, no new packages.

---

### 2026-09-18 | v3.40.1 LIVE-SITE VERIFICATION (post-merge) — admin-OTP walkthrough + public API checks; 3 new bugs found
- **Deploy confirmed**: PR #128 merged (`15fa0a3`), `main` @ `e183a3a`, Netlify deploy ready. Site verified with the **admin OTP login flow** (`lib/auth.ts:79-96` runs before any DB query, so it works despite the hold) + public no-auth endpoint checks.
- **v3.40.1 mechanism PROVEN live**: `/api/health` → `isNetlify: true`, `blobsContextAvailable: true`, `AWS_REGION us-east-2`; mirror restored from Blobs (**258 rows matching the seed**: corporate_action 40, recommendation_tracker 100, audit_log 50, worker_task 49); `/api/corporate-actions/combined` serves mirror rows; `/markets/analytics` Dividends section renders the 40 mirror records (direct restore proof).
- **Verdict — public surfaces healthy**: `/markets` FULLY LIVE (NIFTY 50 23,346.40 +0.33%, BANK/IT/NEXT 50/MIDCAP/SMALLCAP/AUTO); `/markets/analytics` **12 of 13 sections live** (breadth 2,387 advances / 1,154 declines / 112 unchanged / 3,653 total, 37-page table; only Insider Trading empty by source); `/markets/screener` live 2,000 stocks (fresh TV sync); `/news` 28 articles India/Global; `/recommendations` **Swing FULLY LIVE** (20 picks / 200 flagged / 34 screeners, real prices, `analysisStatus: pending`) while Today's Picks is empty (pre-existing stale daily run since Jul 19 — scheduler Standby); `/company/RELIANCE` live quote; `/portfolio` + `/watchlist` graceful empty states; admin console shells render but DB-dependent data APIs 500 (Prisma-only).
- **3 NEW bugs found (code paths verified against source)**: **(15)** `/alerts` blank-page CRASH — `/api/alerts` 500 `{error}` (`route.ts:57-62`) + unguarded `data.filter` (`page.tsx:112`) → React Error Boundary (header-only body, 112 chars); **(16)** `/api/corporate-actions/combined` SQLite-mirror fast path returns RAW snake_case rows while the Prisma path maps camelCase → `/markets/calendar` shows zero events despite 40 fetched rows (+ IST off-by-one), analytics/admin tables render `-`; **(17)** admin/user APIs Prisma-only under the hold — `/api/admin/workers/status` **no auth + no fallback** 500-loop (186-188 console errors), `/api/admin/users` 500 even WITH a valid session, monitoring 5×500, dividends calendar `totalDividends: 0`, `/api/screener/saved` 500.
- **Local enrichment attempt CLOSED as blocked**: the only `market_cache` writers (`getOrFetchNseData` callers) all fail/hang from this machine (NSE-side), so a local re-seed was skipped per the user's chosen option "Verify live pages + report".
- **Docs**: Lessons **129 + 130** + Update Log; `BUGS.md` 15/16/17; this entry; `Primer.md`; `.agents/session-todos.md`; `.agents/handoffs/active/latest.md`.

---

### 2026-09-18 | v3.40.1 — production recovery fix: SQLite-mirror deferred Blobs restore + Netlify runtime detection + health telemetry (branch `fix/production-analytics-rec-serve`)
- **User finding**: production cold start (2026-09-18) served empty analytics — every hot route fell back to the empty SQLite mirror and the boot `getMirrorBlobsStore()` failure was **memoized as null forever** (transient store-resolution failure → permanent null memo → the v3.39.0 disk→Blobs→fresh restore chain never reached Blobs on cold instances).
- **Fix 1 (root cause, `lib/sqlite.ts`)**: `getMirrorBlobsStore()` no longer memoizes a failure permanently — on throw it records `__sqliteMirrorBlobsStoreFailedAt = Date.now()` and re-attempts after `MIRROR_BLOBS_STORE_NEGATIVE_TTL_MS = 60_000`. **Fix 2 (deferred retry, one-shot 30s)**: NEW `scheduleDeferredMirrorRestore()`/`cancelDeferredMirrorRestore()`/`retryDeferredMirrorRestore()` — on boot with NO disk snapshot (`initSqliteBackup` after `logger.info "SQLite backup initialized"` and `fromSnapshot === false`) a `.unref()`'d one-shot `setTimeout(MIRROR_DEFERRED_RESTORE_DELAY_MS = 30_000)` retries `downloadMirrorSnapshotFromBlobs()`; `retryDeferredMirrorRestore()` clears `__sqliteMirrorBlobsStoreFailedAt = 0` BEFORE the download (else the 60s negative TTL blocks the 30s retry — Lesson 128). **Fix 3 (swap guard)**: restore only when `mirrorHasLiveData()` (counts `market_cache`/`corporate_action`/`daily_recommendation_run`; empty/throw → false) → closes old db, swaps `state.db`/`_instance`, re-runs column ensures, `persistMirrorSnapshot()` + `uploadMirrorSnapshotToBlobs()`.
- **Fix 4 (NEW `lib/netlify.ts`)**: `isNetlifyRuntime()` detects the Netlify environment WITHOUT `NETLIFY` env (prod observes only `AWS_REGION=us-east-2` + `ENVIRONMENT=production`) — checks `NETLIFY===`true``, `NETLIFY_BLOBS_REGION`, `NETLIFY_BLOBS_CONTEXT`, `NETLIFY_SITE_ID`, `NETLIFY_DEPLOY_ID`, `NETLIFY_AUTH_TOKEN`, `ENVIRONMENT==="production" && AWS_REGION`, `globalThis.netlifyBlobsContext`. **Fix 5 (`app/api/health/route.ts`)**: `isNetlify` + `blobsContextAvailable` fields; `NETLIFY_BLOBS_REGION` added to `SAFE_VARS` (deliberately NOT `NETLIFY_BLOBS_CONTEXT` — may carry tokens).
- **Correction vs earlier design**: deploy trace showed region-context resolution is ONE-Shot (no `retry(3)`), so the deferred restore is one-shot 30s (not 2-shot), unref'd, cancelled via `resetMirrorBlobsOverrides()`.
- **Tests**: NEW `lib/__tests__/netlify.test.ts` **11/11** (8 `isNetlifyRuntime` matrix + 3 `netlifyBlobsContextAvailable`); `sqliteMirror.test.ts` **8/8** (2 NEW golden v3.40.1 tests: deferred retry recovers into empty mirror / skips when live data exists — the skip test asserts `blobs.getCalls.length === getsBeforeRetry` get-count delta, NOT `.some(arrayBuffer)`, because boot 1's `restoreMirrorSnapshot()` already probes Blobs); `sqlite.test.ts` + `dbOpTiering.test.ts` 97/97 (incl. :2461 "continues disk-only without a Blobs store").
- **Verified**: full `npm run test` **93/93 suites / 1283 pass / 4 skip / 0 fail exit 0**; `npx tsc --noEmit` **46 = exact baseline (prod 0)**; `npm run quickbuild` clean **185/185 pages**; isolated `backtestDataService.test.ts` 16/16 in 1.6s — pre-existing Jest teardown noise ("Cannot log after tests are done") PROVEN pre-existing (fire-and-forget boot calls racing teardown), not caused by the new unref'd one-shot timer. Blobs store `tradenext-sqlite-mirror` seeded from `logs/sqlite-mirror.sqlite` (14,716,928 B, ETag `4bc5a64f...`) with sha256 round-trip verified.
- **Docs**: this entry + Lessons 128 + CHANGELOG row update + versions-v3.40.md v3.40.1 section + versions-index.md row + Primer + HANDOFF + session-todos + TODO Quick Reference. **Commit/push/PR/deploy pending user.**

---

### 2026-09-18 | PR #127 merged → Netlify deploy failure root-caused + fixed (emails plugin CJS/ESM) → retry deploy LIVE
- **PR #127 merged** by user (merge commit `48df4d4`; main tip `898a3f6` "docs: update changelog [skip ci]"). Netlify auto-deploy **failed**: `Bundling of function "emails" failed` — `/.netlify/functions-internal/emails/index.js` is CommonJS but the closest `package.json` (repo root) is `"type": "module"` → "Either use ESM syntax, rename to .cjs, or remove type:module". Build itself was green (185/185; `P6003 planLimitReached` messages are non-fatal plan-hold noise with SQLite fallback).
- **Root cause**: UI-installed legacy plugin `@netlify/plugin-emails@1.1.1` ("from Netlify app", NOT in repo/package.json) generates the emails function at build time. Prior in-repo attempts (commits `dbeba23` + `112b281` on main — `netlify/functions/package.json` commonjs + build-command tweak) FAILED (deploys 19:46/19:56 errored). The `11d6947` guard (build command pre-creates `.netlify/functions-internal/emails/package.json` `{"type":"commonjs"}`) got wiped — the plugin's onBuild regenerates that directory AFTER build.command. Verified via `netlify api getSite`: site plugins = `@netlify/plugin-nextjs` + `@netlify/plugin-lighthouse` only AFTER the user disabled the Emails extension in the UI. (MCP `get-extensions` catalog does NOT include plugin-emails — legacy plugin, not a managed extension — so extension-API removal wasn't the lever; the UI disable removed it.)
- **Fix/verify**: user disabled Emails extension in Netlify UI → retried deploy `6aacebd948646f5a24068933` (`context: production`, `commit_ref: 898a3f6` = v3.40.0 merge) → **state `ready`**, `published_at` 07:46:13Z, `deploy_time` 106s, available functions = ONLY `___netlify-server-handler` + `__csp-violations` (no emails), secrets scan 900 files / 0 matches. **v3.40.0 now LIVE on tradenext6.netlify.app.**
- **Post-merge branch cleanup**: `git fetch --prune` dropped stale `origin/feat/agentic-context-orchestration` ref (GitHub auto-deleted the remote branch on merge); local `feat/agentic-context-orchestration` (was `8cc839a`) deleted — the "keep till merged" condition is met. Lesson 127 added.
- **Working tree**: THIS entry uncommitted (docs only). No code changes in this session.

---

### 2026-09-18 | CodeQL js/incomplete-sanitization fixed in chunk-output.mjs + branch housekeeping (v3.40.0 pre-merge)
- **Action**: CodeQL flagged `scripts/dev-checks/chunk-output.mjs:59` — `escapeCell` escaped `|` but not `\` (`js/incomplete-sanitization`), so a data backslash before a pipe in tool output defeats the table-cell escape. Fix = backslash-first ordering: `value.replace(/\\/g, "\\\\").replace(/\|/g, "\\|")` (both `/g`) with the rationale in a comment. Regression test added — 3 fixtures, one per case, each special char on the chunk's **first** line (`renderIndex` only renders `chunk.lines[0]`; the first fixture attempt put `x|y`/`c\d` on lines 2–3 and never exercised the escaping). Suite re-run alone: **92/92 suites, 1270 passed / 4 skipped** (baseline was 1269). Lesson 126 added.
- **Branch cleanup (user-directed: keep PR #127 + its branch until merged)**:
  - Verified `fix/turbopack-tracing-harness` is an ancestor of `feat/agentic-context-orchestration` (v3.39.4 commits carried by PR #127) → deleted local + remote.
  - `feat/sqlite-durable-mirror`: `git log origin/main..branch` empty → fully merged → deleted local + remote.
  - `fix/sqlite-upsert-worker-undefined-bind`: unique commit `85c18d9` not an ancestor, but `git diff origin/main 85c18d9 -- …` empty (content = main's `797e449`) → deleted remote; **local kept** (branch tip has extra unmerged commits → LOST class).
  - **54 local branches fully contained in `origin/main`** deleted (verified per-branch with `git merge-base --is-ancestor` via `branch-delete.sh`).
  - **Kept**: `main`, `feat/agentic-context-orchestration` (until PR #127 merges), 2 dependabot remotes, and the LOST set (`buildfix`, `docs`, `feat8`, `fix/cleanup-todos-bugs`, `fix1`, `fix2`, `ph9`–`ph18` lineage, `` ph`7 ``) — deleting LOST would orphan unmerged commits.
  - Remotes now: `main`, `feat/agentic-context-orchestration` `37fa87c`, 2 dependabot.
- **Pending**: commit + push the CodeQL fix + docs (then re-poll CI — CodeQL Analyze should report the alert cleared on the new head).

### 2026-09-18 | Lint gate restored — `next lint` removal + FlatCompat crash fixed, 7 real hook/link/alias findings fixed, legacy debt triaged to warnings (v3.40.0 pre-merge)
- **Action**: `npm run lint` had been broken repo-wide for the whole Next 16 era (`"lint": "next lint"` → Next 16 removed the command → `Invalid project directory provided, no such directory: …\lint`); `eslint .` then crashed in the legacy `FlatCompat` bridge (`@eslint/eslintrc` `config-validator` → `TypeError: Converting circular structure to JSON`, ESLint 9.39.3 + `eslint-config-next` 16.3.5).
- **Fix**: `package.json` `"lint"` → `"eslint ."`; `eslint.config.mjs` rewritten to native flat configs (`eslint-config-next/core-web-vitals` + `/typescript`, no `FlatCompat`) with the override object registering its own plugins (`react`, `react-hooks`, `@typescript-eslint` — flat config scopes plugins per object) + `ignores` for `.next/.context/coverage/e2e-screenshots/logs/playwright-report/test-results/worker_logs`.
- **Real findings fixed in code**: 3 × `react-hooks/rules-of-hooks` — `app/admin/utils/ingest-csv/page.tsx` (both effects hoisted above the admin early return; `fetchHistory` hoisted too because `dealType` defaults truthy → the post-render callback would hit a TDZ reference in the loading state) and `app/components/analytics/GoogleAnalytics.tsx` (unconditional `useEffect`, gated internally, invalid-ID warn preserved); 2 × `@next/next/no-html-link-for-pages` — `app/markets/screener/advanced/page.tsx` breadcrumb `<a>` → `<Link>` (already imported); 2 × `@typescript-eslint/no-this-alias` — `lib/__tests__/dbOpTiering.test.ts` + `lib/__tests__/sqlite.test.ts` mocks → `run: (params: any[]) => this.run(sql, params)`.
- **Legacy debt triaged to `warn` (documented in config)**: `@typescript-eslint/no-require-imports` ×88 (CJS scripts / Jest setup+tests / deliberate server conditional loads), `react/no-unescaped-entities` ×21, react-hooks v6 React-Compiler rules `set-state-in-effect` ×7, `preserve-manual-memoization` ×3, `purity` ×2, `refs` ×1, `set-state-in-render` ×1. Follow-up cleanup = raise back to error + remove now-unused `eslint-disable` directives (75 `--fix`able).
- **Verified**: `npm run lint` **0 errors / 1139 warnings exit 0** A� `check-tsc-baseline.mjs` 46/0 delta +0 OK A� `npm run test` (alone) **92/92 suites, 1269 passed / 4 skipped** A� `npm run quickbuild` **185/185, 0 warning lines** A� `check-doc-sizes.mjs` **73.8 KB / 100 KB**. No migration; no new packages. Lesson 123 added.
- **CI follow-up 2 (PR #127 run 3)**: after the `.remember/` fix, `Run tests` went green but the W7 *"Build + Turbopack warning gate"* failed — it could **never** pass: `WARN=$(grep -i "warn" … | wc -l)` exits 1 on zero matches, and the Actions step runs `/usr/bin/bash -e` + `set -o pipefail`, so a CLEAN build aborted the step before printing `build warnings:`. Repro under git-bash `-e -o pipefail`; fix = `awk 'tolower($0) ~ /warn/ && $0 !~ /^npm warn/ {c++} END {print c+0}'` (exits 0 always) + `|| true` on the failure-branch grep; fixture-verified (clean → exit 0, dirty → exit 1). **PR #127 now fully green** (all 6 checks pass: context-budget, tsc-baseline, quality-gate, CodeQL ×2, GitGuardian, Playwright e2e). Lesson 125 added.
- **CI follow-up (PR #127)**: first CI run failed only in `quality-gate`'s Jest step — `lib/__tests__/chunk-output.test.ts` "ignores .remember/" expected `true`, got `false` (green locally). Cause: `.remember/` was ignored only by an **untracked** `.remember/.gitignore` (`*`), absent from a fresh checkout, so `git check-ignore` found no rule there. Fix = `/.remember/` added to the committed root `.gitignore` beside `/.context/` (comment explains why); re-verified `git check-ignore -v` → `.gitignore:66` and the suite **12/12**. Push note: HTTPS push rejected for OAuth `workflow` scope on `.github/workflows/quality-gate.yml` → pushed via the SSH remote URL instead (`gh auth refresh -h github.com -s workflow` restores HTTPS). Lesson 124 added.
- **Also verified for the pending PR**: `feat/sqlite-durable-mirror` is fully contained in HEAD (`git log HEAD..origin/feat/sqlite-durable-mirror` empty) and `fix/sqlite-upsert-worker-undefined-bind`'s only unique commit `85c18d9` is present as `797e449` (byte-identical `app/api/admin/nse-warm/route.ts` + `lib/nse-client.ts`; only the bot-trimmed CHANGELOG differs) → no cherry-picks needed. PR-vs-main = 45 files, +5365/−2009.

### 2026-09-18 | Agentic context + orchestration + harness — kills the remaining compaction loop, orchestrator prefers parallel subagents, subagent health monitor, handoff SCHEMA v1.1, code/test/build gates (v3.40.0)
- **Action**: v3.39.4 removed the injected-file refill (358→72 KB); this epic fixes the REMAINING loop — the **conversation transcript** (large tool outputs + long summaries re-sent every request, resident after compaction). User directive: >hotfix → own branch `feat/agentic-context-orchestration` (stacked on `fix/turbopack-tracing-harness @ ca56a74`), maintain decisions/handoff, use subagents in parallel where possible, make the orchestrator self-improve, improve agent-to-agent handoff, and upgrade the harness.
- **W1 tool-output protocol** (`1f73d3c`): NEW `.agents/rules/tool-output-protocol.md` + NEW `scripts/dev-checks/chunk-output.mjs` (`splitLines`/`planChunks`/`renderChunk`/`renderIndex`/`chunkOutput`; idempotent; line-ranges preserved; stale parts pruned; Node built-ins) + NEW `lib/__tests__/chunk-output.test.ts` (12 tests); registered in `.agents/INDEX.md`, cross-linked from `session-memory-rules.md` §7.
- **W2 durable memory** (`40181c4`, `db08255`): NEW `.agents/rules/durable-memory.md` (two-tier `.remember/now.md` + memory MCP; 7-value `entityType` vocabulary; query-before-read); `.remember/now.md` normalised. **Found + documented a real MCP defect**: `search_nodes` is a literal contiguous substring match over the WHOLE query (not tokenised/ranked/fuzzy) — `"Injected context budget doc-size"` → 0 hits, `"Injected context budget"` → 1, `"baseline"` → 2 → **empty result ≠ no memory**; retry a shorter query before re-reading a doc.
- **W3 compaction headroom** (`f758e67`): `.opencode/opencode.json` `compaction.reserved` **10000 → 30000** (user-approved).
- **W4 orchestrator** (`ac91571`): NEW `.agents/agents/orchestrator.md` + `.agents/commands/orchestrate.md`; `mode: "primary"` NOT subagent (subagents can't dispatch subagents), `tools` omitted to inherit defaults; `.agents/RULES.md` §0 parallel-dispatch rule; agents **16** (primary 3 = build/plan/orchestrator; subagents 13), commands **11**; Tier A (parallel) / Tier B (chunked-sequential).
- **W5+W6 subagent health + handoff** (`ac90b53`): NEW `.agents/agents/orchestrator-health.md` (per-kind budgets, liveness checks at 50 %/100 %, **max 1 retry**, taxonomy `provider-blocked | timeout | stalled | error | completed`); `.agents/handoffs/SCHEMA.md` **v1.1** (§8 `Subagent Status`, §9 `Handoff Summary`, additive-only BC); `agent-to-agent.md` Tier Downgrade Path; `latest.md` rewritten.
- **W7 harness** (`973cae5`): NEW `scripts/dev-checks/check-tsc-baseline.mjs` (total-vs-prod classification, regression → exit 1, `--json`, `--update`, exit 2 when tsc never ran, `TSC_BASELINE_CMD` test seam) + committed `tsc-baseline.json` `{total:46,prod:0,recorded:"2026-09-18"}`; `check-doc-sizes.mjs` +`checkInjectedBudget()`/`--json`/`.context/out` advisory; `.githooks/pre-commit` §8; `quality-gate.yml` NEW `context-budget` + `tsc-baseline` jobs + Build = Turbopack warning gate.
- **Phase 7 tests** (`6a9135a`): NEW `check-tsc-baseline.test.ts` (9) + NEW `check-doc-sizes.test.ts` (14) → **23/23**. **Incident (Lesson 120)**: the helper hardcoded the repo script path → "harness" tests ran the REAL script and the `--update` case **overwrote the committed baseline** with `{total:2,prod:1}`; fixed via explicit `run(script, …)` + `afterAll` byte-identity guards on the protected files; recovered with `git checkout`.
- **Verified**: `npx tsc --noEmit` **46 = exact baseline (prod 0)** · `check-tsc-baseline` OK exit 0 · `quickbuild` **0 Turbopack warnings / 185/185 pages** · doc budget **73.0 KB / 100 KB** · new suites **23/23** · quality-gate YAML parses. No migration; no new packages.
- **Honest limit (D3/D11)**: every `task` subagent type fails here — `Error from provider (Console): OpenCode's free tier can only be used from within OpenCode` → Tier A can't be demoed; orchestration is capability-detected and Tier B is the live path. **merge/PR pending user**.

### 2026-09-18 | Context-optimisation — slim injected instruction files (358 KB → 72 KB) + modular read-on-demand docs + Turbopack tracing → 0 warnings (v3.39.4)
- **Action**: Fixed the compaction loop at its root. `.opencode/opencode.json` → `instructions` injects the FULL contents of 5 files into every request ≈ **358 KB ≈ 90K tokens**; after each compaction they are re-injected instantly, refilling context (with `compaction.reserved` only 10000).
- **Slim** (NEW `.context/slim-docs.mjs`, marker-based + EOL-preserving): `AGENTS.md` 191,017 → **20,260 B** (version history → `.agents/changelog/versions-index.md`); `TODO.md` 134,054 → **20,792 B** (Quick Reference → `.agents/changelog/todo-quick-reference-archive.md`). Injected total **358 KB → 72.2 KB**. Context meter dropped 78% → 12%.
- **Modular docs**: NEW `.agents/INDEX.md` (topic→file manifest); NEW `.agents/session-archive/` — `Primer.md` 1159 → **213 lines** (version log ≤ v3.30.0 + Sessions 1–23 → `primer-history-archive.md`), `agent-memory.md` 1092 → **373 lines** (→ `agent-memory-archive.md`); archived via NEW `.context/chunk-history.mjs` (reversible; files git-tracked). `Lessons.md` deliberately NOT age-chunked — it is a live rulebook where old entries are still active rules.
- **Guards**: NEW context-budget rule in `.agents/rules/session-memory-rules.md` §7 (injected set ≤ ~100 KB, injected files must be thin indexes) + NEW `scripts/dev-checks/check-doc-sizes.mjs` (parses `instructions` from `opencode.json`; 100 KB total / 32 KB per-file budgets; exit 1 when over).
- **Turbopack**: all **33** `/*turbopackIgnore: true*/` sites applied — `lib/logger.ts` (9), `lib/services/ingestService.ts` (2), `lib/services/worker/worker-logger.ts` (14), `lib/sqlite.ts` (8, bare-fn form).
- **Verified**: `npx tsc --noEmit` **46 = exact baseline**; `npm run quickbuild` **BUILD_OK**, **0 Turbopack warnings** (was 33), 185/185 pages. No migration; no new packages (Node built-ins only). Branch `fix/turbopack-tracing-harness`; **commit pending user**.

### 2026-09-18 | Netlify deploy triage — `@netlify/plugin-emails` "emails" function bundling failure root-caused; `npm run quickbuild` mandatory pre-commit gate (v3.39.3, docs/config only)
- **Action**: Netlify deploy failed with `Bundling of function "emails" failed` — `.netlify/functions-internal/emails/index.js` emits CommonJS but the nearest package.json declares `"type": "module"` → esbuild bundling error. Investigation: `@netlify/plugin-emails` (a Netlify-UI-added integration, NOT in `package.json`/lockfile) creates the `emails` function AFTER the Next build and WIPES the pre-seeded in-directory `package.json` commonjs guard before bundling; the repo has NO `emails/` dir and ZERO `@netlify/emails` refs (real email path = nodemailer `lib/alerts/delivery/email.ts`).
- **Fix (user-apply, pending)**: remove/disable the `@netlify/plugin-emails` integration in the Netlify UI. Ruled out: dropping root `"type":"module"` (would break tracked ESM `.js` — `jest.setup.js`, `scripts/*.js`, `scripts/dev-checks/*.js`, `__mocks__/@openrouter/agent.js`).
- **Docs/config landed (this session)**: `.agents/RULES.md` (pre-staged, reference only) — Verify step 4 + §3 self-healing bullet + pre-push bullet now require `npm run quickbuild` ("Netlify deploys the same command; a broken build = blocked deploys"); `.agents/pre-commit-workflow.md` — NEW `□ LOCAL BUILD (v3.39.3 — Netlify deploy gate)` checklist block + Quick Commands `npm run quickbuild  # Production build (Netlify deploy gate)`; `AGENTS.md` — v3.39.3 table row (Sep 18 2026) + self-healing bullet adds `npm run quickbuild`; `TODO.md` — v3.39.3 Quick Reference row at top; `agent-memory.md` — this entry; `.agents/session-todos.md` — restructured to v3.39.3 Current (old v3.39.0/v3.39.1 preserved as carried-forward).
- **Deferred (documented, no code)**: Turbopack 33× `Dynamic filesystem access causes tracing of the whole project` workspace-misreport warnings → planned `/*turbopackIgnore: true*/` at dynamic fs call sites (`lib/logger.ts`, `lib/services/ingestService.ts`, `lib/services/worker/worker-logger.ts`, `lib/sqlite.ts`); `@netlify/plugin-nextjs` 5.15.8 → 5.16.0 upgrade available.
- **Files Modified (docs)**: `.agents/RULES.md` (pre-staged), `.agents/pre-commit-workflow.md`, `AGENTS.md`, `TODO.md`, `agent-memory.md`, `.agents/session-todos.md` — docs/config only; NO code in `lib/`/`app/`, NO `netlify.toml` change.
- **Status**: On `main`, HEAD `112b281`; commits `dbeba23` + `112b281` LOCAL only — behind origin/main by 1. **Push/PR/deploy PENDING USER** (no push/merge/deploy without explicit approval). Next: user removes `@netlify/plugin-emails` in Netlify UI → `npm run quickbuild` → redeploy.

### 2026-09-15/17 | SQLite Durable Mirror (v3.39.0, Spec 12) + WCAG/UI batch (v3.39.1) — committed on `feat/sqlite-durable-mirror` (push/PR/deploy PENDING USER)
- **Action**: Durable-mirror feature (v3.39.0) + UI polish batch (v3.39.1), committed + docs updated.
- **v3.39.0 (SQLite Durable Mirror)**: user directive — Prisma plan-limit hold until **2026-10-02**; the in-memory SQLite mirror + breaker-gated boot `syncFromPrisma()` lost hot-route data on EVERY restart. Fix: `lib/sqlite.ts` (+288/−14) — `state.db.export()` snapshot → disk `logs/sqlite-mirror.sqlite` (NEW `getMirrorSnapshotPath()` 762 + `os.tmpdir()` fallback for jsdom `resolveLogsDir()===''` — root-causes the stray repo-root artifact, `lib/logger.ts:5`); boot restore disk → Blobs → fresh + schema replay; cadence = eager post-sync + piggybacked on the existing 60s `OPS_PERSIST_INTERVAL_MS` timer (no new timers); dynamic `import("@netlify/blobs")` upload store `tradenext-sqlite-mirror`, 200 MB cap, digest gate, fail-open. Tests: `sqlite.test.ts` +239 (10 mirror + FakeBlobsStore) + NEW `sqliteMirror.test.ts` 6 golden REAL sql.js WASM. Full 89/89 / 1234 pass / 4 skip / 0 fail; tsc 46; no migration. Commits `e4623a3` + `e9db4a7`.
- **v3.39.1 (WCAG/UI batch)**: `lib/charting.tsx` NEW `INDEX_TV_TICKERS` — index chart buttons open TradingView (NIFTY→NSE-NIFTY, BANKNIFTY→NSE-BANKNIFTY, NIFTYIT→NSE-CNXIT, SENSEX→BSE-SENSEX, FINNIFTY→NSE-CNXFINANCE); stocks keep NSE Charting `?symbol=<SYM>-EQ`; flat-0.00 neutral gray pill in `StockQuoteHeader.tsx`; `INDICATOR_TEXT_CLASSES` (MA20 amber-700/300, MA50 violet-700/300, MA200 cyan-700/300 ≥4.5:1) + "Price Chart" h3→h2 in `NSEStockChart.tsx`; mobile-menu `aria-label` in `Header.tsx`; contrast tweaks in CorporateDataTabs/PiotroskiFScore/CompanyIntelligence/VerdictCard/UnifiedChart. 9 files +65/−32, committed `309deac`.
- **Status**: All committed on `feat/sqlite-durable-mirror` (on top of unreleased v3.39.x base fixes `2182687`/`f470e6d`/PR #123 `e84fb34` + PRs #124/#125). **Push/PR/deploy PENDING USER.**

### 2026-09-13 | Netlify deploy fix — `@netlify/blobs` RE-ADDED `^11.0.3` — PR #123 merged, deploy GREEN, live-verified 0 console errors (branch `docs-blobs-lessons` for docs commit; fix was on branch per user merge)
- **Action**: Triage + fix of the production deploy failure caused by the v3.38.2 dependency cleanup.
- **Root cause**: `07f42fe` (v3.38.2) removed `@netlify/blobs` from `package.json` (zero source refs — 56 grep matches all in CHANGELOG) but the Netlify **Edge bundler** imports it internally → build failed ("Build script returned non-zero exit code: 2", failed build `6aa6d681de472d000829d0eb`). Local `npm run quickbuild` does NOT exercise the Edge bundler — only the deploy confirms.
- **Fix**: `e84fb34` re-added `"@netlify/blobs": "^11.0.3"` (2 files +380/−4 — package.json + package-lock.json, blobs-11.0.3.tgz). Identical precedent `c1b27cb` fixed the same failure with `^10.7.7` — the package was re-added TWICE, it MUST stay in package.json.
- **Verification**: tsc 46 = exact baseline (0 new) + full build OK pre-fix-commit; PR #123 MERGED (`9166601`) → Netlify deploy GREEN (`#BEF9C6`) → live site dashboard serving with **0 console errors** (1 benign INFO-level report-only CSP notice @ `/_next/static/chunks/3zfpuu3o318py.js:5` — Next.js inline telemetry, nonce present, no action).
- **Files Modified (fix)**: `package.json` + `package-lock.json` (blobs RE-ADDED ^11.0.3).
- **Docs**: Primer.md Last Updated + Current Project Status blocks; HANDOFF.md YAML + Handoff Required incident paragraph; agent-memory.md entry — on branch `docs-blobs-lessons` (docs-only commit; no push to main).
- **Status**: ✅ RESOLVED + DEPLOYED + LIVE-VERIFIED. `main` at `86595f0` (clean). PR #121 (v3.38.x) + PR #118 remain merge/deploy PENDING USER. Lesson recorded: ALWAYS run full build + tsc baseline before EVERY commit; keep `@netlify/blobs`.

### 2026-09-13 | v3.38.2 — Dependabot vulnerability-fix batch: next/third-parties/nodemailer/morgan/csv-parse bumps, mysql2 override 3.24.4, @netlify/blobs removal, npm-audit 0 critical / 3 high (branch `fix/db-health-ops-count-manual-sync` on top of pushed v3.38.1 `36f2c9b`; package.json + package-lock.json only; full 88/88 suites / 1207 pass / 4 skip / 0 fail exit 0; tsc 46 = exact baseline; no migration; committed + pushed; PR #121 OPEN — merge/deploy PENDING USER)

### 2026-09-13 | v3.38.0 + v3.38.1 — Admin SET_OPS_COUNTER authority fix + db-health "Sync Operations Count" UI; Swing generatedAt push-sink NULL guard (Prisma 23502/23503) + AI-monitoring "Last hour" timeframe filter (branch `fix/db-health-ops-count-manual-sync`)
- **v3.38.0 (committed `d5645a9`)**: user directive — "In admin — update / set the monthly ops counter manually if needed" — the admin is the authority on actual Prisma-dashboard consumption, so the admin must be able to MANUALLY correct the ops-usage date + total used for plan display.
- **Fix (v3.38.0)**: (1) `lib/services/opsMonthly.ts` +42 — NEW `setOpsMonthlyDay(day)` / `setOpsMonthlyTotal(total)` mutators on `__opsMonthly` + `readOpsLedger()` refresh + `resetOpsMonthlyForTests()` flush. (2) `lib/audit.ts` +1 — `ADMIN_DB_SET_OPS_COUNTER`. (3) db-health POST `set_ops_counter` (`app/api/admin/db-health/route.ts` +75): zod `setOpsCounterPayload` validates `action` + `set-day`/`set-val` TOGETHER (`{action}` only → 400), `setOpsMonthlyTotal` → `dbOpsCounter.total = round(abs(total))` with `.reset = false` (live counter NOT zeroed — display-only override until next fold), fail-open `.catch` → 500. (4) db-health UI second card (`app/admin/utils/db-health/page.tsx` +110): day/total inputs + Update/Reset + authority tooltip "affects the ops-usage UI display and monthly plan usage — NOT the live counter itself" + `warn('unsafe')`.
- **v3.38.1 (6-file bundle, committed `d35343e`)**: user directive — db-health "Recent DB Errors" showed repeating Prisma 23502 NOT NULL on `swing_signals.create` every ~6h + the AI-monitoring "Last hour" filter appeared broken.
- **Root cause (v3.38.1)**: `upsertSwingAnalysisJob` bound `created_at` ONLY inside `payload` → SQLite mirror `generated_at` stayed NULL → every `pushSqliteToPrisma` swing sink passed `swing_signals.create` `generatedAt: :undefined` → 23502 NOT NULL (swing cards' target/stop blanked in the catch branch) + 23503 FK cascade (victory rows DESTROYED); Prisma 23502 reset → ResolverFailed → `runScheduledOps` `.finally` recoverySync → `recordDbError` ring. AI-monitoring: `getAiCallsMerged` never filtered the in-memory ring by timeframe (only `getAiStatsMerged` re-filtered after merging) → "Last hour" showed ~all calls.
- **Fix (v3.38.1)**: (1) NEW top-level `generatedAt: new Date()` binding on `upsertSwingAnalysisJob` (`lib/services/swingRecommendationService.ts` ~L1227) — mirror `generated_at` now populated. (2) `lib/sqlitePushSinks.ts` ~L497 — push sink gains `generatedAt` fallback `() => (row.generatedAt as string) ?? row.created_at` (cast `tbl.date(...)`) — heals pre-fix mirror rows; no more 23502/23503. (3) `lib/services/ai/ai-monitoring.ts` L331-340 — NEW memory-ring cutoff filter `cutoff = Date.now() - timeframeMinutes*60*1000` (skip `entry.timestamp < cutoff`) + passes `timeframeMinutes` to `getPersistedAiCalls`.
- **Files Modified (v3.38.1)**: `lib/services/ai/ai-monitoring.ts` (+11/−1); `lib/__tests__/ai-monitoring.test.ts` (+86 incl. `enqueueWriteBehind` mock); `lib/services/swingRecommendationService.ts` (+6); `lib/sqlitePushSinks.ts` (+6/−1); `lib/__tests__/sqlite.test.ts` (+30); `lib/__tests__/swingRecommendationService.test.ts` (+21); diff 6 files +157/−3.
- **Verification (v3.38.1)**: `ai-monitoring.test.ts` 5/5 (NEW timeframe-filter describe), `sqlite.test.ts` +30 (23502 regression: asserts `params[3]` created_at binding + `job-null-gen` row), `swingRecommendationService.test.ts` +21 (40/40); full **88 suites / 1207 pass / 4 skip / 0 fail**; tsc **46 = exact baseline (0 new)**; no migration; no new packages. Live :3000: AI-monitoring "Last hour" clean + 0 console errors.
- **Docs**: `.agents/changelog/versions-v3.38.md` NEW (v3.38.0 primary + v3.38.1 section); CHANGELOG index row, AGENTS.md rows (v3.38.1 + v3.38.0), TODO.md rows, agent-memory (this), HANDOFF yaml/paragraph, session-todos v3.38.1 header, Primer + Lessons entries — ALL APPLIED + committed with docs `36f2c9b`.
- **Status**: CODE + TESTS + LIVE-VERIFIED + DOCS DONE; feature committed `d35343e` (6 files +157/−3) + docs committed `36f2c9b` (9 files +206/−3); branch PUSHED; **PR #121 OPEN** (https://github.com/luckyhegde6/TradeNext/pull/121); merge/deploy PENDING USER.

### 2026-09-12 | v3.37.0 — Worker engine monitoring fixes 1–5 (issue #119) — long-task busy heartbeat + dead worker-logs route + Scheduler Leadership card + cron-ledger running-guard + daemon leader cross-check (branch `fix/workers-tasks-monitoring`)
- **Root cause (Fix 1)**: the 5-min liveness heartbeat (`writeHeartbeat("live")`) wrote SQLite ONLY → Prisma `worker_status.lastHeartbeat` went stale ~10 min in → a cross-instance reaper (`worker-169.254.80.183`) legitimately-but-wrongly reaped the `run_daily_recommendations` worker mid-batch (recovery via `checkScheduledJobs` re-claim masked the symptom).
- **Fix**: (1) NEW `TASK_HEARTBEAT_MS = 240_000` per-task **BUSY heartbeat** (`updateHeartbeat("busy", task.id)` — unref'd interval, cleared in the task `finally`, also fires at the 60s async `setTimeout` poll) refreshes Prisma `worker_status.lastHeartbeat` every 4 min so a long task can never be reaped mid-flight; `TASK_TIMEOUT_MS` 40→**240 min** last-resort only. (2) NEW `app/api/admin/workers/logs/route.ts` (GET list `{files}`, GET `?taskId=` `{content}` with `MAX_TASK_ID_LEN = 128`, DELETE `?taskId=` `{deleted}`; `auth()+admin();` — the Logs sub-tab dead since v3.14.0 because the route never existed). (3) `app/admin/utils/workers/page.tsx` — `leader-*` rows → NEW **"Scheduler Leadership"** card (holding = `lastHeartbeat < 10 min`), filtered OUT of the Active Workers grid. (4) `worker-service.ts` L148-167 — `recordSystemRunOutcome` EXPORTED + only `task?.cronJobId && task.status === "running"` records; timeout/reap paths write the ledger BEFORE the failed-status write; try/catch → `logger.warn` non-fatal (no more double-count / amend-completed). (5) `app/api/admin/cron/daemon/route.ts` — daemon ALSO refreshes the shared `leader-cron-daemon` row; route cross-checks BOTH rows (`Promise.all`) + `leaderFresh` within `LEADER_STALENESS_MS` (10 min).
- **Files Modified**: `lib/services/worker/worker-engine.ts` (+72), `lib/services/worker/worker-service.ts` (+11), `app/admin/utils/workers/page.tsx` (+40), `app/api/admin/workers/logs/route.ts` (NEW +52), `app/api/admin/cron/daemon/route.ts` (+31); `lib/__tests__/worker-engine.test.ts` (+254), NEW `lib/__tests__/cronDaemonRoute.test.ts` (4), NEW `lib/__tests__/worker-service.test.ts` (7); diff 6 files +407/−53 + 2 new test files.
- **Verification**: `worker-service.test.ts` **7/7** + `cronDaemonRoute.test.ts` **4/4** + `worker-engine.test.ts` **23/23**; full **88/88 suites / 1192 pass / 4 skip / 0 fail**; tsc **46 = exact baseline (0 new)**; no migration; no new packages. Live :3000 (Workers tab, Logs tab, Scheduler Leadership 3/3 on `LAPTOP-HM25SVAR-19820`, cron chip "Scheduler Live" + heartbeat ~102s, daemon route 200 `running:true`, 0 console errors; route `registeredJobs` "0" = Turbopack dev module-split artifact — `localhost.cron_job` HAS 4 active rows, prod `next start` shows 4).
- **Status**: CODE + TESTS + LIVE-VERIFIED; commit + push PENDING (2 commits: code + docs [skip ci]); merge/deploy PENDING USER.

### 2026-09-11 | v3.36.0 — Swing steady-state serve fix — done/failed analysis jobs served indefinitely, `force=1`-only regeneration with performance kick (issue #119 branch)
- **Root cause**: plain `/api/recommendations/swing` loads RE-RAN the full screener+AI pipeline at steady state — after a done/in-flight `SwingAnalysisJob` existed, every load past the DB fast-path's age gate re-scanned Chartink + re-analyzed the top-20 with OpenRouter, burning NSE requests + tokens with no new data (Swing tab slow again; the v3.13.0+ DB-job model only gates the fast-path on age).
- **Fix**: `lib/services/swingRecommendationService.ts` serve-path rewrite (~L988-1135): existing done/failed result served **INDEFINITELY** (no age gate, no auto-regen on a plain load); **`force=1` = the ONLY regeneration trigger** (supersedes pending/running jobs AND kicks `checkSwingPerformance()` for the superseded job); liveness `staleBefore = Date.now() - SWING_JOB_STALE_MS` (anchor `pending→createdAt`, `running→startedAt`, else `NaN`); **newest-TERMINAL (done/failed) fallback** when the newest job is fresh in-flight; **Prisma fallback** when the SQLite mirror is empty (serve a done job without re-scanning); **durable pending job on plain first load** (mirror+Prisma both empty — NO in-memory-only run).
- **Files Modified**: `lib/services/swingRecommendationService.ts`; `lib/__tests__/swingRecommendationService.test.ts` (5 new tests); diff 2 files +266/−12.
- **Verification**: swing **57/57** (5 new: old-done-served-indefinitely, newest-done-behind-stale-pending, Prisma-fallback-serve-without-rescan, durable-pending-on-first-load, force-perf-kick); tsc **46 = exact baseline (0 new)**; no migration; no new packages. Live-verified :3000 all 5 behavior points (timeline in `.agents/changelog/versions-v3.36.md`).
- **Status**: CODE + TESTS + LIVE-VERIFIED; commit + push PENDING (2 commits: code + docs); merge/deploy PENDING USER.

### 2026-09-11 | v3.35.0 — Flaky `intelligence.test.ts` CI fix — prisma mock isolates the fire-and-forget `IntelligenceCache` upsert (zero real-DB writes in tests)
- **Root cause**: `setIntelligenceCache` (`lib/services/intelligence/cache.ts` :101-124) fires `prisma.intelligenceCache.upsert(...)` UN-AWAITED → the real-Postgres `beforeEach` `deleteMany` teardown (`intelligence.test.ts` :73-80) races the in-flight upsert → stale row → spurious `INTELLIGENCE_CACHE_HIT` → flaky failures at :187/:246/:279 (the "documented pre-existing flake" since v3.25.0, now FIXED).
- **Fix**: NEW full prisma mock factory (`{ __esModule: true, default: { intelligenceCache: { findUnique/upsert/delete/deleteMany/count/findMany → jest.fn() } } }`) inserted between the `beforeEach` close and the Tests header; `jest.clearAllMocks()` clears call history but keeps impls.
- **Files Modified**: `lib/__tests__/intelligence.test.ts` (test-only +21; single-file surgical change).
- **Verification**: targeted `intelligence.test.ts` **13/13 PASS**; full **86/86 suites / 1170 pass / 4 skip / 0 fail** — FIRST fully-green full run (no more pre-existing-flake asterisk); tsc **46 = exact baseline (0 new)**; no migration; no new packages.
- **Commit**: `2036724` on PR #118 branch `fix/leader-watchdog-self-heal` (on top of v3.34.1 merge `05b91e8`); pushed per user approval; PR #118 merge/deploy pending user.

### 2026-09-11 | v3.34.1 — sql.js WASM async-load gate fix — zero "Cannot log after tests are done" noise (commit `d91fb01` on `feat/db-health-monthly-ops`, merged into PR #118 branch `fix/leader-watchdog-self-heal`)
- **Action**: `getSqlJs()` loaded `sql-wasm.wasm` via async `fs.readFile`/stream → sql.js init + stray "Cannot log after tests are done" lines landed after a Jest file finished. Fix: synchronous `wasmBinary` load (`fs.readFileSync`, try/catch per candidate path) so sql.js boots fully before any test finishes; `getSqlJs()` memoized.
- **Files Modified**: `lib/sqlite.ts` (getSqlJs WASM loading; single-file surgical change).
- **Verification**: sqlite + cron-daemon **88/88** under `CI=true --runInBand` with zero log-after-tests noise; full **1154 pass / 4 skip / 1 fail** (1 = documented pre-existing `intelligence.test.ts` flake); tsc **46 = exact baseline (0 new)**; no migration; no new packages.
- **Commit**: `d91fb01` on `feat/db-health-monthly-ops` (merged into PR #118 branch).

### 2026-09-11 | v3.34.0 — Monthly Query Consumption: db-health monthly-ops window (deferred Plan 09 rev-v3 c/d) — branch `feat/db-health-monthly-ops` (on top of committed v3.30.0 `653b617`); MERGED into PR #118 branch `fix/leader-watchdog-self-heal` per user-approved plan; PR #118 merge/push/deploy pending USER
- **Action**: Implemented the deferred "db-health monthly-ops window" from Plan 09 rev-v3 c/d. **User directive (v3.31.0)**: plan limit MONTHLY 200K ops/mo (reset on the 2nd) + Prisma calls only at 3 moments — db-health only showed TODAY's `dbOpsCounter`, the wrong unit for a monthly plan, and the counter resets per process/deploy so a restart loses the month's history. **(1) Ledger module (pure)** — NEW `lib/services/opsMonthly.ts`: `OpsMonthlyState {monthKey, days}` on globalThis `__opsMonthly` (mirrors `dbOpsCounter` module-graph pattern); month key = `getIstDayKey().slice(0, 7)`; `getOpsMonthlyState()` lazy-seeds and starts a fresh ledger at IST month rollover (prior month dropped — plan resets monthly); `foldOpsCounterIntoMonthly()` merges a day idempotently via `Math.max` (high-water — re-persisting a restored snapshot can never shrink); `buildQueryConsumption()` pure aggregation behind the route (merges live day over persisted → restarted instance keeps high-water; replaces current-day entry before summing → no double count; perDay newest-first ≤ 31; `DB_PLAN_LIMIT_OPS_MONTHLY` default 200_000). PURE module contract: only import is `getIstDayKey` from `@/lib/prisma`, NEVER invoked at module load (several suites mock `@/lib/prisma` without named exports and pull the module in via `lib/sqlite.ts`). **(2) Persistence** (`lib/sqlite.ts` +64) — `SqliteFallback.persistOpsMonthly()`/`restoreOpsMonthly()` (iface :244-250) snapshot under `_backup_meta` key `"ops_monthly"` (`OPS_MONTHLY_KEY` :1653); `initSqliteBackup()` restores before initial sync (:1421) + persists after (:1440); the 60s `startOpsCounterPersistence()` tick also folds+persists (:1705); `restoreOpsMonthly` discards a stale previous-month snapshot (monthKey mismatch — never leaks prior-month numbers into a new month); fallback exports :6037-6041. **(3) API/UI** — db-health GET `queryConsumption` block (`buildQueryConsumption(getOpsMonthlyState(), dbOpsCounter, planLimitMonthly)`; +12, zero Prisma ops) + "Monthly Query Consumption" card (`page.tsx` +77: month key, MTD reads/writes/total, plan-usage bar + remaining, today's merged row, per-day table ≤ 31); `.env.example` +5. **(4) No hot-path change** — `$allOperations` keeps bumping the live counter; `lib/prisma.ts` diff = 6-line pointer comment only. **Test-trap fixed (Lesson #114)**: sqlite.test.ts monthly describe test-3 called `resetSqliteStateForTests()` which nulls real module state IN PLACE without re-init → next test crashed `TypeError: Cannot read properties of null (reading 'persistTimeCorrection')`; fix = test-3 ends `await ensureSqliteBackup();` BEFORE `resetOpsMonthlyForTests()`.
- **Files Created**: `lib/services/opsMonthly.ts`, `lib/__tests__/opsMonthly.test.ts`, `.agents/changelog/versions-v3.34.md`, `.agents/sessions/2026-09-11-monthly-ops/` (decisions + flow).
- **Files Modified**: `lib/sqlite.ts` (+64), `app/api/admin/db-health/route.ts` (+12), `app/admin/utils/db-health/page.tsx` (+77), `.env.example` (+5), `lib/prisma.ts` (+6 comment), `lib/__tests__/sqlite.test.ts` (+92 monthly describe), `lib/__tests__/dbHealthRoute.test.ts` (+56 queryConsumption) + docs (AGENTS.md v3.34.0 row, CHANGELOG index + versions-v3.34.md, TODO.md row, HANDOFF.md, Primer.md, Lessons #114, session-todos, latest.md handoff, sessions flow/decisions, agent-memory (this)).
- **Verification**: sqlite suite **83/83** (incl. daemon-sqlite-first.test.ts); targeted **94/94** across 7 suites (opsMonthly, dbHealthRoute, timeCorrection, backtestDataService, worker-engine, recommendationPerformanceService, recommendationCronService); tsc **46 = exact baseline (0 new)**; no schema change → no migration; no new packages; diff 7 files +310/−2 + 2 new files.
- **Deferred**: `query_cache` (Plan 09 rev-v3 c/d) — separate later increment.
- **Commit**: `5d754b7` + `d91fb01` merged into PR #118 branch `fix/leader-watchdog-self-heal` per user-approved plan; PR #118 push/merge/deploy PENDING USER (no push/merge/deploy without explicit approval).

### 2026-09-10 | v3.33.1 — Swing performance touch-tracking fix — intraday HIGH/LOW touches count (on `fix/leader-watchdog-self-heal`; own commit by workstream pending user)
- **Action**: `checkSwingPerformance` evaluated hits using the LATEST CLOSE only → an intraday touch that closed back inside the range was never counted (missed exits). NEW `maxHighSincePosting`/`minLowSincePosting` (close-only preserved); ONE `$queryRaw` `daily_prices` window (`ticker = ANY(${symbols}) AND "tradeDate" >= MIN(postedAt)`, ASC; per-signal JS filter); live-quote bridge captures `dayHigh`/`dayLow`; BUY intraday-touch target-wins; reason strings `touched … intraday` vs `crossed`; audit metadata +2.
- **Files Modified**: `lib/services/swingPerformanceService.ts` + `lib/__tests__/swingPerformanceService.test.ts` ONLY.
- **Verification**: `swingPerformanceService.test.ts` **27/27**; full **1154 pass / 4 skip / 1 fail** (pre-existing `intelligence.test.ts` flake); tsc **46 = exact baseline (0 new)**; no migration.
- **Commit**: own workstream commit pending user (push/merge/deploy blocked).

### 2026-09-10 | v3.33.0 — Leader Watchdog Self-Heal — scheduler dead must never happen again (on `fix/leader-watchdog-self-heal`, on top of committed v3.32.1 `bcde7ae`; own commit by workstream, push/merge/deploy pending user)
- **Action**: Executed the user directive "don't let this happen again" — prod scheduler dead since ~2026-09-08 07:06 UTC with NO auto-recovery. **Root cause**: one-shot leadership (`acquireLeaderLock` at boot + `startLeaderHeartbeat(role, onLost)`); v3.28.2 stops engines on `onLost` but nothing re-acquires → standby pollers forever; only manual admin "Start Engine" recovered. **Fix**: NEW `watchLeaderRole(role, handlers)` self-healing loop (`lib/services/leader.ts`) — standby → adaptive probe (fresh foreign row = SLOW 300s re-probe; stale/absent = claim via `updateMany` count>0 | `create` | P2002 → false | `isDbUnavailableError` → fail-open) → re-probe OWN row (null/not-ours → `failOpenEvents++`) → **leader** + heartbeat `LEADER_HEARTBEAT_MS` 300s; renewal 0 → internal `onLost` → standby + `handlers.onLost` + FAST re-probe `LEADER_CLAIM_FAST_MS` 60s; `stop()`; phase-guard (no double `onAcquired`); steady-state = 1 `findUnique`/300s. `LEADER_STALENESS_MS` 15→**10 min** (human-approved); NEW `LeaderWatchStatus`/`getLeaderWatchStatuses()` (globalThis `__leaderWatchStatus`, zero-Prisma). Wiring (`instrumentation.ts`): worker/cron-daemon `onAcquired`/`onLost` (idempotent starts), sqlite-sync log-only. db-health: route 7 leader imports :8, GET leader block :207–225, POST :242; page `leaderWatch`/`leaderTuning` (no server-only import).
- **Files Created**: `lib/__tests__/leaderWatch.test.ts` (8), `.agents/specs/11-scheduler-self-heal.md`, `.agents/plans/11-scheduler-self-heal.md`, `.agents/changelog/versions-v3.33.md`.
- **Files Modified**: `lib/services/leader.ts`, `instrumentation.ts`, `app/api/admin/db-health/route.ts`, `app/admin/utils/db-health/page.tsx`, `lib/__tests__/instrumentation.test.ts` (7 rewritten), `lib/__tests__/dbHealthRoute.test.ts`, `lib/__tests__/cron-daemon.test.ts`, `lib/__tests__/leader.test.ts` (:68), `lib/__tests__/sqlite.test.ts` (:282) + docs (AGENTS.md rows, CHANGELOG index, versions-v3.33.md, TODO rows, Primer, agent-memory (this), Lessons #113, session-todos, HANDOFF, latest.md).
- **Verification**: targeted **125/125**; full **1154 pass / 4 skip / 1 fail** (1 = documented pre-existing `intelligence.test.ts` flake); tsc **46 = exact baseline (0 new)**; no migration; no new packages.
- **Commit**: own workstream commit pending user (push/merge/deploy blocked).

### 2026-09-10 | v3.32.1 — db-health POST body-parsed-once hotfix (restore + set_time_correction 400s) — on `main`, post-merge of v3.32.0 PR #117 `38a27bf` (HEAD `b75deb0`); working tree = route fix + regression test + full v3.32.1 doc set, commit pending user
- **Action**: Post-merge hotfix. **Root cause**: POST `app/api/admin/db-health/route.ts` reads `req.json()` at top (~:238) for `action`, then `restore` (~:272) and `set_time_correction` (~:434) RE-READ the body — a Web `Request` body stream is single-use (`bodyUsed` after first `json()`) → second read throws → both 400'd as `"Invalid restore payload"`/`"Invalid payload"` (`restore` broken since v3.21.2; `set_time_correction` inherited the pattern in v3.32.0). **Fix (surgical, route only)**: hoisted `let action = "sync_sqlite"; let requestBody = {}; try { requestBody = (await req.json()) ... }` at POST top with `// v3.32.1 fix: parse the body ONCE here and reuse requestBody`; `restore` reuses `requestBody as { data?: string; file?: string }` (unparseable → `{}` → existing `"Missing base64 sqlite data"` 400); `set_time_correction` reuses `requestBody as { istDateTime?: string }` (missing → zod `"istDateTime is required"`, was masked `"Invalid payload"`).
- **Files Created**: `lib/__tests__/dbHealthRoute.test.ts` (5 tests — real `Request` via `jsonPost` helper enforcing `bodyUsed`).
- **Files Modified**: `app/api/admin/db-health/route.ts` (route only) + docs (AGENTS.md v3.32.1 row + v3.32.0 MERGED, CHANGELOG index + versions-v3.32.md v3.32.1 section, TODO.md row, HANDOFF.md, Primer.md, Lessons #112, session-todos, latest.md handoff, sessions flow/decisions, agent-memory (this)).
- **Verification**: `dbHealthRoute.test.ts` **5/5**; tsc **46 = exact baseline (0 new)**; no migration; no new packages. **Live-verified** (Playwright :3000 admin db-health): Save Correction → `"Correction saved: server clock is 1 min SLOW (offset 1)"` + chip + footnote `"Active offset: 1 min"`; Clear → `"No correction saved — using the raw server clock"`; 0 console errors.
- **Deferred**: live `probe_time` DB check; durable Netlify `TZ`/`UTC` env fix.
- **Commit**: pending user approval (no push/merge/deploy).

### 2026-09-10 | v3.32.0 — Admin Time Synchronisation: server/DB time display + IST correction (✅ MERGED to `main` via PR #117 `38a27bf`; `e74ae54` feat · `df7959d` docs · `b75deb0` docs update; post-merge hotfix in v3.32.1)
- **Action**: Executed the user directive "in admin db health screen display server time and db time. and also admin can enter ist time so if server time is misaligned it can be corrected through admin entered input for timezone corrections". **Root cause**: Netlify host clock treats IST wall-clock as UTC → stored cron `nextRun` skew ≈ +5.5h; `calculateNextRun` (UTC-correct) takes `from` from the server clock. **(1) `lib/sqlite.ts` probe + persistence** — `probeDbTimeNow()` (`SELECT NOW()` via `$queryRawUnsafe`, 10s timeout, `IST_OFFSET_MINUTES = 330`, `TIME_ALIGN_TOLERANCE_MS = 60_000`) + `persistTimeCorrection`/`deleteTimeCorrection`/`restoreTimeCorrection`/`persistTimeProbe`/`restoreTimeProbe` (`_backup_meta` keys `time_correction`/`time_probe_db`; types `:1753-1789`, interface `:247-257`, fallback `:5978-5983`). **(2) NEW `lib/services/timeCorrection.ts`** — correction engine (`offsetMinutes = trueNow − serverNow`; `applyOffset` identity@0; `getCorrectedNow()`/`getCronFrom()` lazy read-through no cache; `getTimeDiagnostics()`); **REAL BUG FIXED**: `parseIstDateTimeLocal` round-trip guard → `toIstIso(parsed).slice(0,16) === input` (epoch = `Date.UTC(y,m-1,d,h,min) − offset`). **(3) db-health API/UI** — POST `probe_time` (route.ts:395-425, 30s throttle) + `set_time_correction`/`clear_time_correction`; GET zero-Prisma `time` block; Time Synchronisation card (`page.tsx:247/505-515/1540`); `lib/audit.ts` +2. **(4) Wiring** — `getCronFrom()`/`getCorrectedNow()` in `recommendationCronService.ts` + `worker-engine.ts` (nextRun + due-claim).
- **Files Created**: `lib/services/timeCorrection.ts`, `lib/__tests__/timeCorrection.test.ts`, `.agents/specs/10-admin-time-correction.md`, `.agents/plans/10-admin-time-correction.md`, `.agents/changelog/versions-v3.32.md`, `.agents/sessions/2026-09-10-time-correction/` (decisions + flow).
- **Verification**: targeted 4 suites **118/118** (timeCorrection 20/20, sqlite 71/71, recommendationCronService, worker-engine); full **1106 pass / 4 skip / 1 fail** (1 = documented pre-existing `intelligence.test.ts` flake); tsc **46 = exact baseline (0 new)**; no migration; no new packages; diff 7 modified files +565/−10.
- **Deferred**: live `probe_time` DB verification (Postgres/Docker not running locally); durable TZ fix = correct `TZ`/`UTC` env on Netlify.
- **Commit**: MERGED via PR #117 `38a27bf` (`e74ae54` feat · `df7959d` docs · `b75deb0` docs update); post-merge hotfix in v3.32.1 (working tree = route fix + regression test + docs).

### 2026-09-09 | v3.31.0 — SQLite-first NSE read architecture + low-frequency Prisma sync (Plan 09, on `fix/v3.29.1-header-watchlist`, on top of committed v3.30.0; code committed `9303bd7`→`653b617`, 9 commits)
- **Action**: Executed the user directive "Plan limit is now MONTHLY 200K ops/mo (resetting 2nd)" + "Prisma calls ALLOWED ONLY at 3 moments — boot hydration, 6h SQLite→Prisma push, ONE hourly ops-usage write — zero between". **(1) sync_history ledger** — durable `sync_history` table + `recordSyncHistory()` (prune-100) + `recentSyncs` in health; also fixed the SCHEMA_SQL stray-`;`-in-`--`-comment sql.js parse error (root cause of v3.30.0 boot noise → comments now `/* */`). **(2) Boot hydration on every instance** — `syncFromPrisma(opts)` `reason`/`skipReconcile`/`leaderBypass`/`force`; `initSqliteBackup()` → `{reason:"boot", skipReconcile:true, leaderBypass:true}`. **(3) NSE rate guard** — NEW `lib/services/nseRateGuard.ts` + 6 tests; wired `nse-client.ts` + `market-cache.ts`. **(4) `_sync_outbox` + 6h PUSH engine** — `_sync_outbox` table + `pushSqliteToPrisma` (grouped latest-op-wins, chunk-200 sinks, `reconcileControlToPrisma`); NEW `lib/sqlitePushSinks.ts`; 6h probe pivots PULL→PUSH. **Prod bug**: `if (!isLeader("sqlite-sync"))` never fired (isLeader returns Promise) → awaited. **(5) NSE captures SQLite+outbox only**, auto-promote off (`NSE_PROMOTE_ENABLED=1`). **(6) Jobs write SQLite-first (recs/swing/perf)** — 5 mirror tables + 7 write-through helpers. **(7) Admin long-lived datasets SQLite-first** — +7 helpers + 4 admin CRUD routes flipped. **(8) db-health wiring** — GET outbox/derived-counts/sync-history; POST `push_to_prisma`; UI Outbox + Derived-counts + Push button; 5 hot-route headers.
- **Files Created**: `lib/services/nseRateGuard.ts`, `lib/sqlitePushSinks.ts`, `.agents/changelog/versions-v3.31.md`, `.agents/sessions/2026-09-08-plan09-sqlite-first/` (decisions + flow).
- **Verification**: sqlite.test.ts **68/68** (11 new P8); instrumentation +1; NEW nseRateGuard 6; full **1105 pass / 4 skip / 1 fail** (1 = documented pre-existing `intelligence.test.ts` flake); tsc **46 = exact baseline (0 new)**; no migration. **Deferred (plan rev-v3 c/d)**: `query_cache` + db-health monthly-ops window.
- **Docs commit**: pending user approval (no push/merge/deploy).

### 2026-09-07 | v3.30.0 — Daemon control-plane cadence + SQLite mirror touch-freshness + `upsertCronJob` Date-binding fix + Netlify WASM staging (on `fix/v3.29.1-header-watchlist`, on top of committed v3.29.2 `1e907f1`; committed `8af65cc` Phases 1–4, Date-bind fix + docs in the v3.30.0 doc commit)
- **Action**: Executed the user directive "i don't want to see this every time — defaults in `.env` on Netlify" (recurring db-health noise) — root causes traced and fixed. **(1) Cadence** (`leader.ts` `LEADER_STALENESS_MS` 5min→**15min** + `LEADER_HEARTBEAT_MS` 60s→**300s**; `cron-daemon.ts` NEW `SWING_DRAIN_INTERVAL_MS = 900s` — swing drain off the 60s resync tick; `worker-engine.ts` `REAP_INTERVAL_MS` 60s→**300s**). **(2) Mirror freshness** — NEW `SqliteFallback.touchControlMirror(table)` re-marks `control_write_at:<table>` = NOW; `discoverPendingTask()` touches on the ready-DB path so an idle system's mirror freshness tracks poll cadence, not task activity (no more silent Prisma fallback). **(3) Schema-init root cause (the big one)** — SCHEMA_SQL stray `;` inside `--` comments → sql.js `near "Prisma": syntax error` every boot → SQLite never `ready` → ALL SQLite-first reads silently fell back to Prisma (the recurring db-health noise). **(4) Netlify WASM staging** — NEW `scripts/copy-sql-wasm-netlify.mjs` → `public/sql-wasm.wasm` copied to `.next/sql-wasm.wasm` post-build (Netlify publish dir = `.next`; quickbuild/build wired; non-fatal). **(5) `upsertCronJob` Date-binding fix** (`lib/sqlite.ts`) — Prisma `CronJob` rows carry real `Date` instances; raw bind → locale string breaks the ISO read-back `new Date(String(col))` in `reconcileControlToPrisma` (12h reconcile could write corrupt `nextRun`); NEW `toIso(v)` applied to lastRun/nextRun/createdAt binds (pattern :2524-2525). Same latent pattern in `upsertWorkerTask`/`upsertWorkerStatus` noted, left untouched.
- **Files Created**: `scripts/copy-sql-wasm-netlify.mjs`, `.agents/changelog/versions-v3.30.md`, `.agents/sessions/2026-09-08-v3.30.0-daemon-cadence/` (decisions + flow).
- **Files Modified**: `lib/services/leader.ts`, `lib/services/worker/cron-daemon.ts`, `lib/services/worker/worker-engine.ts`, `lib/sqlite.ts`, `lib/__tests__/sqlite.test.ts`, `lib/__tests__/daemon-sqlite-first.test.ts`, `lib/__tests__/leader.test.ts`, `package.json`, docs (AGENTS.md v3.30.0 row, CHANGELOG index + versions-v3.30.md, TODO.md row, Primer.md, agent-memory.md (this), Lessons #109, session-todos, latest.md handoff, HANDOFF.md).
- **Tests**: targeted **96/96** (sqlite **40** incl. NEW Date-binding describe 2 — ISO not locale fails pre-fix, re-upsert same id replaces — + mock INSERT regex/OR REPLACE semantics; daemon-sqlite-first +2 touch; leader constants); tsc **46 = exact baseline (0 new)**; no schema change → no migration.
- **Status**: Code + tests + verification + docs complete; Phase 1–4 committed `8af65cc`, Date-bind fix + docs committed in the v3.30.0 doc commit (no push/merge/deploy without explicit approval). **Plan 09 (SQLite-first read architecture) user-approved — Phase 0 (WASM build verification) starting.**

### 2026-09-07 | v3.29.2 — Admin AI model management: true built-ins permanently locked (`openrouter/free` + `openrouter/auto` can NEVER be removed; on `fix/v3.29.1-header-watchlist`, on top of committed v3.29.1 `6e8db23`)
- **Action**: Implemented the user directive "a built-in model cannot be removed". **(1) API** — `app/api/admin/ai/config/route.ts` DELETE now zod-validates `modelId` (NOT `id` — `{id}` → 400 "modelId is required"); builtin → **400 "Cannot remove built-in models"** with nothing persisted; unknown → 404; catalog → hidden from GET; custom → removed outright; removing the ACTIVE model resets `ai_config.model` → `DEFAULT_MODEL` + `resetLLM()`. `app/api/admin/ai/custom-models/route.ts`: add/remove, and adding a hidden catalog id restores via the **catalog path** (CATALOG badge, no custom-collection dup). **(2) Constants** (`lib/services/ai/config.ts`): NEW `BUILTIN_MODELS`/`BUILTIN_MODEL_IDS`; `AVAILABLE_MODELS` (8) contains no built-ins; sync-guard test ties `AI_FALLBACK_MODELS` ⊆ `BUILTIN_MODEL_IDS`. **(3) UI** (`app/admin/ai/page.tsx`): "Model Library" heading + locked-note subtitle; active-model select → 3 optgroups (Built-in / Available / Custom); per-model Remove list = catalog ∪ custom **minus builtins**.
- **Files Created**: `lib/__tests__/aiModelCatalog.test.ts` (4), `lib/__tests__/adminAiConfigModelManagement.test.ts` (10), `.agents/sessions/2026-09-07-admin-ai-model-management/` (decisions + flow).
- **Files Modified**: `app/api/admin/ai/config/route.ts`, `app/api/admin/ai/custom-models/route.ts`, `lib/services/ai/config.ts`, `lib/services/ai/modelChain.ts`, `app/admin/ai/page.tsx`, docs (AGENTS.md v3.29.2 row, CHANGELOG index + versions-v3.29.md v3.29.2 section, TODO.md row, Primer.md, agent-memory.md (this), Lessons #108, session-todos).
- **Tests**: NEW **14** (aiModelCatalog 4 + adminAiConfigModelManagement 10 — incl. builtin-400 regression that fails pre-fix); guard-run `modelChain.test.ts` (5 pre-existing) → **19/19**; tsc **46 = exact baseline (0 new)**; no schema change → no migration.
- **Live verification (Playwright :3000, admin)**: builtin DELETE → 400; UI remove/re-add gpt-oss toasts + CATALOG badge persists across reload; combobox = **all 11 options** across 3 optgroups (an earlier "7 options" read was an a11y-snapshot truncation); 0 console errors; pre/post DB state identical (no pollution).
- **Status**: Code + tests + live verification + docs complete; **committed `1e907f1`** on `fix/v3.29.1-header-watchlist` (no push/merge without explicit approval; the unrelated v3.28.x-era changes were committed separately as the v3.30.0 code commit `8af65cc`).

### 2026-09-06 | v3.29.1 — Header overflow fix + Watchlist logged-out infinite-skeleton fix (browser/DevTools visual-verification batch, on `main` on top of merged v3.29.0 `d7e54cf`)
- **Action**: Executed the user-requested browser + Chrome DevTools visual test of `/watchlist` (logged-out + logged-in) — the v3.29.0 carry-over item. **Found + fixed a real bug during the observation**: `app/watchlist/page.tsx` :303 render guard `if (status === "loading" || loading)` dead-coded the `unauthenticated` "Please sign in to view your watchlist." card — the local `loading` (`useState(true)`) is ONLY cleared inside `fetchWatchlists()` (runs only when `authenticated`) → logged-out visitors saw an **infinite skeleton**; the e2e suite structurally cannot catch it (the watchlist spec logs in first). Fix (1 line): `status === "loading" || (status === "authenticated" && loading)`. Also carried + verified the **header overflow fix** (`app/Header.tsx`, CSS-only) — 372 DOM overflow checks + 9-width quick-check loop, 0 overflow @1440/@375 on watchlist/alerts/screener/advanced-screener; full e2e **87 passed / 2 flaky / 0 failed**.
- **Files Created**: `app/watchlist/__tests__/page.test.tsx` (3 tests), `.agents/sessions/2026-09-06-v3.29.1-header-watchlist/` (decisions + flow).
- **Files Modified**: `app/Header.tsx` (CSS-only overflow fix), `app/watchlist/page.tsx` (:303), docs (AGENTS.md v3.29.1 row, CHANGELOG index + versions-v3.29.md v3.29.1 section, TODO.md row, Primer.md, agent-memory.md (this), Lessons #107, session-todos, latest.md handoff, HANDOFF.md).
- **Tests**: NEW watchlist page test **3/3** (unauthenticated → sign-in prompt + no skeleton [regression — fails pre-fix]; loading → skeleton; authenticated-empty → CTA; mocks `useSession`/`fetch`/`AiActionButton`/`Autocomplete`/`useLivePrices`). tsc **46 = exact baseline (0 new)**; no schema change → no migration; full jest **1043 pass / 4 skip / 1 fail** (1 = documented pre-existing `intelligence.test.ts` flake).
- **Live verification (Chrome DevTools, :3000)**: logged-out isolated context → "/watchlist — sign in" card, **0 skeletons**; logged-in demo → "Demo AI Watchlist" (RELIANCE ● LIVE ₹1,310.90 / -23.90 (-1.79%), OHLC table, Analyze/+ Add/Delete), 0 console errors (Web Vitals GOOD), no overflow @375×812 / @2696.
- **Status**: Code + tests + live verification + docs complete; **commit pending user** (no push/merge without explicit approval).

### 2026-09-05 | v3.29.0 — UI/UX audit fixes: backtest symbol-gate softening + AI-failure error surfacing + mobile-nav Alerts (on top of v3.28.5 `6700076`, branch `fix/v3.28.1-sqlite-self-heal`)
- **Action**: Executed plan 07 UI/UX audit fixes (three phases). **(1) Backtest gate softening** — removed v3.28.5's `isBacktestSymbolAllowed` (dead code after softening; `lib/services/symbolReference.ts` now exports only `mergeSymbolSuggestions`); `app/api/backtest/run/route.ts` always falls through to `getBacktestData(symbolUpper)`, derives `symbolSource = "known"|"unlisted"` (:83) from `prisma.symbol.findUnique` for labeling only (echoed :178), and the only no-data failure is the pre-existing `barCount < 50 → 400` guard — a static-table miss is NOT proof of a bad ticker (fresh listings/BE/BZ unsynced rows). `symbolReference.test.ts` **11 → 7** (4 gate tests removed). **(2) AI-failure surfacing** — NEW `lib/aiErrorMessage.ts` `extractErrorMessage(err, fallback)` (unwraps `Error.message`, nested `{error:{message}}`); `AiActionButton` gains `error?: string | null` → small red status line (:114-116, hidden while loading); watchlist throw-site (:254) normalizes `err.error || HTTP <status>` and passes `error={aiError}` — fixes BOTH the swallowed `/api/ai/query` 500s and the `[object Object]` render (bonus regression found during Playwright verification). **(3) Mobile nav** — logged-in quick-access `grid-cols-2` (:312) now includes F&O Analytics (:334) + Alerts (:337).
- **Files Created**: `lib/aiErrorMessage.ts`, `lib/__tests__/backtestSymbolFallthrough.test.ts` (4), `lib/__tests__/watchlistAiError.test.ts` (8), `app/components/__tests__/AiActionButton.test.tsx` (4; path deviation from convention, uses plain `test(`), `.agents/changelog/versions-v3.29.md`, `.agents/sessions/2026-09-05-fix-v3.28.1-sqlite-self-heal/`.
- **Files Modified**: `app/api/backtest/run/route.ts`, `app/components/AiActionButton.tsx`, `app/watchlist/page.tsx`, `app/Header.tsx`, `lib/services/symbolReference.ts` (helper removed), `lib/__tests__/symbolReference.test.ts` (11→7), docs (AGENTS.md v3.29.0 row, CHANGELOG index + versions-v3.29.md, TODO.md row, Primer.md, agent-memory.md, session-todos, HANDOFF).
- **Tests**: targeted **23/23** (backtestSymbolFallthrough 4 + watchlistAiError 8 + AiActionButton 4 + symbolReference 7); full suite **1043 pass / 4 skip / 1 fail** (1 = documented pre-existing `intelligence.test.ts` flake — excluding it 72 suites / 1043 / 4 / 0). tsc **46 = exact baseline (0 new)**; no schema change → no migration.
- **Live verification (Playwright, admin session, :3000)**: `RBLBANK` backtest → 200 `symbolSource:"unlisted"` 70 bars; simulated AI 500 `{error:{message:"AI provider unavailable (simulated 500)"}}` → red line shows extracted text + button stays enabled; mobile 375×812 hamburger grid shows Dashboard/Portfolio/F&O Analytics/Alerts; `[object Object]` regression re-verified fixed; "Test AI" watchlist deleted via UI.
- **Status**: Code + tests + docs verified; **commit pending user** (branch push will carry v3.28.5 `6700076` too; do not push/merge without explicit approval; do not amend `6700076`/`c90f052`).

### 2026-09-05 | v3.28.5 — NSE scrip-list constant (on top of v3.28.4, branch `fix/v3.28.1-sqlite-self-heal`)
- **Action**: User directive — NSE "Securities available for trading" CSV → "create a constant which gets loaded for the Symbol references". Found the machine endpoint after the www.nseindia.com page proved JS-guarded: `https://archives.nseindia.com/content/equities/EQUITY_L.csv` (HTTP 200, 181,507 B; header `SYMBOL,NAME OF COMPANY, SERIES, DATE OF LISTING, PAID UP VALUE, MARKET LOT, ISIN NUMBER, FACE VALUE`; **2,570 clean rows** — 8 fields each, no embedded commas/quotes/dupes; series EQ 2,288 / BE 254 / BZ 28). Wrote NEW `scripts/fetch-nse-scrips.ts` generator (dry-run default, `--write` gate, BOM strip, ≥2000-row guard; `node:https` `get` import — no default export, TS1192) → generated `lib/services/nseScripList.ts` (464 KB: `NseScrip`, `NSE_SCRIPS` sorted, derived `NSE_SYMBOL_SET`/`NSE_SCRIP_BY_SYMBOL`, `isNseSymbol` O(1) case-insensitive, `getNseScrip`, `searchNseSymbols` prefix-priority). VERIFIED RELIANCE row: `{ symbol:"RELIANCE", companyName:"Reliance Industries Limited", series:"EQ", dateOfListing:"29-NOV-1995", paidUpValue:10, marketLot:1, isin:"INE002A01018", faceValue:10 }`.
- **Files Created**: `lib/services/nseScripList.ts` (generated constant), `scripts/fetch-nse-scrips.ts` (generator), `lib/__tests__/nseScripList.test.ts` (11 tests).
- **Files Modified**: `package.json` (`"fetch:scrips": "npx tsx scripts/fetch-nse-scrips.ts"`), docs (AGENTS.md v3.28.5 row + NSE Integration Notes bullet, CHANGELOG index + `.agents/changelog/versions-v3.28.md`, TODO.md row, Primer.md, agent-memory.md, Lessons #106).
- **Tests**: `nseScripList.test.ts` **11/11** (dataset sanity incl. ISIN `^IN[0-9A-Z]{10}$` — two authoring fixes: SME scrips start `IN9` not `INE`; `searchNseSymbols("RELI")` → RELIABLE precedes RELIANCE alphabetically). tsc **46 = exact baseline (0 new)**; no schema change → no migration.
- **Status**: Code + tests verified; **commit pending user** (no push/merge without explicit approval). **Consumer wiring (increment below) DONE**.
- **Increment — v3.28.5 consumer wiring** (same branch/session): NEW `lib/services/symbolReference.ts` (pure `mergeSymbolSuggestions` — constant-first dedupe/cap with trim+upper on both branches, constant's companyName wins; + `isBacktestSymbolAllowed(symbol, hasDbRecord) = hasDbRecord || isNseSymbol(symbol)`). `app/api/symbols/search/route.ts` → constant-first merge (source `merged`/`constant`/`db`, NSE fallback only when merged empty; both consumers read `data.symbols || []` — shape-safe). `app/api/backtest/run/route.ts` → the only `prisma.symbol.findUnique` site (L76) now gates via `isBacktestSymbolAllowed` → listed-but-unsynced symbols fall through to `getBacktestData` instead of 404. `lib/services/nseIpoService.ts` → `IpoIssue.listed?` + private `enrichIpoIssue` (trim+upper → `getNseScrip`) + `IposTab` emerald "Listed" pill. Two authoring fixes: `NseScrip` fixture fields numeric (`paidUpValue`/`marketLot`/`faceValue` — generators parse CSV as numbers), merge DB branch needed `.trim()` for dedupe parity. **Files Created**: `lib/services/symbolReference.ts`, `lib/__tests__/symbolReference.test.ts` (11). **Files Restored + Extended**: `lib/__tests__/nseIpoService.test.ts` — the first authoring pass accidentally OVERWROTE the existing v3.6.4 test file (commit `a3c0aa9`); restored via `git checkout --`, then a NEW `getUpcomingIpoIssues — scrip-list enrichment` describe appended → file total **15** (11 pre-existing + 4 new). **Files Modified**: symbols/search route, backtest route, nseIpoService, IposTab, docs. **Tests**: targeted 37 (11 scrip-list + 15 IPO + 11 symbolReference) + adjacent IPO/backtest 64 green; tsc **46 = exact baseline (0 new)**; no migration.

### 2026-09-05 | v3.28.4 — Read-first recommendations route + edge-cache the heavy latest-run reads (on top of v3.28.3 `a1dd094`, branch `fix/v3.28.1-sqlite-self-heal`; v3.28.3 committed+pushed)
- **Action**: User-approved "Both" fix for two compounding issues found via db-health read-tier telemetry: (1) **key collision** — `app/api/recommendations/route.ts` wrote its serialized `responseBody` under the **service's** `LATEST_KEY` (`"recommendations:latest"`), clobbering the `LatestCacheEntry {runId, newestRunId, data}` → `cached.runId === undefined` → the cross-instance fingerprint check re-ran the heavy stocks-include query (95 rows) on EVERY request (`recommendations.prisma` 14/14 huge-query misses); (2) those heavy reads were unedge-cached despite v3.27.0's `withAccelerateCache`. **Fix**: route now uses its own `ROUTE_CACHE_KEY = "recommendations:api:latest"` + `ROUTE_CACHE_TTL_SECONDS = 60` + typed `RouteRecommendationsCacheBody`, with a read-first memory fast path after the breaker block (`recordRead("recommendations.memory", {hit:true})`, `servedFrom: "memory_cache"`, zero Prisma); all three legacy `"recommendations:latest"` refs (breaker fallback, `set`, DB-error fallback) switched to the route key so the service key is never touched by the route; `getLatestRecommendations`' two heavy `findFirst` reads (latestRun stocks-include + newestRun lightweight select) wrapped in `withAccelerateCache({ttl:60, swr:30})`; **fingerprint probes stay uncached** (cross-instance staleness guard); the `findFirst`+`include` payload falls back to the bare model through the wrapper → re-used the existing `as RunWithStocks | null` cast at the `serializedStocks` usage (line-1308 pattern). `invalidateRecommendationsCache()` = `flushAll()` clears both keys.
- **Files Modified**: `app/api/recommendations/route.ts` (route cache key/body/TTL + read-first fast path), `lib/services/dailyRecommendationService.ts` (import 34, `withAccelerateCache` wraps on the two heavy reads, cast fix), `lib/__tests__/dailyRecommendationService.test.ts` (+`withAccelerateCache` pure stub + regression), docs (AGENTS.md v3.28.4 row, CHANGELOG index + `.agents/changelog/versions-v3.28.md` (also removed orphaned duplicate `# v3.28.2` header at EOF), TODO.md row, Primer.md, agent-memory.md, session-todos, HANDOFF).
- **Tests**: `dailyRecommendationService.test.ts` **34/34**; targeted readTier + recPerf **25/25**; full suite **1004 pass / 4 skip / 2 fail** (2 = documented pre-existing `intelligence.test.ts` async cache-flake — excluding it 72 suites / 1004 / 4 / 0). tsc **46 = exact baseline (0 new)**; no schema change → no migration.
- **Status**: Code + tests + docs verified; **commit pending user** (on `fix/v3.28.1-sqlite-self-heal`, on top of `a1dd094`; do not push/merge without explicit approval; do not amend `a1dd094`/`5a63fc4`/`718b5d2`). OpenRouter quota (Issue 3) remains report-only; BUGS.md #14 Prisma Postgres Phase 0 pending before Dec 1 2026.

### 2026-09-04 | v3.28.2 — Lost-leader engine stop: single-active-worker enforcement (on top of v3.28.1, branch `fix/v3.28.1-sqlite-self-heal`)
- **Action**: Post-ship audit (user request — "tasks/worker failing, only 1 worker/task active at a time"). Verified every persistence path: (1) AI-call tracking persists — `trackAiCall` → memory ring + `enqueueWriteBehind("server_log")` to SQLite (zero Prisma per call) + two-tier `getPersistedAiCalls` merge (v3.24.0); (2) daily Recommendations persist — run create → `tracker.createMany` → `stock.createMany` → run.update, no-fake-HOLD `deleteMany` intact (v3.11.1); (3) performance tracking persists — status updates + `RecommendationStatusHistory` creates + 360-day archive sweep; (4) IPO details persist — `ipoAnalysisService` generate via OpenRouter → DB `market_cache` + memory, cleanup + stale prune exist; (5) Swing trackers persist — `persistSwingTrackers` + `patchSwingSignalAnalysis` on `analysisStatus==="done"` (non-fatal); (6) crons synced during normal sync — `syncFromPrisma` pulls `cron_job` rows + `reconcileControlToPrisma` at the 6h sync pushes `nextRun`/`lastRun`/counters + `worker_status` heartbeats + completed/failed `worker_task` statuses. ✅ all verified.
- **Defect found and fixed**: `instrumentation.ts` `onLost` callbacks only `logger.warn` — combined with `acquireLeaderLock` **fail-open on DB-unavailable** (every instance starts worker poll loop + cron daemon during a DB blip), the losers' `renewLeaderLock` returned false on DB recovery → `onLost` fired → they **kept polling forever** → N active workers/tasks concurrently. **Fix (surgical, `instrumentation.ts` only)**: worker `onLost` → `stopWorkerEngine()` (poll timeout + heartbeat + scheduler intervals cleared); cron `onLost` → `stopCronDaemon()` (node-cron tasks destroyed + intervals cleared); sqlite-sync `onLost` stays log-only (sync is gated per-run by `isLeader` — nothing long-running to stop).
- **Files Modified**: `instrumentation.ts` (+2 `onLost` lines, destructured `stopCronDaemon`/`stopWorkerEngine` from dynamic imports), docs (AGENTS.md v3.28.2 row, CHANGELOG index + `.agents/changelog/versions-v3.28.md`, Primer.md, agent-memory.md, Lessons #105, session-todos, HANDOFF).
- **Files Created**: `lib/__tests__/instrumentation.test.ts` (5 — starts when leader for all 3 roles; worker onLost → `stopWorkerEngine` fired; cron onLost → `stopCronDaemon` fired; not-leader → no start; non-node runtime → early return).
- **Tests**: `instrumentation.test.ts` 5/5; targeted 85/85 (sqlite 36 + daemon-sqlite-first + dbOpTiering + historical + leader); full suite **1003 pass / 4 skip / 1 fail** (1003 = 998 + 5; 1 = documented pre-existing `intelligence.test.ts` flake — excluding it 72 suites / 1003 / 4 / 0). tsc **46 = exact baseline (0 new)**; no schema change → no migration.
- **Status**: Code + tests + docs verified; **commit pending user** (on `fix/v3.28.1-sqlite-self-heal`, on top of `718b5d2`; do not push/merge without explicit approval).

### 2026-09-04 | v3.28.1 — SQLite partial-init self-heal + promote not-ready guard (prod triage after v3.28.0 deploy)
- **Action**: Fixed two live-prod symptoms that appeared after the v3.28.0 deploy to `main`: (1) dashboard "SQLite Not Ready", (2) `promoteNseToPrisma … no such table: daily_price` (and `chartink_screener_result`). **Root cause (1 defect → both symptoms)**: `initSqliteBackup` (`lib/sqlite.ts` :970) assigns `state.db = db` (:976) BEFORE the schema loop (:979-982). If any schema statement throws, the catch (:1016+) leaves `state.db` non-null (partially built — `daily_price`/`chartink_screener_result` missing) + `ready:false`, and the `if (state.db) return` early-return (:971) made the `ensureSqliteBackup()` retry a **permanent no-op** → dashboard "SQLite Not Ready" (from `ready`) AND `promoteNseToPrisma` "no such table" (it only guarded non-null `state.db`, not `ready`). `ensureNseColumns` is ALTER-only (can't create missing tables). **Fix (1) self-healing** — init catch now resets `state.db = null` + `_instance = null` so the next `ensureSqliteBackup()` REBUILDS from scratch (verified: schema-throw via patched `MockDatabase.run` → fallback null after catch → next init ready). **Fix (2)** — `promoteNseToPrisma()`/`promoteTable()` now require `!state.ready ||` so a partial mirror is SKIPPED (all-zero summary, no Prisma ops, no throw).
- **Files Modified**: `lib/sqlite.ts` (+16/-2 — init catch state reset + promote ready-guards), `lib/__tests__/sqlite.test.ts` (+57 — 2 new v3.28.1 regression tests in the `ensureSqliteBackup (lazy on-demand init)` describe), docs (AGENTS.md v3.28.1 row, CHANGELOG index, `.agents/changelog/versions-v3.28.md`, Primer.md, agent-memory.md).
- **Tests**: Full suite **998 pass / 4 skip / 1 fail** (998 = 995 baseline + 3; the 1 fail = documented pre-existing `intelligence.test.ts` async cache-flake, times out in isolation, `intelligence.ts`/`cache.ts` untouched — excluding it: 71 suites / **998 pass / 4 skip / 0 fail** from these changes). Targeted: `sqlite.test.ts` 36/36 (log line `SQLite backup init failed, error=simulated schema-loop failure` confirms the partial-init regression path executed) + daemon-sqlite-first/dbOpTiering/historical (31) green. tsc **46 = exact baseline (0 new)**; no schema change → no migration.
- **Status**: Code + tests + docs verified; **commit pending user** (on `v3.26.0-prod-failure-triage`; do not commit/push/merge without explicit approval; do not amend `8020dee`/`a6d902e`/`24e3586`/`3605c64`). **Deferred**: daily recommendation job failures (Issue 3) — investigate after this ships.

### 2026-09-04 | v3.28.0 — SQLite-first NSE data store: mirror + read-first + write-through + instant promote
- **Action**: Per user directives — "all NSE sync triggers against SQLite; all things cached, reads hit SQLite FIRST → live fetch, NOT Prisma fallback for NSE reads; add SQLite schema similar to Prisma + keep updated + indexes" — built the SQLite-first NSE data store so SQLite is the primary live store for NSE-backed market data (stock list, corporate actions, daily OHLCV/backtest prices, Chartink captures) with a hot memory cache + an instant SQLite→Prisma promote engine. **(`lib/sqlite.ts` SCHEMA)** — NEW `symbols`, `daily_price`, `chartink_screener_result` tables + indexes (daily_price OHLCV mirror keyed `NSE:${SYMBOL}` to match Prisma `daily_prices.ticker`); extended `corporate_action` (+8 cols) + `chartink_screener` (+5 cols); idempotent `ensureNseColumns(db)` ALTER-guard; `SqliteFallback` interface +7 methods + type-safe `createFallback` impls via local `sv()` converter. **(Write mirrors)** — `executeStockSync`→`cacheSymbol`+`flushNseToPrisma()`; `executeCorpActionsSync`→`cacheCorporateActions` (`(actions as any[])` cast + `parseActionPurpose`) + flush; `executeHistoricalPriceSync` (dry-run-false)→`cacheDailyPriceBars(\`NSE:${symbol}\`, …)` mapping normalized `SecurityWiseHistoricalBar` fields + flush; `runChartinkUnifiedScreeners`→`cacheChartinkResults(templateId, rows)` per run — all lazy `@/lib/sqlite`, non-fatal. **(Read-first)** — `getBacktestData` NEW SQLite-first step (`getSqlitePriceRange(\`NSE:${sym}\`)`, `source:"sqlite"` when ≥20 bars, memory-cached, falls through to DB/NSE); `BacktestDataResult.source` union +`"sqlite"`; NSE-fetch step also mirrors via `cacheDailyPriceBars`. **(Promote engine)** — `promoteNseToPrisma()` (leader-gated, reads SQLite, chunked `createMany`), `flushNseToPrisma()`, `startNsePromoteFlush()` (60s unref'd) booted from `instrumentation.ts`, `stopNsePromoteFlush()`. **(Dev-gated Prisma query logger)** — `PRISMA_QUERY_LOG=1` dev-only, constructor `log:["query"]` ($extends wrapper doesn't propagate `$on('query')`). **(Tests)** — `historicalPriceSyncService.test.ts` mock `@/lib/sqlite` fixed (`cacheDailyPriceBars`+`flushNseToPrisma`); backtest/chartink/worker suites unaffected.
- **Files Created**: `.agents/changelog/versions-v3.28.md`, `.agents/specs/06-sqlite-first-nse-store.md`, `.agents/plans/06-sqlite-first-nse-store.md`. Temp scripts `scripts/_tmp-obs.ts` + `scripts/_tmp-qlog-test.ts` deleted (not referenced).
- **Files Modified**: `lib/sqlite.ts` (+752, SCHEMA_SQL + NSE helpers + promote engine + `parseJsonLoose`), `lib/services/worker/worker-service.ts` (+49, `executeStockSync`/`executeCorpActionsSync` wired), `lib/services/historicalPriceSyncService.ts` (+31, mirror + flush), `lib/services/backtestDataService.ts` (+51, SQLite-first read + mirror write), `lib/services/chartinkUnifiedScreenerService.ts` (+20, per-run cache), `lib/prisma.ts` (+14 dev query-logger), `instrumentation.ts` (+`startNsePromoteFlush`), `lib/__tests__/historicalPriceSyncService.test.ts` (+10), docs (AGENTS.md v3.28.0 row, CHANGELOG index, Primer.md, agent-memory.md). Regression fix `lib/prisma.ts` committed `8020dee`.
- **Tests**: Full suite **995 pass / 4 skip / 2 fail** — the 2 fails are the documented pre-existing `intelligence.test.ts` async cache-flake (fails run-to-run regardless; excluding it: 71 suites / **995 pass / 4 skip / 0 fail** from these changes). Targeted: `backtestDataService.test.ts` 16/16, chartink suites 38/38, `historicalPriceSyncService.test.ts` 15/15. tsc **46 = exact baseline** (0 new, 3 runs); no schema change → no migration.
- **Lesson 105**: The `$extends` query wrapper on the Prisma client does NOT propagate `$on('query')` listeners — the dev-gated per-query SQL logger must be wired through the **constructor's `log:["query"]`** option instead. Also: SQLite mirror helpers that kick async `ensureSqliteBackup()` on cold-start/test (WASM not loaded) fail harmlessly — wrap with `.catch(() => {})`; and when mirroring Prisma-backed NSE data into SQLite, always key OHLCV by `NSE:${SYMBOL}` (matches `daily_prices.ticker`) so the promote engine round-trips ticker verbatim.
- **Status**: Code + tests + docs verified; **diff pending user commit + push** (on `v3.26.0-prod-failure-triage`; no auto-commit/push/deploy).

### 2026-09-04 | v3.27.0 — Prisma Postgres Migration Phase 1-3: `withAccelerate()` wiring + `cacheStrategy` at 5 direct-Prisma read sites
- **Action**: Started the mandatory migration off standalone Prisma Accelerate (`prisma+postgres://accelerate.prisma-data.net`, **retires Dec 1 2026**) to Prisma Postgres (Phase 0 = manual Console provisioning at deploy-time, no code). Phase 1-2 ship the intermediate win: activate the already-installed `@prisma/extension-accelerate` `withAccelerate()` on the runtime client + explicit **`cacheStrategy`** on the 5 highest-frequency direct-Prisma reads. **(1) Phase 1 (`lib/prisma.ts`)** — `new PrismaClient({ accelerateUrl }).$extends(withAccelerate())` in the accelerate branch (extension order documented: `withAccelerate()` FIRST so the `$allOperations` circuit-breaker/op-count/timeout extension wraps the cached reads); `let prismaClient: any` + explicit `$allOperations` param types preserved the 46-error tsc baseline; `type AccelerateClient = PrismaClient` (reverted from a manual intersection — the base `PrismaClient` model read-args hard-code `cacheStrategy: never`, and intersections don't override `never`). NEW **`withAccelerateCache(strategy)(args)`** boundary helper — preserves Prisma contextual typing via `Parameters<T>[0]` (so `orderBy: 'asc'|'desc'` literals still resolve) while injecting `cacheStrategy` at the query boundary (runtime client supports it; base type doesn't). NEW `ACCELERATE_CACHE_TTL = Number(process.env.PRISMA_ACCELERATE_CACHE_TTL) || 300` export. **(2) Phase 2 — 5 sites**: `corporateAction.findMany` `{ttl:300,swr:60}` (corp-actions/combined route); `chartinkScreenerResult.findMany` `{ttl:900,swr:300}` (`chartinkScreenerService`, the actual direct Prisma read); `recommendationTracker.findMany` ×2 `{ttl:600,swr:60}` (`recommendationPerformanceService`); `dailyPrice.findFirst` ×2 `{ttl:60,swr:30}` (`lib/stock-service.ts`; `aggregate` left unwrapped); `marketCache.findUnique` `{ttl:300,swr:60}`. **(3) Test coupling fixes** — `recommendationPerformanceService.test` + `chartinkScreenerService.test` mock `@/lib/prisma` with `{__esModule, default}` only, so the new named export was `undefined` → added the pure `withAccelerateCache` stub to both factories; chartink exact-args assertion → `expect.objectContaining`.
- **Files Created**: `.agents/changelog/versions-v3.27.md`, `.agents/specs/05-prisma-postgres-migration.md`, `.agents/plans/05-prisma-postgres-migration.md` (spec + plan committed `db5a5cc`).
- **Files Modified**: `lib/prisma.ts` (`withAccelerate()` + `AccelerateClient` + `withAccelerateCache` + `ACCELERATE_CACHE_TTL`), `app/api/corporate-actions/combined/route.ts`, `lib/services/chartinkScreenerService.ts`, `lib/services/recommendationPerformanceService.ts`, `lib/stock-service.ts`, `lib/market-cache.ts`, `lib/__tests__/recommendationPerformanceService.test.ts`, `lib/__tests__/chartinkScreenerService.test.ts`, docs (AGENTS.md v3.27.0 row, CHANGELOG index + versions-v3.27.md, TODO.md row, Primer.md, agent-memory.md, Lessons.md).
- **Tests**: Full suite **995 pass / 4 skip / 2 fail** — the 2 fails are the documented pre-existing `intelligence.test.ts` async cache-flake (`INTELLIGENCE_CACHE_HIT` vs `INTELLIGENCE_GENERATED` race), confirmed fails identically in isolation; excluding it: 71 suites / **995 pass / 4 skip / 0 fail** from these changes. tsc **46 = exact baseline** (0 new); `prisma validate` valid + `prisma generate` ok; no schema change → no migration.
- **Lesson 104**: Prisma Accelerate/Postgres query-cache `cacheStrategy` is **not representable on the base `PrismaClient` read-arg types** (`cacheStrategy: never`, and intersections don't override `never`) while the `$extends(withAccelerate())` runtime client accepts it — so injecting it requires a pure **boundary transform** `(withAccelerateCache(strategy)(args))` typed via `Parameters<T>[0]`/`ReturnType<T>` that preserves the caller's contextual argument typing and spreads the strategy through, rather than a client-type intersection/restructure. Also: any new named export from `lib/prisma.ts` **breaks test factories that mock `@/lib/prisma` with `{__esModule, default}` only** (the named export resolves `undefined`) — add a pure stub for it, and loosen exact-args assertions that would now receive the injected `cacheStrategy` to `expect.objectContaining({...})`.
- **Status**: Code + tests + docs verified; **diff pending user commit** (8 code files + doc files, on `v3.26.0-prod-failure-triage`; no auto-commit/push/deploy).

### 2026-09-03 | v3.26.0 — Prod-failure triage: P2002 false-errors + Plan-limit breaker false-trip + "a is not iterable" Market Sync + 12h→6h sync cadence
- **Action**: Triaged three production failure signatures surfaced from the **DB Errors panel**, **deploy logs**, and **admin Tasks panel** (on `main`, on top of v3.25.0). **(1) P2002 false-error reporting** (`lib/prisma.ts`) — the DB Errors panel showed 3× `WorkerStatus create` P2002; root cause = benign leader-election "create-or-stand-by" races in `lib/services/leader.ts` (10+ cold-start instances contend for `leader-<role>`; every loser throws P2002, handled gracefully) were recorded as DB faults → NEW `isBenignUniqueConflict(err)` (code `P2002`); the `$allOperations` `.catch` now SKIPS `recordDbError` for them (error still propagates unchanged — only the diagnostic recording is skipped). **(2) Plan-limit breaker false-trip (critical)** (`lib/prisma.ts`) — the breaker opened on ANY `isDbUnavailableError`, so a transient Accelerate-proxy `fetch failed`/connection blip on a HEALTHY DB tripped the 5-min global freeze (logs: 17:00:34 `fetch failed` → 17:04:36 "Plan limit circuit breaker open" → 17:28/17:29 repeat → "Swing analysis processor crashed" + "Cron daemon resync deferred" with ZERO Prisma access for the cooldown; the "half-open probe" is dead code while open). FIX: the breaker now trips ONLY on `isPlanLimitHoldError` (P6003/"hold on your account"/"planLimitReached"/query timeout); transient comms errors keep per-query graceful degradation (worker backoff + cached/empty fallbacks) without freezing the global breaker. Removed now-unused `isDbUnavailableError` import. **(3) "a is not iterable" Daily Market Sync** (`lib/services/worker/worker-service.ts`) — `executeMarketDataSync` (`market_data` behind `Daily Market Sync (System)` + `historical_sync` + `market_data_fetch`) did `for (const stock of getIndexStocks(indexName))` with NO null guard; `getIndexStocks` (`lib/index-service.ts`) returns `null` on NSE error (lines 783/809) → `TypeError: null is not iterable` (minified "a is not iterable") — matches prod failures 2/9 + 3/9 at 01:01. Added `if (!Array.isArray(stocks) || stocks.length === 0) throw new Error("No stocks fetched from NSE (market data sync)")` mirroring the proven `executeStockSync` guard. **(4) 12h→6h sync cadence (user directive)** (`lib/sqlite.ts`) — `PROBE_INTERVAL_MS` 12h→6h; the periodic probe now runs a full `syncFromPrisma()` (→`reconcileControlToPrisma`) on EVERY available tick (was down→up only), so the SQLite→Prisma control-plane reconcile reaches Prisma every 6h even on a healthy DB. Stale "12h" comments updated to 6h in sqlite.ts + swingRecommendationService + corp-actions/combined + recommendations routes (`IPO_ANALYSIS_CACHE_TTL_SECONDS` 12h cache TTL left unchanged).
- **Files Created**: `.agents/changelog/versions-v3.26.md`.
- **Files Modified**: `lib/prisma.ts` (P2002 skip + breaker trip predicate + removed unused import), `lib/services/worker/worker-service.ts` (market-data iterable/empty guard), `lib/sqlite.ts` (PROBE_INTERVAL_MS + every-tick reconcile + 12h→6h comments), `lib/services/swingRecommendationService.ts` + `app/api/corporate-actions/combined/route.ts` + `app/api/recommendations/route.ts` (12h→6h comments), `lib/__tests__/db-utils.test.ts` (breaker-trip regression), docs (AGENTS.md v3.26.0 row, CHANGELOG index, TODO.md row, Primer.md, agent-memory.md, Lessons.md).
- **Tests**: NEW db-utils breaker-trip regression (transient `fetch failed`/ECONNREFUSED/P1001/Accelerate-proxy/Bad Gateway → `isDbUnavailableError=true` but `isPlanLimitHoldError=false`, so the breaker never trips; benign P2002 → both false). Targeted suite: **db-utils 24 + daemon-sqlite-first + dbOpTiering + leader 58 pass**; **sqlite 34 pass**; tsc **46 = exact baseline** (0 new); no schema change → no migration.
- **Lesson 103**: (a) The plan-limit circuit breaker must trip ONLY on a genuine hold signal (`isPlanLimitHoldError`), never on a broad `isDbUnavailableError` — a transient network/connection blip is exactly what per-query graceful degradation (worker backoff + cached/empty fallbacks) is for, and a global 5-min freeze with an unreachable "half-open probe" is far worse than a slow degraded request. (b) NSE list-fetchers like `getIndexStocks` return `null` on error/empty — ALWAYS guard the `for...of` with `Array.isArray` + empty check and throw a readable error (webpack minifies `TypeError: null is not iterable` to the cryptic "a is not iterable"), mirroring the `executeStockSync` pattern. (c) Application-level benign P2002 races (leader-election "create-or-stand-by") must not be recorded as DB health faults — skip `recordDbError` for `code === "P2002"` while still propagating the error.
- **Status**: Code + tests + docs verified; **diff pending user commit** (on `main`, uncommitted; no auto-commit/push/deploy).

### 2026-09-03 | v3.25.0 — SQLite-Primary daemon control plane (high-frequency worker/cron check-reads + task-status writes move off Prisma to the local SQLite mirror; Prisma written ONLY at the 12h `syncFromPrisma` reconcile)
- **Action**: Per user directive — "i only want to write these to the prisma during the 12hr sync job. if sqlite is empty then fetch from the prisma but write to sqlite." — moved the daemon control plane to SQLite-first. **(1) `lib/sqlite.ts`** — SQLite-primary control plane: `ensureControlColumns()` (idempotent `PRAGMA table_info`-guarded `ALTER TABLE ADD COLUMN` for `worker_task.assigned_to/cron_job_id/payload` + `cron_job.config`; SQLite-only, NO Prisma schema/migration); NEW `SqliteFallback` `upsertWorkerTask`/`upsertWorkerStatus`/`upsertCronJob`/`deleteWorkerTask`/`deleteCronJob`/`isControlMirrorFresh(table,maxAgeMs)` (non-empty AND fresh `control_write_at:<table>` in `_backup_meta` → trust mirror, else Prisma+seed); `syncFromPrisma` backfill maps the new columns + NEW SQLite→Prisma **`reconcileControlToPrisma(db)`** at the top of the 12h sync (worker_status upsert, cron_job conditional updateMany, completed/failed worker_task updateMany — the ONLY Prisma writes to these tables). **(2) `worker-engine.ts`** — NEW exported `discoverPendingTask()`: SQLite-first pending-task read (fresh mirror → highest-priority pending; fresh-but-empty trusted → null; else Prisma `findFirst` + `upsertWorkerTask` seed); poll uses it; completion/failure `upsertWorkerTask` self-corrects the mirror. **Reaper liveness reads + stateless-transition heartbeat + atomic `updateMany` CLAIM stay on Prisma** (cross-instance coordination — a per-instance mirror would blind the reaper to other instances, v3.12.0 bug; claim count 0 → skip guards stale discovery). **(3) `cron-daemon.ts`** — `syncCronJobs` SQLite-first (fresh mirror → `getCronJobs()` + `parseConfig()` active-filtered) else Prisma `findMany` + reseed; `fireJob` re-fetch stays Prisma. **(4) `task-orchestrator.ts`** — `seedTaskMirror(task)` (lazy `@/lib/sqlite`, non-fatal) after each task create so a brand-new admin/cron task is poll-visible immediately (Prisma create kept — shared-truth).
- **Files Created**: `lib/__tests__/daemon-sqlite-first.test.ts` (7 tests), `.agents/changelog/versions-v3.25.md`.
- **Files Modified**: `lib/sqlite.ts`, `lib/services/worker/worker-engine.ts`, `lib/services/worker/cron-daemon.ts`, `lib/services/worker/task-orchestrator.ts`, docs (AGENTS.md v3.25.0 row, CHANGELOG index, TODO.md row, Primer.md, agent-memory.md; plan + spec rewritten to the corrected SQLite-primary model with final-decision divergences).
- **Tests**: NEW `daemon-sqlite-first.test.ts` **7/7** (SQLite-first fresh/zero-Prisma, fresh-empty-trusted, stale→Prisma+seed, no-sqlite degrade for both `discoverPendingTask` + `syncCronJobs`, cron fresh/stale+reseed, atomic-claim-count-0). Full suite **994 pass / 4 skip** (the 2 fails are the PRE-EXISTING async `intelligence.test.ts` cache flake, fails 1-3 tests run-to-run with AND without these changes; excluding it — 71 suites / **983 pass / 4 skip / 0 fail**, +7 from the 989 baseline); tsc **46 = exact baseline** (0 new); no schema change → no migration.
- **Lesson (carried)**: The daemon control-plane reads/writes are high-frequency and near-constant — they must hit the local SQLite mirror, not Prisma; but **cross-instance atomic coordination (worker task claim, leader lock+heartbeat, reaper liveness, stateless-transition heartbeat, `fireJob` re-fetch) must stay on Prisma** — SQLite is per-process/in-memory, so a per-instance mirror cannot see or coordinate with other Netlify instances (would blind the reaper → v3.12.0 prod bug, or allow duplicate/parallel task execution, or cause split leadership → PR #113 bug).
- **Status**: Code + tests + docs verified; **commit/push pending user** (no auto-commit/push/deploy). **⚠️ `.env` must be restored to the Accelerate URL (currently local PG for testing; backup `C:\Users\lucky\AppData\Local\Temp\opencode\tradenext.env.accelerate.bak`) before finish.**

### 2026-09-02 | v3.23.0 — SQLite-primary READ tier during plan-limit breaker holds + DB-log download/export UI + readTier telemetry
- **Action**: Completed `feat/db-ops-reduction-read-tier` branch (on top of v3.22.0) — all hot reads serve from SQLite when the plan-limit circuit breaker is OPEN, plus operational log download/export UI and a zero-DB read-tier telemetry registry. **(1) SQLite-first read gating** — `isPlanLimitBreakerOpen()` from `lib/db-utils.ts` (synchronous globalThis check, zero overhead). When breaker OPEN: `app/api/recommendations/route.ts` → `sqlite.getLatestRecommendations()` (`servedFrom: "sqlite_mirror"`, falls to memory `recommendationsCache`); `lib/services/swingRecommendationService.ts` → gates DB job fast-path + persist-job block on `!breakerOpen`, breaker-open fallback returns screener-only "pending" feed with **no Prisma writes** (atomic `updateMany` claim remains sole writer exception per user directive); `app/api/screener/chartink/route.ts` → SQLite-first gate serving `sqlite.getChartinkScreeners()` without calling Prisma; `app/api/corporate-actions/combined/route.ts` → SQLite-first gate after memory-cache fast path (`{ data, source: "sqlite_mirror" }`). **(2) DB-log download/export UI** — GET `/api/admin/db-health` returns `dbLogFiles` via `getDbLogFiles()` (filesystem-only, zero Prisma); new "DB Logs — Download / Export" card with live per-kind buttons (`?export=api_request|server_log|audit_log`), archived-files table (reverse-chron, KB sizes, per-date Download via `?archiveFile=<date>`), pending queue counts + message area. **(3) Worker/task/cron logs downloadable** — NEW `readAllLogs(limit=200)` in `lib/services/worker/worker-logger.ts` (bulk-concatenates all `worker_logs/*.log` newest-first, traversal-guarded, zero DB); monitoring `worker-logs` case gains `?action=download` (`Content-Type: text/plain` + `Content-Disposition: attachment`); Workers tab "Download all" button. **(4) Chartink cache TTL** 5m → 15m in `chartinkScreenerService.ts`. **(5) readTier telemetry** — NEW `lib/services/readTier.ts` (zero-DB globalThis `__readTier` registry, `ReadSource` = sqlite/memory/prisma/nse/filesystem/other, `recordRead`/`getReadMetrics`/`resetReadMetrics`, per-reader min/max/avg/hits/misses/rows, per-source aggregation, bounded long-query ring `MAX_LONG=15`, SQLite perf grid); instrumented across 6 call sites (sqlite mirror helpers, recommendations Prisma, swing breaker-open, screener Prisma, corp-actions memory+Prisma); db-health GET returns `readTier` + `cache.metrics`; new "Cache & Read-Tier Utilisation" card (NodeCache hit-rates, HIGH-frequency sqlite/memory table, LOW-frequency Prisma warning, long/large-queries, SQLite latency grid).
- **Files Created**: `lib/services/readTier.ts` (telemetry registry), `lib/__tests__/readTier.test.ts` (11 tests).
- **Files Modified**: `app/api/recommendations/route.ts`, `lib/services/swingRecommendationService.ts`, `app/api/screener/chartink/route.ts`, `app/api/corporate-actions/combined/route.ts`, `lib/services/worker/worker-logger.ts`, `lib/services/chartinkScreenerService.ts`, `app/api/admin/db-health/route.ts`, `app/admin/utils/db-health/page.tsx`, `app/api/admin/monitoring/route.ts`, docs (AGENTS.md row, CHANGELOG index + versions-v3.23.md, TODO.md row, Primer.md, agent-memory.md, Lessons.md).
- **Tests**: NEW `readTier.test.ts` **11/11** (record/aggregate, min/max/avg, misses, default source+miss, reader separation, long-query capture+sorted, no sub-threshold, ring ≤15, SQLite perf, reset, all source keys). Full suite **986 pass / 4 skip / 0 fail** (was 975/4, +11); tsc **46 = baseline** (0 new); no schema change → no migration.
- **Lesson 100**: NodeCache `getStats()` is per-process and resets on deploy/`flushAll()` — hot reads (recommendations, screener, corp-actions, SQLite mirror) short-circuit before generic NodeCaches so `getStats()` never sees them; real cache-hit telemetry needs a call-site instrumentation layer (`readTier`) that records reads at the point they happen, not inside the cache layer itself. Also: the db-health dashboard must remain zero-Prisma even when surfacing new telemetry — use filesystem-only `getDbLogFiles()` and in-memory `getReadMetrics()`/`getCacheMetrics()`.
- **Status**: Code + tests + docs verified; **commit/PR/deploy pending user** (no auto-commit/push/deploy).

### 2026-09-02 | v3.21.1 — DB Health ops visibility: SQLite ops-counter persistence + Total Operations/Plan Usage UI + sql.js WASM fix
- **Action**: (1) **Live-site bug FIX** — `/admin/utils/db-health` showed "SQLite Not Ready": sql.js is a native/WebAssembly module; the server bundle must exclude it from webpack and the WASM file needs an explicit `locateFile`. Added `'sql.js'` to `next.config.ts` `serverExternalPackages` + `lib/sqlite.ts` `resolveSqlWasm()` (`node_modules/sql.js/dist` → `public/`, default `sql-wasm.wasm`) into `initSqlJs({ locateFile })`. (2) **IO-count reconciliation (user-approved "Display + persist")** — Prisma dashboard Total Operations is authoritative (all reads+writes through the Accelerate proxy) vs in-memory `dbOpsCounter` (resets on every deploy). `lib/prisma.ts` now exports `getIstDayKey` (single IST-day-key source, shared with sqlite); `lib/sqlite.ts` adds `persistOpsCounter()`/`restoreOpsCounter()` — key `ops_counter` in `_backup_meta`, **IST-day guard** (a snapshot from a different day is discarded so the counter resets daily, never replays yesterday) + **`Math.max` merge** (a newer snapshot never reduces the count) — plus 60s `startOpsCounterPersistence()` on globalThis (same singleton pattern as prisma/cache) booted from `instrumentation.ts`; restore runs at init + after initial sync. `/api/admin/db-health` GET returns `totalOperations`/`planLimit` (env `DB_PLAN_LIMIT_OPS`, default 10,000)/`planOperationsRemaining` and persists the counter on every GET; POST sync persists after `syncFromPrisma()`. UI: stat grid 5→6 cards with "Total Ops Today" (threshold colors >90 red / >70 amber) + "Plan Operations Usage" bar (reads vs writes stacked vs plan, remaining count, italic footnote) + "Plan Ops n% Used" amber warning badge at >80%. (3) **Test-infra mock semantics fixes** in `sqlite.test.ts`: mock `exec()` now projects only requested SELECT columns (real sql.js returns just `value`; the mock returned all → `LIMIT 1` picked the stale row) and the INSERT handler implements `INSERT OR REPLACE` (drop same-PK rows — without it a second persist duplicated the key row and broke the roundtrip); added `getIstDayKey` to the prisma mock.
- **Files Modified**: `AGENTS.md` (v3.21.1 row), `.agents/CHANGELOG.md` index + `.agents/changelog/versions-v3.21.md` (v3.21.1 section), `TODO.md` (row), `Primer.md`, `agent-memory.md`, `Lessons.md` (#95), `session-todos.md`.
- **Tests**: `npx jest --testPathPatterns="sqlite.test"` → 20/20; full suite **920 pass / 4 skip** (was 917/4, +3); tsc **46 = baseline** (0 new production errors).
- **Lesson 95**: sql.js (and any WASM/native module) must be added to `serverExternalPackages` in Next.js and given an explicit `locateFile` — otherwise the WASM binary 404s at runtime and the whole fallback layer silently dies; and sql.js `exec()` returns only the requested columns — mocks that return all columns break `LIMIT 1`-driven reads.
- **Status**: Code + tests + docs verified on `main` (7 files, uncommitted); **commit pending user** (no auto-commit/push/deploy). Post-deploy: live-verify `/admin/utils/db-health` shows SQLite Ready + Total Ops restored.
- **Follow-up increment (same session, same v3.21.1)**: committed `4c47348` + docs `47e6677` on branch `feat/db-health-ops-visibility` (pushed; PR NOT created). Then added **per-type DB-error summary + lazy SQLite re-init** (uncommitted): NEW `classifyDbError()` in `lib/db-utils.ts` (`DbErrorType` = plan_limit/timeout/accelerate_proxy/connection/write_budget/other, ordered checks) + per-type `dbErrorCounts` (`__dbErrorCounts` globalThis, lazy IST-day rollover) with `recordDbError()` classification + `getDbErrorCounts()`; `persistDbErrorCounts()`/`restoreDbErrorCounts()` (key `db_error_counts` in `_backup_meta`, IST-day guard + per-key `Math.max` merge, same 60s tick as the ops counter); **NEW `ensureSqliteBackup()`** lazy on-demand init (`_initPromise` finally-reset — a failed init is retried on the next call, never stuck "Not Ready") + `resetSqliteStateForTests()` test hook (must mutate module `state` IN PLACE — replacing `g.__sqliteBackup` orphans the module's captured `state` binding); `/api/admin/db-health` GET returns `dbErrorSummary {day, counts}` (GET+POST ensure init); UI per-type summary chips (plan_limit/connection red w/ ring >0, timeout/accelerate_proxy/write_budget amber, other gray) + error total + IST-day footnote above Recent DB Errors. Tests: `db-utils.test.ts` `classifyDbError` (7 cases incl. real prod Accelerate message, benign P2021/P2002/P2025→other, non-Error→other) + `sqlite.test.ts` 5 new (error-count roundtrip, stale-day via mocked `getIstDayKey` reassignment, Math.max merge, ensure-ready, re-init after reset) → **suite 932 pass / 4 skip** (was 920/4, +12); tsc 46 = baseline (0 new production errors). **Commit of increment pending user.**

### 2026-09-02 | v3.21.2 — Stock-quote tiering (cache → SQLite → Prisma, zero after-hours DB writes) + TTL ms→s fix + SQLite backup/restore in db-health
- **Action**: COMMITTED `7409616` + pushed on `feat/db-health-ops-visibility` (one increment: Fix A–E + backup/restore, per user directive). **(A)** `lib/stock-service.ts` `syncDailyPriceOnce(symbol, snap)` — upsert gated to market-open + seed-once per symbol per IST day via `globalThis.__dailyPriceSynced` Set keyed `${getIstDayKey()}:${symbol}` (failure retries, not added to the Set); **zero Prisma writes after hours**. **(B)** `lib/sqlite.ts` — NEW `daily_price_snapshot` table (ticker PK, close/change/percentChange/lastUpdatedAt) + `createFallback` `getDailyPriceSnapshot`/`setDailyPriceSnapshot` + exported `getSqliteDailyPriceSnapshot`/`cacheDailyPriceSnapshot`; `syncFromPrisma` seeds via `DISTINCT ON` latest row per ticker; closed-market read chain `buildQuoteFromSnapshot`: hotCache → SQLite snapshot (ZERO Prisma) → **on snapshot miss only** `prisma.dailyPrice.findFirst` + `aggregate` + maybe `prevDayPrice` (2-3 Prisma reads, result re-populates hotCache). **(C)** `lib/enhanced-cache.ts` `Math.ceil(getRecommendedTTL(ms)/1000)` before `cacheInstance.set` — NodeCache TTL is SECONDS; ms was passed as seconds → entries lived ~1000× too long; corrected `enhanced-cache.test.ts` assertion `120000`→`120`. **(D)** `lib/services/priceSyncService.ts` — `cacheDailyPrice` gated by `isMarketAccumulationWindow()`, `cacheDailyPriceSnapshot` warms SQLite each tick. **(E)** `app/api/admin/db-health/route.ts` — `opsSnapshot` captured BEFORE probe/table-counts (honest per-request ops). **(F) SQLite backup/restore (user directive)**: `getSqlJs()`/`_SQL` module-let lazy `initSqlJs` (WASM from `public/`, serverExternalPackages), `exportSqliteBackup()` → `db.export()` (in-memory Uint8Array), `restoreSqliteBackup()` → 50MB cap + magic header `0x53514c69` + required tables (`_backup_meta`, `daily_recommendation_run`) + build fresh `SQL.Database` → swap via `_instance = createFallback(candidate)`; POST actions `backup`/`restore`; `/admin/utils/db-health` Backup & Restore card (Download ArrowDownTrayIcon decode base64→Blob; Apply Restore file input).
- **Files Created**: `lib/__tests__/dbOpTiering.test.ts` (9 tests: snapshot round-trip, upsert, backup header, restore apply, reject-oversize, reject-missing-tables).
- **Files Modified**: `lib/stock-service.ts`, `lib/sqlite.ts`, `lib/enhanced-cache.ts`, `lib/__tests__/enhanced-cache.test.ts`, `lib/services/priceSyncService.ts`, `app/api/admin/db-health/route.ts`, `app/admin/utils/db-health/page.tsx`, docs (AGENTS.md row, CHANGELOG index + versions-v3.21.md, plan doc, Primer, agent-memory, handoff).
- **Tests**: `dbOpTiering.test.ts` **9/9**; full suite **941 pass / 4 skip** (was 932/4, +9); tsc **46 = baseline**; no schema change → no migration.
- **Lesson 97**: NodeCache `set()` TTL is in SECONDS — always convert ms→s (and prefer explicit seconds) or cache entries live ~1000× too long.

### 2026-09-02 | v3.21.3 — Prisma OpenTelemetry tracing (opt-in) + Prisma Compute P1001 false-alarm diagnosis
- **Action**: (1) **OTel wiring** — installed `@prisma/instrumentation` (7.10.0) + 8 `@opentelemetry/*` (`api`, `sdk-trace-node`, `resources`, `semantic-conventions`, `sdk-trace-base`, `instrumentation`, `context-async-hooks`, `exporter-trace-otlp-http`; 25 pkgs, user-approved question). NEW `lib/otel.ts` `otelSetup()` — **STRICTLY opt-in**: returns false (hard no-op) unless `PRISMA_OTEL_ENABLED=1`; when enabled sets `AsyncHooksContextManager`, `NodeTracerProvider` (resource attrs `OTEL_SERVICE_NAME`/`OTEL_SERVICE_VERSION`, defaults `tradenext`/`3.21.3`), `SimpleSpanProcessor` → `OTLPTraceExporter` at `OTEL_EXPORTER_OTLP_ENDPOINT` (console fallback when unset), `provider.register()`, then `registerInstrumentations({ tracerProvider, instrumentations: [new PrismaInstrumentation()] })`; idempotent via globalThis `__tnPrismaOtelReady`; try/catch so OTel can never crash the app. Wired `lib/prisma.ts` module-top `otelSetup()` **before** the PrismaClient singleton (PrismaInstrumentation wraps the query engine at client construction). `.env.example` documents `PRISMA_OTEL_ENABLED`/`OTEL_EXPORTER_OTLP_ENDPOINT`/`OTEL_SERVICE_NAME`/`OTEL_SERVICE_VERSION`. NEW `lib/__tests__/otel.test.ts` (4 no-op guard tests). (2) **P1001 diagnosis (no code fix — user applies Dashboard toggle)**: the repeated "Prisma Compute Deploy failed — P1001 Can't reach db.prisma.io:5432" (#21) is Prisma Compute's **auto-schema-apply sandbox** running `migrate deploy` in a network-isolated sandbox that can't reach direct-TCP `db.prisma.io:5432`; Netlify deploys are HEALTHY (latest `main` deploy ready, ~60s; build = `prisma generate && quickbuild`, no `migrate deploy`); verified `prisma migrate status` from local = **36 migrations, up to date, ZERO pending** → auto-apply has nothing to do → pure false alarm. FIX (user-approved): Prisma Console → DB → **toggle OFF "apply schema changes automatically"**; future migrations via v3.20.5 runbook (`npx prisma migrate deploy` + DIRECT_URL from an env with egress). BUGS.md #13.
- **Files Created**: `lib/otel.ts`, `lib/__tests__/otel.test.ts`.
- **Files Modified**: `lib/prisma.ts` (import + `otelSetup()` call before singleton), `package.json`/`package-lock.json`, `.env.example`, `BUGS.md` (#13), docs (AGENTS.md v3.21.3 row, CHANGELOG index + versions-v3.21.md, plan doc q4, Primer, agent-memory, handoff latest.md).
- **Tests**: `otel.test.ts` **4/4** (unset→false, "0"→false, "1"→true, idempotent); full suite **945 pass / 4 skip** (was 941/4, +4 — OTel no-op in tests); tsc **46 = exact baseline (0 new)**; no errors in `lib/otel.ts`/`lib/prisma.ts`.
- **Lesson 98**: OpenTelemetry `PrismaInstrumentation` must be registered via `registerInstrumentations` (auto-instrumented client), set up BEFORE the PrismaClient singleton, and must be **env-gated + try/catch + idempotent** so tracing can never break a prod path. Prisma Compute's auto-schema-apply runs `migrate deploy` from a network-isolated sandbox that CANNOT reach direct-TCP hosts — P1001 on an up-to-date DB is a false alarm; disable auto-apply and apply migrations manually via `prisma migrate deploy` from an env with egress.
- **Status**: Code + tests + docs verified on `feat/db-health-ops-visibility` (on top of `7409616`); **commit + push pending user** (no auto-commit/push). Remind at commit: v3.21.0 `feat/stock-analysis-skill` PR + 2 Dependabot advisories.

### 2026-09-02 | v3.22.0 — Write-behind log store (SQLite-primary, Prisma promotion for important logs only) + leader election + audit-tag gap fill — Prisma ops target <1000/day
- **Action**: Completed the full in-flight `feat/db-health-ops-visibility` branch as one deployable unit (on top of v3.21.3). **(1) Leader election** — NEW `lib/services/leader.ts`: `LeaderRole` = `cron-daemon`|`worker`|`sqlite-sync`; `acquireLeaderLock`/`renewLeaderLock`/`releaseLeaderLock`/`isLeader`/`getLeaderInfo` — single-writer lock on `worker_status` (`leader-<role>` row + heartbeat, **5-min staleness**), so the 5-instance Netlify cold-start burst (2026-09-02 prod log: every instance syncing SQLite + scheduling duplicate cron/workers → Prisma ops/BG ~5–10×) reconciles to ONE sync + scheduler + flush timer. **DB down → fail-open to local leader**, re-elect on recovery. **`acquireLeaderLock` reconcile**: `createPath` origin flag — create-path non-conflict/unavailable errors **rethrow** (never silently stand down), generic `updateMany` claim-race failures **stand down → return false**, DB-unavailable returns **true** (fail-open). **(2) Write-behind promotion model** — SQLite = PRIMARY durable log store (14-day TTL); `drainWriteBehind` reads up to `WB_MAX_DRAIN_CHUNKS×WB_CHUNK` (250×8=2000), filters via NEW `isWbImportant()` (api `is_anomaly`/`is_rate_limited`/`status_code>=500`/has `error_message`; server_log only `error`|`warn`; audit action prefix `AUTH|JOIN|PASSWORD|ADMIN|SESSION|LOGIN|LOGOUT` OR ends `_FAILED`/`_BLOCKED`/`_REJECTED` OR `response_status>=400` w/ error) and promotes ONLY the important subset in **ONE `createMany`**; bulk info/api logs stay SQLite-only (**0 Prisma ops**). Returns `{flushed, retained, skipped}` (kind-keyed). **Double-count fix**: removed `dbOpsCounter.writes += chunk.length` in `writeWbRowsToPrisma` (`createMany`=1 op via `$allOperations`); sql.js mock `DELETE` honors `WHERE <pk> IN (...)`. NEW `pruneWriteBehind()` (14d TTL by PK), `startWriteBehindFlush()`/`stopWriteBehindFlush()` (15-min leader-gated on the `sqlite-sync` role, drains+prunes) booted after `startOpsCounterPersistence()` in `instrumentation.ts`; state + `WriteBehindStats` + `getWriteBehindStats()` + `flushWriteBehind()` surface `lastPromoted`/`lastRetained`; `SqliteFallback.flushWriteBehind` interface includes `retained`. **(3) Audit-tag gap fill (auto-promoted `ADMIN_*`)** — `lib/audit.ts` `AuditAction` +9: `ADMIN_DB_SYNC`, `ADMIN_DB_FLUSH_PRICES`, `ADMIN_DB_FLUSH_LOGS` (metadata flushed/retained/pending), `ADMIN_DB_DEPLOY_PREP` (surfaces retained), `ADMIN_DB_BACKUP`, `ADMIN_DB_RESTORE` — each `void createAuditLog` in `app/api/admin/db-health/route.ts` POST actions — + `ADMIN_RECOMMENDATION_RUN`, `ADMIN_PERFORMANCE_CHECK`, `ADMIN_SWING_PERFORMANCE_CHECK` (admin recommendations POST `run_now`/`check_performance`/`check_swing_performance`, `taskId` metadata). **(4) db-health UI kind-key fix** — `app/admin/utils/db-health/page.tsx`: `pending`/`lastFlushCounts` keyed by KIND (`api_request`/`server_log`/`audit_log`) NOT table (`wb_*`) — fixed pending cards + "Last Flush" line (were reading zeros forever) + emerald `lastPromoted` vs amber `lastRetained` split. **(5) cron-daemon heartbeat → local SQLite** — `writeHeartbeat` now `writeLivenessHeartbeat("cron-daemon", {daemonId, registeredJobs, memoryUsageMb})` to `_backup_meta` (in-memory `lastHeartbeatAt` powers the admin chip); **zero Prisma ops per heartbeat**; `cron-daemon.test.ts` updated to the new contract (+`@/lib/sqlite` mock exposing `getSqliteFallback().writeLivenessHeartbeat`, asserts `prisma.workerStatus.upsert` NOT called).
- **Files Created**: `lib/services/leader.ts`, `lib/__tests__/leader.test.ts` (18), `.agents/specs/01-db-ops-reduction.md`, `.agents/plans/01-db-ops-reduction.md`, `.agents/changelog/versions-v3.22.md`.
- **Files Modified**: `lib/sqlite.ts` (promotion model, `isWbImportant`, `pruneWriteBehind`, `startWriteBehindFlush`/`stopWriteBehindFlush`, state/stats `wbLastPromoted`/`wbLastRetained`, `WriteBehindStats`, `SqliteFallback` interface, `wbLastFlushCounts` kind-keyed, double-count removal, mock `__resetStore` + `DELETE IN`), `instrumentation.ts` (boot flush timer), `lib/audit.ts` (+9), `app/api/admin/db-health/route.ts` (6 `ADMIN_DB_*` audits, deploy_prep retained), `app/api/admin/recommendations/route.ts` (3 admin audits), `app/admin/utils/db-health/page.tsx` (kind-key fix + promoted/retained split), `lib/services/worker/cron-daemon.ts` (heartbeat → SQLite), `lib/__tests__/sqlite.test.ts` (33/33), `lib/__tests__/audit.test.ts` (9 new actions), `lib/__tests__/cron-daemon.test.ts`, docs (AGENTS.md v3.22.0 row, CHANGELOG index, TODO.md row, Primer, Lessons, agent-memory, ARCHITECTURE, rules/guardrails).
- **Tests**: sqlite `__resetStore` mock isolation (write-behind `beforeEach` now async clears store + `resetSqliteStateForTests()` + `ensureSqliteBackup()`); promotion test (`flushed`/`retained` splits, `createMany` called once, `lastPromoted`/`lastRetained`); regression (600 error rows → 3 `createMany`, writes counter unchanged); `leader.test.ts` **18/18** (claim/reclaim/staleness/renew/release/fail-open/stand-down/rethrow); `cron-daemon.test.ts` heartbeat contract. Full suite **972 pass / 4 skip / 0 fail** (was 945/4, +27); tsc **46 = baseline (0 new)**; no schema change → no migration.
- **Lesson 99**: A write-behind log store plus single-writer leadership is how a multi-instance deploy stops multiplying Prisma plan ops: SQLite (in-memory, wiped on deploy) is the primary durable log store with a 14-day TTL, and only *important* rows (error/warn server logs + security/critical audit tags + api 5xx/rate-limited/anomaly) get promoted to Prisma in ONE `createMany` per drain — bulk info/api logs never touch Prisma. Leader election (`worker_status` `leader-<role>` + 5-min staleness + DB-down fail-open) reconciles N instances to one SQLite-sync + scheduler + flush timer. And `createMany` counts as **1 op**, never `+= rows`.
- **Status**: Code + tests + docs verified as the deployable v3.22.0 unit; **commit/PR/deploy pending user** (no auto-commit/push/deploy). After deploy, live-verify `/admin/utils/db-health` shows promoted-vs-retained split + healthy ops count; single cron-daemon/write-behind heartbeat; leader implied by one `leader-sqlite-sync` row.

### 2026-08-28 | v3.20.4 — Plan-limit breaker false-positive FIX + missing `intelligence_cache` migration (CI-RED root cause)
- **Action**: Playwright CI went RED on auth/login ("Plan limit circuit breaker open"). Investigation proved NOT an external prod hold (CI uses a fresh local TimescaleDB) but TWO code defects. **(A) `isDbUnavailableError()` false-positive (v3.20.3 regression, PRIMARY)**: the blanket `name.includes("prismaclient") && name.includes("request")` branch classified EVERY `PrismaClientKnownRequestError` (benign P2021/P2002/P2025) as "DB unavailable" → `$allOperations` opened the global plan-limit breaker on the FIRST benign error → 5-min full DB freeze (`PLAN_LIMIT_COOLDOWN_MS`) → auth failed. Removed the blanket + redundant bare `msg.includes("exceeded")` (would also match value-out-of-range data errors) → the breaker now trips ONLY on REAL hold/unavailability (P6003, ECONN*/ETIMEDOUT, P2024 timeout, hold/proxy/fetch-failed messages), never on benign app-level request errors. **(B) Missing `intelligence_cache` migration (v3.18.0 gap, trigger)**: applied only via local `db push` (no ledger) → `migrate deploy` (CI/prod) never created the table → `restoreIntelligenceCacheFromDB()` P2021 tripped Defect A. Added `prisma/migrations/20260828000000_add_intelligence_cache` — validated column/index-for-index identical to Prisma's `db push` output (id/symbol text NOT NULL, version int default 1, data jsonb, modelUsed, generatedAt/createdAt default now, expiresAt; unique symbol + symbol-expiresAt index) so CI/prod `migrate deploy` creates it exactly; non-destructive on DBs that already have it via `db push`.
- **Files Modified**: `lib/db-utils.ts` (removed blanket `name` catch-all + bare `"exceeded"` from `isDbUnavailableError`), `.agents/changelog/versions-v3.20.md` (v3.20.4 entry), `.agents/CHANGELOG.md` + `CHANGELOG.md` (v3.20.4 row), `AGENTS.md` (v3.20.4 table row), `Lessons.md` (#94), `Primer.md`, `agent-memory.md`, `TODO.md`.
- **Files Created**: `prisma/migrations/20260828000000_add_intelligence_cache/migration.sql`; `lib/__tests__/db-utils.test.ts` +4 real-shape regression tests (benign P2021/P2002/P2025 → false; connectivity P1001/P2024/P6003 → true, using the REAL `name:"PrismaClientKnownRequestError"` shape the old code falsely matched).
- **Tests**: Suite **917 pass / 4 skip** (was 915/4, +4); tsc **46 = exact baseline** (0 new production errors).
- **Lesson 94**: error-predicate catch-alls are latent global kill-switches — match by Prisma CODE, not class name; test predicates with the REAL error shape; every new Prisma model needs a real migration folder for `migrate deploy`.
- **Status**: Code + tests + docs verified on `feat/plan-limit-resilience` (PR #107); **commit/push pending user** (no auto-commit). After push, re-run CI to confirm PR #107 goes green, then merge with approval.


- **Action**: Deep upgrade of the v3.18.0 AI Investment Intelligence into an institutional equity-research decision engine. (1) **8-level verdict** (STRONG_BUY/BUY/ACCUMULATE/HOLD/REDUCE/SELL/STRONG_SELL/AVOID) + `conviction` /10 + `confidence` /100 (new `Verdict` enum in `intelligenceTypes.ts`). (2) **12-section memo** — executive thesis, fundamental score with evidence labels (CALCULATED_METRIC/FACT/MANAGEMENT/INFERENCE/INTERPRETATION), management DNA, valuation zones (attractive/fair/over + current-price marker), technical structure incl. marketPhase, shareholding analysis, risk matrix (category/probability/impact/pricedIn), catalysts, bull/base/bear scenario, contrarian view + what-would-change-my-mind, portfolio action (positionSizing), invalidation zones. (3) **Honest data gaps** — `dataGaps` + DataGapsBanner (never fabricated). (4) **Optional raw-text document ingestion** — annual-report/concall pasted into company-page textareas (50KB cap each) → appended to prompt as secondary-unverified sections. (5) **Backward compatible, no DB migration** — legacy `buildIntelligencePrompt`/`parseIntelligenceResponse` kept (18 tests pass); all new `IntelligenceAnalysis` fields optional `?`; legacy JSON parses onto the 8-level enum.
- **Files Created**: `lib/services/document/normalize.ts` (normalizeDocumentText), `app/components/intelligence/sections/` (ManagementDnaSection, ValuationZonesSection, ContrarianSection, PortfolioActionSection, DataGapsBanner, TechnicalStructureSection, FundamentalScoreSection, ShareholdingAnalysisSection, ExecutiveThesisSection), `lib/__tests__/stock-analysis-prompt.test.ts` (21), `lib/__tests__/document-normalize.test.ts` (9).
- **Files Modified**: `intelligenceTypes.ts` (+Verdict/EvidenceLabel/MarketPhase/EvidencePoint/ManagementDna/ValuationZones/RiskItem/ContrarianView/PortfolioAction + expanded IntelligenceAnalysis), `lib/services/ai/intelligence-prompt.ts` (+buildStockAnalysisPrompt/parseStockAnalysisResponse, legacy kept), `lib/services/ai/intelligence.ts` (orchestrator documents path + audit metadata modelUsed/verdict/conviction/confidence/hasDocuments/partialData; whitespace-only docs → hasDocuments:false fix), `lib/services/intelligence/adapters.ts` (sma200 280-day best-effort), `app/api/company/[ticker]/intelligence/route.ts` (POST Zod documents schema, 50K cap, 400 invalid), UI — `VerdictCard` (8-verdict + conviction bar), rewritten `IntelligencePanel`/`CompanyIntelligence`/`RiskCatalystMatrix`, `lib/__tests__/intelligence.test.ts` (+3 documents tests).
- **Tests**: Suite **915 pass / 4 skip** (+32, was 883/4); tsc **46 = baseline** (0 new production errors).
- **Docs Updated**: AGENTS.md v3.21.0 row, `.agents/CHANGELOG.md` index + versions-v3.21.md, CHANGELOG.md, TODO.md, Primer.md, agent-memory.md, session `2026-08-28-stock-analysis-skill/` (decisions + flow).
- **Status**: Code ready on `feat/stock-analysis-skill`; commit/push/PR pending user (no auto-commit).

### 2026-08-28 | v3.20.3 — Plan-Limit Hold Resilience: Prisma P6003 recognition + circuit breaker + non-blocking audit/log + worker/cron backoff
- **Action**: Hardened the app so it degrades gracefully when the Prisma Postgres account is on its plan-limit hold (code `P6003`, `planLimitReached`). (1) `isDbUnavailableError()` now recognizes the real hold error (`"hold on your account"`/`"planlimitreached"`/`"plan limit reached"`, code `P6003`, names `PrismaQueryTimeoutError`/`PlanLimitOpenError`) so all 18+ graceful-degrade fallback chains actually trigger on the hold (previously treated as a hard 500). (2) Added a plan-limit circuit breaker (`PlanLimitOpenError` + `isPlanLimitBreakerOpen`/`open`/`close`/`getStatus`/`reset`) wired into the `$allOperations` extension — fail-fast (no 120s proxy wait), opens on P6003/hold/timeout, closes on a successful half-open probe (auto-recovery when the hold lifts), `PLAN_LIMIT_COOLDOWN_MS` 5min env-overridable. (3) Made `createAuditLog()` (`lib/audit.ts`) + `logAPIRequest()` (`lib/rate-limit.ts`) **fire-and-forget** (resolve immediately) — ~50+ `await` call sites never stall on a held DB. (4) Worker engine poll loop `setInterval`→self-rescheduling `setTimeout` with DB backoff (30s→5min cap, resets on success, `workerStopped` flag). (5) Cron daemon boot `syncCronJobs()` wrapped in try/catch (warn, no throw) + per-tick resync downgraded to warn on DB-unavailable. (6) Notifications route skips DB-unavailable `console.error` spam (still graceful 200 empty).
- **Files Modified**: `lib/db-utils.ts` (`isDbUnavailableError` P6003/hold + NEW breaker helpers), `lib/prisma.ts` (breaker wired into `$allOperations`, null-safe `.then`/`.catch` chain), `lib/audit.ts` (`createAuditLog` fire-and-forget), `lib/rate-limit.ts` (`logAPIRequest` fire-and-forget), `lib/services/worker/worker-engine.ts` (setTimeout poll + DB backoff + `workerStopped`), `lib/services/worker/cron-daemon.ts` (boot/per-tick DB guard + downgrade), `app/api/notifications/route.ts` (skip DB-unavailable spam).
- **Files Created**: `lib/__tests__/db-utils.test.ts` (14 tests — `isDbUnavailableError` P6003/hold/timeout/PlanLimitOpenError matrix + breaker open/close/cooldown via fake timers).
- **Tests**: Suite **883 pass / 4 skip** (was 869/4, +14); tsc 57 = baseline (0 new production errors; remaining non-`.next` output is pre-existing test-file typing noise).
- **Docs Updated**: AGENTS.md v3.20.3 row, CHANGELOG.md, `.agents/CHANGELOG.md` versions-v3.20 entry, versions-v3.20.md v3.20.3 detail, Primer.md, agent-memory.md, Lessons.md.
- **Status**: Code ready on `feat/plan-limit-resilience`; commit/push/PR pending user. External blockers: Prisma Postgres extension must be removed from Netlify Dashboard before deploy; hold must be lifted before full recovery (breaker auto-recovers).

### 2026-08-28 | playwright-debug skill + agent wiring (tooling/docs only, no code/test/API change)
- **Action**: Built a dedicated **`playwright-debug`** skill from the user-pasted Playwright developer-tooling reference (Inspector `--debug`, HTML report, Codegen, Trace Viewer, emulation), then wired it into every coding/verification agent. (1) **Machine skill** `.opencode/skills/playwright-debug/SKILL.md` (YAML frontmatter `name`/`description`/`allowed-tools: Bash(npx playwright *), Bash(npm run test:e2e:*)`): quick problem→tool matrix, Inspector/UI Mode/Codegen/Trace/Report sections, TradeNext config facts, recommended diagnosis flow, `trace: 'on-first-retry'` gotcha, locator strategy (role/text over raw CSS), related skills. (2) **Human mirror** `.agents/skills/playwright-debug/SKILL.md` (Source: footer). (3) **Deep-dive** `.agents/docs/playwright-debug.md` (matches `playwright-e2e.md` pattern). (4) **6 agent profiles** updated (qa, e2e-agent, bug-hunter, ux-designer, code-reviewer, tdd-guide). (5) **opencode.json** prompts wired for qa, e2e-agent, bug-hunter, ux-designer, code-reviewer, tdd, + build agent UI/UX section. (6) `.agents/AGENT-SKILL-MATRIX.md` + `AGENTS.md` focused-skills table updated.
- **Files Created**: `.opencode/skills/playwright-debug/SKILL.md`, `.agents/skills/playwright-debug/SKILL.md`, `.agents/docs/playwright-debug.md`.
- **Files Modified**: `.opencode/opencode.json` (7 agent prompts), `.agents/agents/{qa,e2e-agent,bug-hunter,ux-designer,code-reviewer,tdd-guide}.md`, `.agents/AGENT-SKILL-MATRIX.md`, `AGENTS.md`.
- **Tests**: N/A — tooling/docs-only, no code/test/API/behavior change to the shipped app.
- **Lesson Learned (opencode.json editing)**: agent prompts are **single-line JSON strings with literal `\n` escapes**; `filesystem_edit_file` is whitespace-sensitive, so a raw `"title"` (unescaped double quotes) inside an inserted command breaks the JSON (SyntaxError). Use the `edit` tool with exact substrings and escape inner quotes as `\"` (e.g. `-g \"title\"`); validate with `node -e "JSON.parse(...)"` after every edit. Verified JSON OK after the fix.
- **Status**: Uncommitted on `feat/plan-limit-resilience` (sits with v3.20.3). Commit only on explicit user request. PR #107 still open/pending user decision.

### 2026-08-27 | v3.20.1 + v3.20.2 — DB Ops Optimization + DB Health Enhancements + Daily Price Cache Batch Writer
- **Action**: Reduced DB ops from ~22K to ~4.2K/day (v3.20.1: worker poll 5s→30s, cron resync 60s→5min, heartbeat 5min→15min, legacy scheduler removed, web-vitals DB writes removed). Added DB failure ring buffer (`recordDbError()`/`getDbErrorLog()` — last 50 errors auto-recorded in `$allOperations` extension). Added Daily Price Cache batch writer (`cacheDailyPrice()` in-memory accumulate during market hours → bulk `$executeRawUnsafe` upsert after 4 PM IST → ~1 write/day). Enhanced DB Health API + UI (ops counter, price cache section, DB errors table, Flush Prices button).
- **Files Modified**: `lib/prisma.ts` (ring buffer + `WRITE_BUDGET_CONFIG`), `lib/services/priceCache.ts` (merged SSE PriceCache + NEW DailyPriceAccumulator), `lib/services/priceSyncService.ts` (`cacheDailyPrice` in `fetchAndEmit`), `instrumentation.ts` (flush timer + worker poll 30s), `app/api/admin/db-health/route.ts` (ops + price cache + errors; `flush_prices` POST action), `app/admin/utils/db-health/page.tsx` (price cache card/section, DB errors table, flush button, day key).
- **Tests**: Suite 869 pass / 4 skip = baseline; tsc 57 = baseline (0 production errors).
- **Docs Updated**: AGENTS.md v3.20.2 row, versions-v3.20.md v3.20.2, TODO.md, agent-memory.md.
- **Status**: Committed `5156eb3` (v3.20.1) + this session's v3.20.2 code on `feat/db-health-price-cache`.

### 2026-08-26 | v3.20.0 — NSE Resilience: All NSE Routes Return Graceful Empty + MCP GET Fix + Constants Consolidation
- **Action**: Hardened all 17 NSE-dependent API routes to return graceful empty/null instead of 500/502 on failure. Fixed MCP GET endpoint (shared handler). Decoupled corporate actions from NSE blocking. Fixed `/api/news/market` Prisma import + error handling. Consolidated NIFTY_50 constants to `lib/constants.ts`. Added 2026 market holidays. Removed stale Prisma Postgres extension from netlify.toml.
- **Files Modified**: `app/api/mcp/route.ts` (shared `handleMcpRequest()`), `app/api/corporate-actions/combined/route.ts` (NSE decoupling via `triggerNseRefresh()`), `app/api/news/market/route.ts` (fixed import, DB catching, memory fallback), `app/api/nse/gainers/route.ts`, `app/api/nse/losers/route.ts`, `app/api/nse/most-active/route.ts`, `app/api/nse/corporate-announcements/route.ts`, `app/api/nse/corporate-events/route.ts`, `app/api/nse/corporate-info/route.ts`, `app/api/nse/corporate-news/route.ts`, `app/api/nse/deals/route.ts`, `app/api/nse/insider-trading/route.ts`, `app/api/nse/marquee/route.ts`, `app/api/nse/indexes/route.ts`, `app/api/nse/index/[index]/route.ts`, `app/api/nse/index/[index]/heatmap/route.ts`, `app/api/nse/index/[index]/advance-decline/route.ts`, `app/api/nse/index/[index]/announcements/route.ts`, `app/api/nse/index/[index]/corp-actions/route.ts`, `app/api/nse/index/[index]/chart/route.ts`, `app/api/nse/index/[index]/symbols/route.ts`, `app/api/nse/stock/[symbol]/quote/route.ts`, `app/api/nse/stock/[symbol]/chart/route.ts`, `app/api/nse/stock/[symbol]/trends/route.ts`, `app/api/nse/stock/[symbol]/corporate/route.ts`, `lib/constants.ts` (NIFTY_50, MARKET_HOLIDAYS), `lib/services/marketCapClassification.ts` (imports from constants), `netlify.toml` (removed stale extension).
- **Tests**: Suite 869 pass / 4 skip (unchanged); tsc 46 = exact baseline.
- **Docs Updated**: AGENTS.md v3.20.0 row, CHANGELOG new versions-v3.20.md, TODO.md, Primer.md, agent-memory.md.
- **Status**: Code verified, uncommitted.

### 2026-08-26 | v3.19.3 — Graceful Degradation When DB Is Unavailable
- **Action**: Fixed 5 graceful degradation issues + SQLite recovery probe bug found during comprehensive live site analysis.
- **Files Modified**: `app/api/metrics/web-vitals/route.ts` (fire-and-forget DB write with try/catch), `app/api/portfolio/route.ts` (empty portfolio + warning on DB failure), `app/api/notifications/route.ts` (empty + warning on DB failure), `app/api/nse/advance-decline/route.ts` (200 not 500 on NSE failure), `lib/sqlite.ts` (recovery probe `else` branch fix).
- **Tests**: Suite 869 pass / 4 skip (unchanged); tsc 46 = exact baseline.
- **Docs Updated**: AGENTS.md v3.19.3 row, CHANGELOG versions-v3.19.md, Lessons.md #87, Primer.md, agent-memory.md, HANDOFF.md.
- **Status**: Committed `35f3c6a` + pushed; PR pending.

### 2026-08-17 | v3.15.0 — Closed IPOs with current prices + IPO analysis TTL cleanup + pipeline redesign (HOLDs collapsible)
- **Action**: Implemented closed IPOs section with current prices, IPO analysis TTL cleanup, and pipeline redesign (top-100 market cap → AI → top-50 actionable + collapsible HOLDs).
- **Files Created**: `app/api/recommendations/ipos/closed/route.ts` (batch price endpoint), `lib/__tests__/closedIpoPrices.test.ts` (18 tests), `.agents/specs/closed-ipos-ttl-cleanup.md`, `.agents/plans/closed-ipos-ttl-cleanup.md`, `.agents/changelog/versions-v3.15.md`
- **Files Modified**: `lib/services/dailyRecommendationService.ts` (selectTopByMarketCap, rankActionableByConfidence, MAX_AI_STOCKS=100, MAX_RECOMMENDABLE_STOCKS=50), `app/components/recommendations/DailyPicksTab.tsx` (showHolds toggle + collapsible HOLD section), `lib/services/ipoAnalysisService.ts` (cache-hit monitoring + cleanStaleIpoAnalysisRows), `lib/services/worker/worker-service.ts` (executeIpoAnalysisPrewarm + executeIpoAnalysisCleanup + standalone task types), `app/api/admin/cron/route.ts` + `app/api/admin/workers/route.ts` (TASK_TYPES updated), `app/components/recommendations/IposTab.tsx` (rewritten: Active+Forthcoming only + collapsible Closed section with gain/loss), `lib/__tests__/ipoAnalysisService.test.ts` (+3 cleanup tests), `lib/__tests__/ipoAnalysisPrewarm.test.ts` (5 pre-warm tests), `AGENTS.md` (v3.15.0 row), `.agents/CHANGELOG.md` (index updated), `.agents/changelog/versions-v3.md` (index updated), `Primer.md` (v3.15.0 status), `agent-memory.md` (this entry), `.agents/session-todos.md` (updated)
- **Tests**: Suite 787 pass / 4 skip (was 758/4, +29 new tests); tsc 46 = exact baseline, 0 new
- **Live-verified**: Pipeline 30 Total / 16 Buy / 5 Hold / 9 Sell, HOLDs collapsed, IPOs tab 4 Active + 1 Upcoming, AI Analysis modal opens, `ipo_analysis: 2 (29%)` in monitoring
- **Specs**: `.agents/specs/pipeline-top100-confidence.md` (approved), `.agents/plans/pipeline-top100-confidence.md` (approved), `.agents/specs/closed-ipos-ttl-cleanup.md` (approved), `.agents/plans/closed-ipos-ttl-cleanup.md` (approved)
- **Status**: All code + tests verified, commit pending user, no push/deploy

### 2026-08-18 | Agent Profile Restructuring — `.agents/` Wiring + Stale Tooling Cleanup
- **Action**: Restructured `.agents/` ecosystem for proper agentic coding: moved misplaced files, updated agent profiles, wired missing agents in opencode.json, updated matrix.
- **Files Moved**: `.agents/changelog/{screener,corp-actions,security-workers,serverless-logging}.md` → `.agents/docs/` (legacy feature deep-dives, not version changelogs)
- **Files Updated**: `.agents/CHANGELOG.md` (index paths), `.agents/docs/README.md` (added 4 moved docs), `AGENTS.md` (skills table + artifact table), `.agents/AGENT-SKILL-MATRIX.md` (14 agents, 8 skills, 6 commands), `Lessons.md` (Lesson 84 — stale tooling refs)
- **Agent Profiles Updated**: `qa.md` (Playwright MCP tools, skill ref), `e2e-agent.md` (Playwright MCP + Chrome DevTools MCP, skill ref), `devops.md` (Netlify-only, removed Vercel)
- **opencode.json Updated**: Added 7 missing agents: qa, e2e-agent, devops, code-reviewer, integrator, observability; added `/nse-integration` command
- **Lesson 84 Added**: Agent profiles must reference correct tooling; every profile needs a Skill reference; opencode.json must have entries for ALL subagent-invocable agents
- **Status**: Commit pending

### 2026-08-19 | v3.19.0 — DB Plan Limit Resilience (Prisma Postgres 10K ops/day exceeded)
- **Action**: Implemented full DB plan limit resilience stack across 14 files: graceful degradation, op reduction, write budget guard, admin OTP fallback, admin DB usage dashboard.
- **Files Created**: `lib/db-utils.ts` (`isDbUnavailableError()`), `app/api/admin/db-usage/route.ts` (admin dashboard endpoint)
- **Files Modified**: `lib/prisma.ts` (dbOpsCounter + write budget guard), `lib/auth.ts` (admin OTP fallback), `lib/services/dailyRecommendationService.ts` (fingerprint bypass + DB error fallback), `lib/services/chartinkScreenerService.ts` (NodeCache + DB error fallback + cache invalidation + fixed cache key to `chartink:screeners:overview`), `lib/services/historicalPriceSyncService.ts` (NIFTY50-only scope, `DEFAULT_MAX_SYMBOLS=300→50`), `lib/services/worker/cron-daemon.ts` (`HEARTBEAT_INTERVAL_MS=300_000`), `lib/services/worker/worker-engine.ts` (`HEARTBEAT_INTERVAL_MS=300_000`, `WORKER_ALIVE_WINDOW_MS=600_000`), `lib/market-cache.ts` (TTL defaults 600/7200), `app/api/corporate-actions/combined/route.ts` (NodeCache + DB error fallback + variable scope fix), `app/api/events/route.ts` (graceful empty on failure), `app/api/recommendations/ipos/route.ts` (graceful empty on failure), `.env.example` (ADMIN_OTP, DB_WRITE_BUDGET docs)
- **Test fixes**: `chartinkScreenerService.test.ts` (correct cache key `chartink:screeners:overview`), `historicalPriceSyncService.test.ts` (NIFTY50-only scope), `cron-daemon.test.ts` (600s heartbeat window)
- **Docs updated**: AGENTS.md v3.19.0 row, CHANGELOG index + versions-v3.19.md, TODO.md marked done, Primer, agent-memory (this entry), Lessons #85 (cache key must match source)
- **Suite**: 852 pass / 4 skip = exact baseline; tsc 46 = exact baseline, 0 new
- **Status**: All code + tests verified, commit pending user, no push/deploy

### 2026-08-25 | v3.19.2 — SQLite Expanded + Re-sync + Admin DB Health Dashboard
- **Action**: Expanded the SQLite backup layer to cover all 10 tables (6 new: worker_status, server_log, audit_log, cron_job, cron_run, worker_task), added background recovery sync (5-min probe when Prisma is down, auto-sync on recovery), and built a full admin DB health monitoring dashboard.
- **Files Created**: `app/api/admin/db-health/route.ts` (GET: Prisma probe + ops + table counts + SQLite health; POST: manual sync), `app/admin/utils/db-health/page.tsx` (dashboard: status badges, stat cards, write budget bar, table comparison, sync history, manual sync, 30s refresh)
- **Files Modified**: `lib/sqlite.ts` (rewritten — 6 new tables, expanded `syncFromPrisma()` for all 10 tables, new query helpers `getServerLogs`/`getAuditLogs`/`getCronJobs`/`getCronRuns`/`getWorkerStatuses`/`getWorkerTasks`/`getHealthStatus`, `syncHistory` array, `startRecoveryProbe()` background interval, `lastProbeAt`), `app/admin/utils/layout.tsx` (DB Health nav entry), `lib/__tests__/sqlite.test.ts` (expanded 9 → 17 tests for new tables + health status + failure history)
- **Tests**: Suite 869 pass / 4 skip (was 861/4, +8 new); tsc 46 = exact baseline, 0 new
- **Docs updated**: AGENTS.md v3.19.2 row, CHANGELOG index + versions-v3.19.md, TODO.md, Primer.md, agent-memory (this entry), HANDOFF.md, ARCHITECTURE.md, docs/architecture.html, Lessons.md (Lesson 86), README.md, .agents/docs/monitoring-and-logging.md
- **Status**: All code + tests verified + committed + pushed on `feature/ai-intelligence`

The pre-commit hook is also installed at `.git/hooks/pre-commit`:
- Checks for `console.log` statements (should use logger)
- Detects hardcoded secrets (passwords, API keys, tokens)

---

## Archived Activity (moved out — read on demand)

> Older activity entries moved to **`.agents/session-archive/agent-memory-archive.md`**
> (append-only log — age == obsolete). Recent entries stay inline above.

---

## How to Use

1. **Start of session**: Read `Primer.md` to understand current state
2. **During work**: Use this file to track activities
3. **End of session**: Update `Primer.md` with summary
4. **Before commit**: Read `Lessons.md` to avoid repeated mistakes

---

## Tips

- Use `grep` to search this file for past activities
- Keep entries concise but informative
- Include file names when relevant
- Note any errors or issues encountered



`n- 2026-09-20 (v3.40.4 **Swing steady-state serve fix**: serve stored AI targets on page load; AI runs only on manual refresh. Branch feat/pwa-sprint-phase-b-ga4; commit 9e7ab89. Docs: versions-v3.40.md + versions-index.md + Primer Last Updated + this log updated; push pending user).

- 2026-09-23 — **v3.40.8**: P0 Laya spike scaffold + install + download + chained encoder→head smoke PASSED, VERDICT APPROVE onnxruntime-node (SPLIT graphs, chain 2316ms @ seq=128, RSS 611MB). IO contract mapped. Next: engine P1–P6 (user approval).
