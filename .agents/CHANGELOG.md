# TradeNext Changelog

> Full version history + legacy feature docs (moved here from AGENTS.md 2026-08-06). Since **v3.39.4** the compact version table ALSO lives here (`versions-index.md`) — `AGENTS.md` is a thin index only, so nothing large is auto-injected each session. This directory is the source of detail.
>
> **Reading guidance**: load only the subfile you need (e.g. v3.x for recent work), never the whole directory. v3/v2/v1 detail is split per-minor (`versions-v3.13.md` … `versions-v3.0.md`, `versions-v2.2.md`/`versions-v2.1.md`, `versions-v1.16.md` … `versions-v1.8.md`); `versions-v3.md` / `versions-v2.md` / `versions-v1.md` are per-minor indexes.

## Index

| File | Contents |
|------|----------|
| [versions-v3.41.md](./changelog/versions-v3.41.md) | **v3.41.2** — **Spec 01 Recommendations plan-limit fallbacks (History/Performance/Ideas) + HistoryTab error state**: NEW `lib/sqlite.ts` `getRecommendationRuns()` (camelCase, newest-first, unique_stocks>0, status filter, limit clamp 1..2000) → `top-stocks/route.ts` `topStocksFromSqlite()` fallback (mirror read → serialize → 1 h cache; Prisma `$queryRaw` kept) → `recommendationPerformanceService.ts` `listItemFromMirrorTracker`/`getPerformanceListFromSqlite` (JS status filter, timerange) + wrapped Prisma branch → `syncedDataService.ts` breaker/hold-aware `mirrorWriteThrough` + `mirrorReadMarketCache` (steps 2+3; `isPlanLimitBreakerOpen()` + `getDbHealthState` + DateTime-safe cache key). Contract: zero Prisma ops in fallbacks, degraded `[]` only when mirror HAS data (mirror-exhausted/not-ready RETHROWS the ORIGINAL 500 — Lesson 138 masked-500-as-empty-state fix), only `isDbUnavailableError` caught, Prisma happy path byte-identical. HistoryTab `error` state + Retry rendered BEFORE empty-state list. NEW recommendationsPlanLimitFallbacks.test.ts 21/21; tsc 46 exact (0 new) · lint 0 · **109/109 (1440 pass / 4 skip / 0 fail)** · quickbuild 189/189 ✓ · e2e recommendations.spec.ts **10/10** (live dev server). · **v3.41.1** — **Spec 17 Decision Engine monitoring + e2e hardening**: lib/services/decision/monitoring.ts zero-Prisma ring buffer (max 500) + stats aggregation wired into client evaluate/ping + POC A screener + POC B swing (`DECISION_TRACE_ENABLED`-gated, off = byte-identical) + GET /api/admin/decision/monitoring (stats\|traces) + DELETE clear + Decision Engine tab in AI Monitoring (6 stat cards + breakdowns + traces + Clear) + OpenAPI + NEW e2e/decision-monitoring.spec.ts (env-only creds, serial, resilient login — first WebKit login in suite, Lesson 136) + **auth-gate hardening (Lesson 137 — Auth.js double-submit CSRF race root-caused via trace: concurrent no-cookie session GETs mint tokens A+B, jar keeps B, signIn csrf GET returns A → MissingCSRF → "Invalid email or password" on correct creds; config retries can't fix → in-context 2-attempt resubmit loop in e2e/auth.setup.ts + e2e/login.spec.ts)**. Verified: tsc 46 exact · lint 0 · 108/108 (1416 pass / 4 skip / 0 fail) · quickbuild ✓ · full e2e headless 86 passed / 1 failed (pre-existing nav flake News-webkit) / 2 did-not-run (serial skips) — auth gate green in every run. · **v3.41.0** — **Spec 16 Decision Engine CORE (Laya-only)** COMMITTED `ab6fd65`: lib/services/decision/ mock engine (types/provider/gate/layaProvider/client/fusion) + confidence-gated ACT/REVIEW + DECISION_EVALUATED/DECISION_GATE audit tags + POC A screener scoring + POC B swing gateAutoGenerate + POST /api/decision/evaluate + GET /api/admin/decision/ping + OpenAPI + admin panel + check-tsc-baseline harness ENOENT fix (Lesson 135); typesafeProvider deleted. Verified: tsc 46 exact · lint 0 · 107/107 (1401 pass / 4 skip / 0 fail) · quickbuild 188/188. v3.41.1 COMMITTED `a269057`; v3.41.2 commit pending user approval |
| [versions-v3.40.md](./changelog/versions-v3.40.md) | **v3.40.5** — **Laya / System-One decision-engine RESEARCH (docs-only)** — 5 parallel subagents scraped `convaiinnovations/laya` HF card + all TypeSafe.AI docs + Medium; **Jev-1.13.0 premise corrected** (`github.com/answers-ai/jev` = 404 — Jev is TypeSafe's proprietary hosted API `/v1/systemone`, model `jev-latest`); outputs `memory.md` (durable budget-preserving reference, not injected), `docs/laya.md`, `docs/designDoc/ph22-laya-decision-engine-design.md` (proposed provider-agnostic decision layer: Laya local / TypeSafe cloud, Choice/Score/Noul, confidence-gated routing; POC screener composite-scoring + Swing gatecheck); Medium article 403-blocked (snippets only); no code/migration/packages; next = spec+plan for engine build. · **v3.40.4** — **Agentic context, orchestration & harness — fixes the REMAINING compaction loop + orchestrator prefers parallel subagents + subagent health monitor + handoff SCHEMA v1.1 + code/test/build gates** (branch `feat/agentic-context-orchestration`, stacked on `fix/turbopack-tracing-harness @ ca56a74`; commits `b6552ca` spec/plan/session → `1f73d3c` W1 → `40181c4`+`db08255` W2 → `f758e67` W3 → `ac91571` W4 → `ac90b53` W5+W6 → `973cae5` W7 → `6a9135a` Phase 7 tests; **MERGED via PR #127 (merge `48df4d4`, main tip `898a3f6` "docs: update changelog [skip ci]") — production deploy initially FAILED (`Bundling of function "emails" failed`: UI-installed legacy plugin `@netlify/plugin-emails@1.1.1` "from Netlify app" generates a CommonJS `emails/index.js` under repo-root `"type": "module"`); root fix = disable the Emails extension in the Netlify UI (site `plugins` verified via `getSite`: nextjs + lighthouse only; the plugin is absent from `get-extensions` so the extensions MCP can't remove it — Lesson 127); retry deploy `6aacebd948646f5a24068933` **ready** on commit `898a3f6`, functions = only `___netlify-server-handler` + `__csp-violations`, secrets scan 900 files / 0 matches — **v3.40.0 LIVE 2026-09-18**. (v3.39.4 killed the injected-file cause — 358→72 KB); the residual loop is the **conversation transcript** (large tool outputs + long summaries re-sent every request, re-injected right after compaction). **W1** NEW `.agents/rules/tool-output-protocol.md` (never land large output in-context; redirect → narrow slice/grep; chunk when too big) + NEW `scripts/dev-checks/chunk-output.mjs` (`splitLines`/`planChunks`/`renderChunk`/`renderIndex`/`chunkOutput`; line-ranges preserved; stale parts pruned) + 12 tests. **W2** NEW `.agents/rules/durable-memory.md` (two-tier `.remember/now.md` + memory MCP; 7-value `entityType` vocabulary; query-before-read) + `.remember/now.md` normalised; real MCP defect documented — `search_nodes` is a LITERAL contiguous substring match over the whole query (not tokenised/ranked/fuzzy: `"Injected context budget doc-size"`→0 but `"Injected context budget"`→1), so an empty result ≠ no memory. **W3** `compaction.reserved` 10000 → **30000** (user-approved). **W4** NEW `.agents/agents/orchestrator.md` + `.agents/commands/orchestrate.md` (`mode: "primary"` NOT subagent — subagents can't dispatch subagents; `tools` omitted to inherit) + `.agents/RULES.md` §0 parallel-dispatch rule; **agents=16 (primary 3: build/plan/orchestrator, subagents 13), commands=11**; Tier A parallel / Tier B chunked-sequential. **W5+W6** NEW `.agents/agents/orchestrator-health.md` (per-kind time budgets, liveness checks at 50%/100%, **max 1 retry**, taxonomy `provider-blocked\|timeout\|stalled\|error\|completed`) + handoff `SCHEMA.md` **v1.1** (§8 Subagent Status, §9 Handoff Summary, additive-only BC clause) + `agent-to-agent.md` Tier Downgrade Path + `latest.md` rewritten. **W7** NEW `scripts/dev-checks/check-tsc-baseline.mjs` (total vs prod classification, regression → exit 1, `--json`, `--update`, exit 2 when tsc never ran; documented test seam `TSC_BASELINE_CMD`) + NEW `scripts/dev-checks/tsc-baseline.json` **`{total:46,prod:0,recorded:"2026-09-18"}`** + `check-doc-sizes.mjs` gains `checkInjectedBudget()`/`--json`/`.context/out` retention warn (advisory, never fails) + `.githooks/pre-commit` §8 advisory + `.github/workflows/quality-gate.yml` NEW `context-budget` + `tsc-baseline` jobs and the Build step now a Turbopack warning gate. **Phase 7** 23 tests (`check-tsc-baseline.test.ts` 9 incl. the 1-prod+2-test classification guard; `check-doc-sizes.test.ts` 14 incl. real-repo read-only + harness failure modes + wiring). **Self-inflicted bug caught by the tests (Lesson 120)**: the helper hardcoded the repo script path, so "harness" tests ran the REAL script → the `--update` case **overwrote the committed `tsc-baseline.json`** with `{total:2,prod:1}`; fixed by `run(script, …)` + `afterAll` byte-identity guards on the protected files; recovered via `git checkout`. Verified: `tsc` **46 = exact baseline (prod 0)**, `check-tsc-baseline` OK exit 0, `quickbuild` **0 Turbopack warnings / 185/185 pages**, doc budget **73.0 KB / 100 KB**, new suites **23/23**. **Subagents unusable here (D3/D11)** — every `task` type fails with the free-tier provider error → capability-detected Tier B is the live path. No migration; no new packages (Node built-ins only). **Pre-merge follow-up (2026-09-18) — lint gate restored**: `npm run lint` was broken repo-wide on Next 16 (`next lint` removed → bad directory) and `eslint .` crashed in the legacy `FlatCompat` bridge (`@eslint/eslintrc` circular-structure `TypeError`); fix = `"lint": "eslint ."` + `eslint.config.mjs` rewritten to the native `eslint-config-next` flat exports (plugins registered per config object) + 7 real findings fixed (3 `react-hooks/rules-of-hooks` — `ingest-csv` effects hoisted above the admin guard, `fetchHistory` hoisted too (TDZ), `GoogleAnalytics` unconditional effect; 2 `<a>`→`<Link>`; 2 test `no-this-alias` → arrow) + legacy debt documented as `warn` (`no-require-imports` ×88, `react/no-unescaped-entities` ×21, react-hooks v6 compiler rules ×14) → **0 errors / 1139 warnings exit 0**; re-verified tsc 46/0, jest 92/92, quickbuild 185/185 + 0 warns, doc budget 73.8 KB. Detail: `.agents/changelog/versions-v3.40.md` §Pre-merge follow-up (lint), §CI follow-up (`.remember/` — Lesson 124), §CI run 2 (warning gate — Lesson 125), §CI run 3 (CodeQL `js/incomplete-sanitization` in `chunk-output.mjs` — Lesson 126; branch cleanup: v3.39.4/SQLite branches deleted — commits carried by PR #127 / `main`). **+ v3.40.1 — Production recovery serving fix: SQLite-mirror deferred Blobs restore + Netlify runtime detection + health telemetry** (branch `fix/production-analytics-rec-serve`; **MERGED via PR #128 (merge `15fa0a3`); `main` tip `e183a3a`**): user finding — production cold start served empty analytics; the boot `getMirrorBlobsStore()` failure was **memoized as null forever** (transient store-resolution throw → permanent null memo → the v3.39.0 disk→Blobs→fresh restore never reached Blobs on cold instances). FIX 1 (`lib/sqlite.ts` +179/−9): `getMirrorBlobsStore()` failure now records `__sqliteMirrorBlobsStoreFailedAt` and re-attempts after `MIRROR_BLOBS_STORE_NEGATIVE_TTL_MS = 60_000`. FIX 2: NEW one-shot deferred restore — `scheduleDeferredMirrorRestore()`/`cancelDeferredMirrorRestore()`/`retryDeferredMirrorRestore()`; boot with NO disk snapshot (`initSqliteBackup` after "SQLite backup initialized", `fromSnapshot === false`) schedules a `.unref()`'d `setTimeout(MIRROR_DEFERRED_RESTORE_DELAY_MS = 30_000)`; retry CLEARS `__sqliteMirrorBlobsStoreFailedAt = 0` BEFORE downloading (else the 60s negative TTL blocks the 30s retry — Lesson 128). FIX 3: swap guard `mirrorHasLiveData()` (counts `market_cache`/`corporate_action`/`daily_recommendation_run`; empty/throw → false) → close old db → swap `state.db`/`_instance` → column ensures → `persistMirrorSnapshot()` + `uploadMirrorSnapshotToBlobs()`. FIX 4: NEW `lib/netlify.ts` — `isNetlifyRuntime()` detects Netlify WITHOUT the `NETLIFY` env (prod has only `AWS_REGION=us-east-2` + `ENVIRONMENT=production`): `NETLIFY`==="true" | `NETLIFY_BLOBS_REGION` | `NETLIFY_BLOBS_CONTEXT` | `NETLIFY_SITE_ID` | `NETLIFY_DEPLOY_ID` | `NETLIFY_AUTH_TOKEN` | `ENVIRONMENT==="production" && AWS_REGION` | `globalThis.netlifyBlobsContext`. FIX 5 (`app/api/health/route.ts`): `isNetlify` + `blobsContextAvailable` fields; `NETLIFY_BLOBS_REGION` in `SAFE_VARS` (deliberately NOT `NETLIFY_BLOBS_CONTEXT` — may carry tokens). Tests: NEW `lib/__tests__/netlify.test.ts` **11/11** (8 detection matrix + 3 context); `sqliteMirror.test.ts` **8/8** (2 golden v3.40.1 — deferred retry recovers empty mirror / skips when live data exists; skip asserts `blobs.getCalls.length === getsBeforeRetry` get-count delta NOT `.some(arrayBuffer)` — boot 1's restore already probes Blobs, Lesson 128d); `sqlite.test.ts`+`dbOpTiering.test.ts` 97/97 (incl. :2461 disk-only path). Full suite **93/93 / 1283 pass / 4 skip / 0 fail exit 0**; `tsc` **46 = exact baseline (prod 0)**; `quickbuild` clean **185/185**. Pre-existing Jest teardown noise ("Cannot log after tests are done") PROVEN pre-existing via isolated `backtestDataService.test.ts` 16/16 in 1.6 s (fire-and-forget boot calls racing teardown; the new unref'd one-shot timer is not a cause). Blobs store `tradenext-sqlite-mirror` seeded (14,716,928 B, ETag `4bc5a64f…`, sha256 round-trip verified). No migration; no new packages. **+ v3.40.2 — Mirror-contract fixes for BUGS 15/16/17** (branch `fix/mirror-contract-fixes`; **MERGED via PR #129 (merge `38ee7da`); `main` tip `27c0770`**): bug 16 — mirror rows are a SECOND contract: NEW `lib/services/corpActionMirror.ts` `mapMirrorCorporateAction()` (tolerant snake→camel, idempotent, never throws) applied to BOTH mirror branches of `/api/corporate-actions/combined` (`sqlite_mirror` + `sqlite_backup`) + `app/markets/calendar/page.tsx` local `toDayKey()` (22-Sep IST ex-date keyed `2026-09-21` via `toISOString` → now `2026-09-22`); bug 15 — `/alerts` blank page: `/api/alerts` list + `action=count` session-scoped mirror fallback (`getMirrorAlerts`) behind `isDbUnavailableError` + page `Array.isArray`-guard + error state with Retry (Lesson 130); bug 17-high — `/api/admin/workers/status` GET+POST now require admin session (POST was publicly writable; 401) + GET mirror fallback `mapMirrorWorkerStatus()` + 5-min `filterWorkers()` (`?includeOffline=true`); `/admin/utils/workers` poll → self-scheduling backoff (10→20→40→60 s cap) + `pollPaused` hint; `dividendCalendarService.fetchDividends` mirrors (`fetchMirrorDividends`) instead of reporting 0. Tests: 4 NEW suites **26/26**; full **97/97 suites, 1309 pass / 4 skip / 0 fail**; `tsc` **46 = exact baseline**; lint **0 errors**; doc budget **76,669 / 102,400 B**; live :3000 `/alerts` + `/markets/calendar` + `/admin/utils/workers` verified 0 console errors. Out of scope (row 17 follow-up, BUGS.md): `/api/admin/users`, `/api/admin/monitoring`, `/api/admin/workers` list, `/api/admin/cron`, `/api/screener/saved`. Lessons **129/130/131**; openapi `/api/admin/workers/status` entry. No migration; no new packages. **+ v3.40.3 — Predeploy mirror-preservation guard: Netlify build-time SQLite snapshot + versioned Blobs backup + SQLite→Prisma drain before every production deploy (Spec 14)** (branch `feature/predeploy-mirror-preserve` on `main` tip `27c0770`; **commit/push/PR/deploy PENDING USER**): `netlify.toml` `[build] command` now starts `node scripts/predeploy/preserve-mirror.mjs && npx prisma generate && npm run quickbuild`; script = self-gated (`CONTEXT === "production"` OR `--force`, skip paths warn + **exit 0** soft-fail), 20s timeout, calls **the live instance** `POST /api/admin/predeploy/preserve` (URL chain `DEPLOY_PRIME_URL`→`URL`→`NEXT_PUBLIC_BASE_URL`→fallback; token NEVER logged; one summary line). Route (NEW, `runtime="nodejs"` + `dynamic="force-dynamic"`, openapi tags `Admin - Deploy`): ordering `getOutboxPending()` → snapshot (`persistMirrorSnapshot`+`exportSqliteBackup`+`uploadMirrorSnapshotToBlobs`) → `createMirrorBackup(bytes)` → `if (!isPlanLimitBreakerOpen())` push (`reason:"deploy"`, leaderGate false) → audit `ADMIN_DB_SYNC`/`predeploy-preserve`; modes **`pushed|backed_up|skipped`** (P6003 hold → `backed_up` by design); push throw → 500 `push_failed`; GET = read-only diagnostics (never pushes). Auth: `x-deploy-guard-token` header vs `DEPLOY_GUARD_TOKEN` env (64-hex, length-checked `timingSafeEqual`; 503 `guard_token_not_configured` when unset+presented) + admin-session fallback (`authorize`). NEW `lib/services/mirrorBackup.ts` (versioned `backups/sqlite-mirror-<ISO>.sqlite`, `MIRROR_BACKUP_KEEP=5`, `MIRROR_BACKUP_MAX_BYTES=200 MiB`, fail-open prune) + `lib/sqlite.ts` Phase 1 (`SyncTrigger "deploy"`, `MirrorBlobsStoreLike` widened, `getMirrorBlobsStore` exported). Tests **25 new** (mirrorBackup 14 + predeployPreserveRoute 11; single `jest.mock("@/lib/sqlite")` isolation); full **99/99 suites, 1334 pass / 4 skip / 0 fail**; `tsc` **46 = exact baseline**; lint **0 errors** (4 new TS files clean); doc budget **74.9/100 KB**; quickbuild **185/185**; `DEPLOY_GUARD_TOKEN` upserted via Netlify MCP (production, builds+runtime, site `78401e5d-b137-4b6d-94bb-ad1ec8de6b05`; temp file deleted — never echoed). Legacy finding (record-only): `.github/workflows/deploy.yml` publishes `./out` via `nwtgck/actions-netlify@v3.0` — NOT the real deploy path (netlify.toml → quickbuild → `.next`). Lessons **132** (2-digit ISO days — `padStart(2,"0")`, `new Date("2026-09-1T…")` = Invalid Date → `createMirrorBackup` silently null) / **133** (build-time self-call order + versioned backups + soft-fail exit 0). No migration; no new packages (Node built-ins + `crypto`). |
| [versions-v3.39.md](./changelog/versions-v3.39.md) | **v3.39.4** — **Context-optimisation — slim injected instruction files (358 KB → 72 KB/session) + modular read-on-demand docs + Turbopack tracing fixes → 0 warnings** (branch `fix/turbopack-tracing-harness`; commit pending user): root cause of the compaction loop = `.opencode/opencode.json` → `instructions` injects the FULL contents of 5 files every request ≈ 358 KB ≈ 90K tokens, re-injected instantly after each compaction (with `compaction.reserved` only 10000). Slimmed via NEW `.context/slim-docs.mjs` (marker-based, EOL-preserving): `AGENTS.md` 191,017 → **20,260 B** (version history moved to `.agents/changelog/versions-index.md`), `TODO.md` 134,054 → **20,792 B** (Quick Reference moved to `.agents/changelog/todo-quick-reference-archive.md`); injected total **72.2 KB**. NEW `.agents/INDEX.md` topic→file manifest. NEW `.agents/session-archive/`: `Primer.md` 1159 → 213 lines (old version log + Sessions 1–23 → `primer-history-archive.md`), `agent-memory.md` 1092 → 373 lines (→ `agent-memory-archive.md`); `Lessons.md` NOT age-chunked (live rulebook — old entries remain active rules). NEW context-budget rule (`.agents/rules/session-memory-rules.md` §7) + NEW `scripts/dev-checks/check-doc-sizes.mjs` (parses `instructions`; 100 KB total / 32 KB per-file). Turbopack: all 33 `/*turbopackIgnore: true*/` sites across `lib/logger.ts` (9), `lib/services/ingestService.ts` (2), `lib/services/worker/worker-logger.ts` (14), `lib/sqlite.ts` (8) → `npm run quickbuild` **0 Turbopack warnings**, 185/185 pages; `npx tsc --noEmit` **46 = exact baseline**. No migration; no new packages. |
| [versions-v3.39.md](./changelog/versions-v3.39.md) | **v3.39.0** — **SQLite Durable Mirror — disk snapshot + Netlify Blobs upload (restart survival, Spec 12)** (branch `feat/sqlite-durable-mirror`, on top of the unreleased v3.39.x base fixes `2182687` toIsoVal + `f470e6d` set_ops_counter queryConsumption + PR #123 `@netlify/blobs` re-add `e84fb34` + PRs #124/#125; committed `e4623a3` feat + `e9db4a7` docs; push/PR/deploy PENDING USER): user directive — Prisma plan-limit hold until **2026-10-02**; the in-memory mirror + breaker-gated boot `syncFromPrisma()` lost all SQLite-served hot-route data on EVERY restart → the mirror must survive warm/instance restarts AND Netlify cold starts with **zero additional Prisma ops**. `lib/sqlite.ts` (+288/−14): `state.db.export()` → disk `logs/sqlite-mirror.sqlite` (NEW `getMirrorSnapshotPath()` 762 + `os.tmpdir()` fallback when `resolveLogsDir()` returns `''` — root-causes the repo-root stray artifact, `lib/logger.ts:5` jsdom `isServer` false); boot restore disk → Blobs → fresh + schema replay upgrades older snapshots; cadence = eager after each `syncFromPrisma()` + piggybacked on the EXISTING 60s `OPS_PERSIST_INTERVAL_MS` timer (1979/1987 — non-leader instances keep fresh mirrors, NO new timers); Blobs upload = dynamic `import("@netlify/blobs")`, store `tradenext-sqlite-mirror`, 200 MB cap, digest gate, memoized `getMirrorBlobsStore()` globalThis, fail-open never throws. Tests: `sqlite.test.ts` +239 (durable-mirror describe 10 + FakeBlobsStore) + NEW `sqliteMirror.test.ts` 6 golden REAL sql.js WASM (separate file — `sqlite.test.ts` hoists the `sql.js` mock); `.gitignore` +4. Full **89/89 / 1234 pass / 4 skip / 0 fail**; tsc 46 exact baseline; no migration; no new packages. + **v3.39.1** — **WCAG a11y contrast fixes + flat-change neutral state + index chart buttons open TradingView + mobile menu aria-label** (committed `309deac` on `feat/sqlite-durable-mirror`; 9 files +65/−32): `lib/charting.tsx` NEW `INDEX_TV_TICKERS` (NIFTY→`NSE-NIFTY`, BANKNIFTY→`NSE-BANKNIFTY`, NIFTYIT→`NSE-CNXIT`, SENSEX→`BSE-SENSEX`, FINNIFTY→`NSE-CNXFINANCE`) — index chart buttons open TradingView, stocks stay `charting.nseindia.com/?symbol=<SYM>-EQ`, unknown → `?symbol=<SYM>`, titles "View `<symbol>` chart on TradingView" / "View on NSE Charting"; `StockQuoteHeader.tsx` flat-0.00 neutral gray pill (no arrow); `NSEStockChart.tsx` NEW `INDICATOR_TEXT_CLASSES` (MA20 amber-700/300, MA50 violet-700/300, MA200 cyan-700/300 — WCAG ≥4.5:1) + "Price Chart" h3→h2; `Header.tsx` mobile-menu `aria-label`; contrast tweaks CorporateDataTabs/PiotroskiFScore/CompanyIntelligence/VerdictCard/UnifiedChart. Full **89/89 / 1234 pass / 4 skip / 0 fail**; tsc 46; no migration. |
| [versions-v3.38.md](./changelog/versions-v3.38.md) | **v3.38.0** — **Admin SET_OPS_COUNTER authority fix + db-health "Sync Operations Count" UI** (branch `fix/db-health-ops-count-manual-sync`; committed `d5645a9`; merge/deploy PENDING USER): user directive — the admin is the authority on actual Prisma-dashboard consumption, so the admin must be able to MANUALLY correct the ops-usage date + total used for plan display. NEW `setOpsMonthlyDay(day)` / `setOpsMonthlyTotal(total)` mutators on the `__opsMonthly` ledger (`lib/services/opsMonthly.ts` +42) + `readOpsLedger()` refresh + `resetOpsMonthlyForTests()` flush; `lib/audit.ts` +1 (`ADMIN_DB_SET_OPS_COUNTER`); db-health POST `set_ops_counter` action (`route.ts` +75): zod `setOpsCounterPayload` validates `action` + `set-day`/`set-val` TOGETHER (`{action}` only → 400), day parsed via `Date.parse`/`new Date(s)` fallback to `getIstDayKey()` + IST `YYYY-MM-DD` zod check, `setOpsMonthlyTotal` → `dbOpsCounter.total = round(abs(total))` with `.reset = false` (live counter NOT zeroed — display-only override until next fold), fail-open `.catch` → 500; db-health UI second card (`page.tsx` +110): local day/total inputs + Update/Reset + authority tooltip "affects the ops-usage UI display and monthly plan usage — NOT the live counter itself" + `warn('unsafe')`; `handleSetOpsCounter` → POST `set` / `reset`, `getCurrentDay` "server" → current day, callbacks refetch via existing `refreshFns`. Tests: `dbHealthRoute.test.ts` +69 (payload validation fns + POST set: `{action}` → 400, value/day variants OK, combined → 200 + audit, malformed → 400 + `"Invalid"` audit), `opsMonthly.test.ts` +51 (set day+fold, set total, reset — 10/10), `audit.test.ts` +3/−1. Full suite **88 / 1207 pass / 4 skip / 0 fail**; tsc **46 = exact baseline**; no migration; diff 7 files +349/−2. + **v3.38.1** — **Swing generatedAt push-sink NULL guard (Prisma 23502/23503) + AI-monitoring "Last hour" timeframe filter** (same branch on `d5645a9`; code+tests VERIFIED; committed `d35343e` (feature) + `36f2c9b` (docs); pushed; **PR #121 OPEN** — merge/deploy PENDING USER): pre-fix swing upsert bound `created_at` ONLY inside `payload` → SQLite mirror `generated_at` NULL → every ~6h `pushSqliteToPrisma` swing sink handed Prisma `swing_signals.create` `generatedAt: :undefined` (prop dropped) → 23502 NOT NULL + 23503 FK cascade → swing cards' target/stop blanked (23502 branch) or victory rows DESTROYED (23503); Prisma 23502 reset → ResolverFailed → `runScheduledOps` `.finally` recoverySync → `recordDbError` "Recent DB Errors". Fix 1 (`swingRecommendationService.ts` ~L1227): `generatedAt: new Date()` bound TOP-LEVEL (mirror `generated_at` populated). Fix 2 (`sqlitePushSinks.ts` ~L497): sink fallback `() => (row.generatedAt as string) ?? row.created_at` with `tbl.date(...)` cast — heals pre-fix rows, no more 23502/23503. Fix 3 (`ai-monitoring.ts` L331-340): `getAiCallsMerged` memory-ring cutoff filter (`cutoff = Date.now() - timeframeMinutes*60*1000`, skip `entry.timestamp < cutoff`) + passes `timeframeMinutes` to `getPersistedAiCalls` — "Last hour" finally applies to the merged view (only `getAiStatsMerged` re-filtered before). Tests: `ai-monitoring.test.ts` +86 (NEW timeframe-filter describe, writes asserted-only `getAiCallsMerged` path, `@/lib/sqlite` mock + `enqueueWriteBehind` — 5/5), `sqlite.test.ts` +30 (23502 regression: `params[3]` = created_at binding + `job-null-gen` row seeded), `swingRecommendationService.test.ts` +21 (top-level Date `generatedAt` + string in payload — 40/40). Full suite **88 / 1207 / 4 / 0**; tsc **46 = exact baseline**; no migration; diff 6 files +157/−3. Live :3000 verified AI-monitoring "Last hour" clean + 0 console errors (swing fixes verified via regression tests + code review). + **v3.38.2** — **Dependabot vulnerability-fix batch** (same branch on `36f2c9b`; next 16.3.1→16.3.5 + eslint-config-next 16.3.5 + @next/third-parties 16.2.0→16.3.5 + csv-parse 7.0.2 + morgan 1.12.1 + nodemailer 9.1.1; @netlify/blobs removed; NEW overrides mysql2 3.24.4 + js-yaml 4.3.2/3.15.2 + @humanfs/node 0.16.8 + fast-uri 3.1.6; npm ls clean; audit 0 critical / 3 high (deepmerge-ts 7.1.5 via @prisma/config — no 7.x patch, do NOT override); full 88/88 / 1207 / 4 / 0 exit 0; tsc 46 exact; no migration). Post-v3.38.2 follow-up fix committed `f470e6d` — `set_ops_counter` POST response now returns `queryConsumption` (db-health "Sync Operations Count" card refresh; 3 files +49/−1; detail at end of versions-v3.38.md). |
| [versions-v3.37.md](./changelog/versions-v3.37.md) | **v3.37.0** — **Worker engine monitoring fixes 1–5 (issue #119) — long-task busy heartbeat + dead worker-logs route + Scheduler Leadership card + cron-ledger running-guard + daemon leader cross-check** (branch `fix/workers-tasks-monitoring`, on top of PR #118 merge `10e2a00`; code+tests VERIFIED, commit/push pending user, merge/deploy PENDING USER): (1) **Long-task busy heartbeat** (`lib/services/worker/worker-engine.ts`) — `TASK_HEARTBEAT_MS = 240_000` per-task BUSY heartbeat (unref'd, cleared in task `finally`, also at the 60s async poll) refreshes Prisma `worker_status.lastHeartbeat` every 4 min so a long task can never be reaped mid-flight; root cause: 5-min liveness heartbeat wrote SQLite ONLY → Prisma `lastHeartbeat` stale ~10 min in → cross-instance reaper `worker-169.254.80.183` killed the healthy `run_daily_recommendations` worker mid-batch; `TASK_TIMEOUT_MS` 40→**240 min** last-resort only. (2) **Dead worker-logs route** — NEW `app/api/admin/workers/logs/route.ts` (GET list `{files}`, GET `?taskId=` `{content}` with `MAX_TASK_ID_LEN = 128`, DELETE `?taskId=` `{deleted}`; `auth()+admin();` — route never existed since v3.14.0 → Logs tab 404'd). (3) **Scheduler Leadership card** (`app/admin/utils/workers/page.tsx`) — `leader-*` rows surfaced as role + id + holding = `lastHeartbeat < 10 min`, filtered OUT of the Active Workers grid. (4) **Cron-ledger running-guard** (`worker-service.ts` L148-167) — `recordSystemRunOutcome` EXPORTED + only `task?.cronJobId && task.status === "running"` records; timeout/reap paths write the ledger BEFORE the failed-status write; try/catch → `logger.warn` non-fatal. (5) **Daemon leader cross-check** (`app/api/admin/cron/daemon/route.ts`) — daemon ALSO refreshes shared `leader-cron-daemon`; route cross-checks BOTH rows via `Promise.all` + `leaderFresh` within `LEADER_STALENESS_MS` (10 min). Tests: `worker-service.test.ts` **7/7** (new) + `cronDaemonRoute.test.ts` **4/4** (new) + `worker-engine.test.ts` **23/23** (+254); full **88/88 suites / 1192 pass / 4 skip / 0 fail**; tsc **46 = exact baseline (0 new)**; no migration; no new packages; diff 6 files +407/−53 + 2 new tests. Live :3000: Workers Active grid + Logs tab file list + Scheduler Leadership 3/3 holding on `LAPTOP-HM25SVAR-19820` + cron chip "Scheduler Live" heartbeat ~102s + daemon route 200 `running:true`; route `registeredJobs` "0 job(s)" = documented Turbopack dev module-graph artifact (`localhost.cron_job` HAS 4 active rows; prod `next start` shows 4); 0 console errors. Commit/push pending user. |
| [versions-v3.36.md](./changelog/versions-v3.36.md) | **v3.36.0** — **Swing steady-state serve fix — done/failed analysis jobs served INDEFINITELY; `force=1`-only regeneration with performance kick** (branch `fix/workers-tasks-monitoring`, on top of PR #118 merge `10e2a00`; code+tests VERIFIED, commit/push pending user, merge/deploy PENDING USER): plain `/api/recommendations/swing` loads were re-running the full screener+AI pipeline at steady state (after a done/in-flight job existed) — burning NSE requests + OpenRouter tokens with no new data. Fix (`lib/services/swingRecommendationService.ts` serve path rewrite ~L988-1135): existing **done/failed result served INDEFINITELY** (no age gate, no auto-regen on plain load); `force=1` is the ONLY regeneration trigger (supersedes pending/running AND kicks `checkSwingPerformance()` for the superseded job); new liveness semantics `staleBefore = Date.now() - SWING_JOB_STALE_MS` (anchor = `pending→createdAt`, `running→startedAt`, else `NaN`); **newest-TERMINAL fallback** when the newest job is fresh in-flight; **Prisma fallback** when the mirror is empty (serve done without re-scan); **durable pending job on plain first load** (mirror+Prisma both empty — no in-memory-only run). Tests: swing **57/57** (5 new: old-done-served-indefinitely, newest-done-behind-stale-pending, Prisma-fallback-serve-without-rescan, durable-pending-on-first-load, force-perf-kick); tsc **46 = exact baseline (0 new)**; no migration; diff 2 files +266/−12. Live :3000 (5/5 points): first load durable `7d841b4e…` + 11 in-flight polls ZERO re-scans; done at 17:18:05; plain load after done → memory cache, no `Swing run starting`; force=1 → new durable job + `Swing performance check starting`; console 64 msgs 0 errors. Commit/push pending user. |
| [versions-v3.35.md](./changelog/versions-v3.35.md) | **v3.35.0** — **Flaky `intelligence.test.ts` CI fix — prisma mock isolates the fire-and-forget `IntelligenceCache` upsert (zero real-DB writes in tests)** — un-awaited `upsert` (`cache.ts` :101-124) raced the `beforeEach` `deleteMany` teardown → stale row → spurious `INTELLIGENCE_CACHE_HIT` flaky fails at :187/:246/:279; ALL-FIXED with a test-only +21 prisma mock factory in `lib/__tests__/intelligence.test.ts`; targeted **13/13**, full **86/86 suites / 1170 pass / 4 skip / 0 fail** — FIRST fully-green full run; tsc **46 = exact baseline (0 new)**; on PR #118 branch on top of v3.34.1 merge `05b91e8`; committed `2036724` + pushed; PR #118 merge/deploy pending user. |
| [versions-v3.34.md](./changelog/versions-v3.34.md) | **v3.34.0** — **Monthly Query Consumption — db-health monthly-ops window (deferred Plan 09 rev-v3 c/d)** (on `feat/db-health-monthly-ops`, merged into PR #118 branch `fix/leader-watchdog-self-heal`): user directive (v3.31.0) "plan limit is now MONTHLY 200K ops/mo (resetting 2nd)" + Prisma calls only at 3 moments — the db-health panel only showed TODAY's ops; with a monthly plan that is the wrong unit and a restarted instance loses the month's history. NEW pure `lib/services/opsMonthly.ts` ledger (`OpsMonthlyState {monthKey, days}` on globalThis `__opsMonthly`; monthKey = `getIstDayKey().slice(0,7)`; lazy seed + fresh ledger at rollover; `foldOpsCounterIntoMonthly()` idempotent `Math.max` high-water merge; `buildQueryConsumption()` no-double-count aggregation, perDay newest-first ≤ 31, `DB_PLAN_LIMIT_OPS_MONTHLY` default 200_000; NEVER invoked at module load); `lib/sqlite.ts` persist/restore under `_backup_meta` `"ops_monthly"` (:1653 — restore :1421, persist :1440, 60s tick :1705) discarding stale previous-month snapshots; db-health GET `queryConsumption` (+12) + "Monthly Query Consumption" card (+77) + `.env.example` +5; no hot-path change (`lib/prisma.ts` = 6-line comment); test-trap Lesson #114; sqlite 83/83, targeted 94/94, tsc 46 baseline, no migration, diff 7 files +310/−2 + 2 new; `query_cache` deferred. + **v3.34.1** — **sql.js WASM async-load gate fix (commit `d91fb01`)** — `getSqlJs()` async `fs.readFile`/stream WASM load left "Cannot log after tests are done" noise; fix = synchronous `wasmBinary` load (`fs.readFileSync` try/catch per candidate path) + memoized `getSqlJs()`; sqlite + cron-daemon **88/88** under `CI=true --runInBand` zero noise; full **1154 pass / 4 skip / 1 fail** (pre-existing flake); tsc 46 baseline; no migration. |
| [versions-v3.33.md](./changelog/versions-v3.33.md) | **v3.33.0** — **Leader Watchdog Self-Heal — a dead scheduler must never happen again** (branch `fix/leader-watchdog-self-heal`, on top of committed v3.32.1 `bcde7ae`; own commit by workstream, commit plan user-approved, push/merge/deploy pending): user directive "don't let this happen again" — prod scheduler dead since ~2026-09-08 07:06 UTC, recovery was a manual admin "Start Engine" click. Root cause: leadership ONE-SHOT (`acquireLeaderLock` at boot + `startLeaderHeartbeat(role, onLost)`; v3.28.2 stops engines on `onLost` but nothing re-acquires → standby pollers forever). NEW `watchLeaderRole(role, handlers)` self-healing loop (`lib/services/leader.ts`): standby → adaptive probe (fresh foreign row = SLOW re-probe `LEADER_CLAIM_SLOW_MS` 300s; stale/absent = claim via `updateMany` count>0 | `create` | P2002→false | `isDbUnavailableError`→fail-open) → re-probe OWN row (`failOpenEvents++`) → leader + 300s heartbeat (`LEADER_HEARTBEAT_MS`); renewal 0 → internal `onLost` → standby + `handlers.onLost` + FAST re-probe (`LEADER_CLAIM_FAST_MS` 60s); `stop()`; phase-guard (no double `onAcquired`); steady-state = 1 `findUnique`/300s/instance. `LEADER_STALENESS_MS` 15→**10 min** (human-approved); NEW `LeaderWatchStatus`/`getLeaderWatchStatuses()` (globalThis `__leaderWatchStatus`, zero-Prisma). Wiring (`instrumentation.ts`): worker/cron-daemon `onAcquired`/`onLost` (idempotent starts), sqlite-sync log-only. Diagnostics: db-health route 7 leader imports :8, GET leader block :207–225, POST :242; page `leaderWatch`/`leaderTuning` (no server-only import). NEW tests `leaderWatch.test.ts` (8) + `instrumentation.test.ts` (7 rewritten) + `dbHealthRoute.test.ts` +1 + `cron-daemon.test.ts` +1; constant fixes `leader.test.ts:68`/`sqlite.test.ts:282`; targeted **125/125**; full **1154 pass / 4 skip / 1 fail** (1 = documented pre-existing `intelligence.test.ts` flake); tsc **46 = exact baseline (0 new)**; no migration; no new packages. Spec `.agents/specs/11-scheduler-self-heal.md` + plan `.agents/plans/11-scheduler-self-heal.md` committed with commit 1 · **v3.33.1** — **Swing performance touch-tracking fix — intraday HIGH/LOW touches count as target/stop hits** (own commit by workstream): `checkSwingPerformance` used latest CLOSE only → intraday touches that closed back inside range never counted; NEW `maxHighSincePosting`/`minLowSincePosting` (close-only preserved), ONE `$queryRaw` `daily_prices` window (`ticker = ANY` + `"tradeDate" >= MIN(postedAt)`, per-signal JS filter), live-quote bridge `dayHigh`/`dayLow`, BUY touch target-wins, reason strings + audit metadata; `swingPerformanceService.test.ts` **27/27**; full **1154 pass / 4 skip / 1 fail**; tsc **46 = exact baseline (0 new)**; no migration |
| [versions-v3.32.md](./changelog/versions-v3.32.md) | **v3.32.0** — **Admin Time Synchronisation — on-demand Postgres NOW() probe + persisted server-clock offset** (✅ MERGED to `main` via PR #117 `38a27bf`; `e74ae54` feat · `df7959d` docs · `b75deb0` docs update) + **v3.32.1** — **db-health POST double-`req.json()` hotfix — body parsed ONCE + reused (restore + set_time_correction 400s fixed; NEW `lib/__tests__/dbHealthRoute.test.ts` 5/5; live-verified Save Correction/Clear on :3000; working tree on `main`, commit pending user): user directive "in admin db health screen display server time and db time. and also admin can enter ist time so if server time is misaligned it can be corrected through admin entered input for timezone corrections." **Root cause**: Netlify host clock treats IST wall-clock as UTC → stored cron `nextRun` skew ≈ +5.5h; `calculateNextRun` (UTC-correct) takes `from` from the server clock. **(1) DB probe + persistence** (`lib/sqlite.ts`) — NEW `probeDbTimeNow()` (`SELECT NOW()` via `$queryRawUnsafe`, 10s timeout, `IST_OFFSET_MINUTES = 330`, `TIME_ALIGN_TOLERANCE_MS = 60_000`) + `persistTimeCorrection`/`deleteTimeCorrection`/`restoreTimeCorrection`/`persistTimeProbe`/`restoreTimeProbe` (`_backup_meta` keys `time_correction`/`time_probe_db`; types `:1753-1789`, interface `:247-257`, fallback `:5978-5983`). **(2) Correction engine** — NEW `lib/services/timeCorrection.ts` (`offsetMinutes = trueNow − serverNow`; `applyOffset` identity@0; `getCorrectedNow()`/`getCronFrom()` lazy read-through, no cache; `getTimeDiagnostics()`); **REAL BUG FIXED**: `parseIstDateTimeLocal` round-trip guard compared parsed-UTC vs IST input → now `toIstIso(parsed).slice(0,16) === input` (epoch = `Date.UTC(y,m-1,d,h,min) − offset`); header documents node-cron OS-bound limitation + durable `TZ`/`UTC` env fix on Netlify. **(3) Device API/UI** — POST `probe_time` (`route.ts:395-425`, 30s throttle → 200-throttled; PG-down → 200 `available:false`; audit `ADMIN_DB_SYNC`/`time-probe`), `set_time_correction`/`clear_time_correction`; GET zero-Prisma `time` block; **Time Synchronisation card** (`page.tsx:247/505-515/1540`); `lib/audit.ts` +`ADMIN_DB_TIME_CORRECTION_SET`/`ADMIN_DB_TIME_CORRECTION_CLEARED`. **(4) Scheduling wiring** — `getCronFrom()` (`recommendationCronService.ts` + `worker-engine.ts` nextRun sites `:598`/`:631`), `getCorrectedNow()` at due-claim `:645`/`:657`. **(5) Tests** — NEW `timeCorrection.test.ts` **20/20** (full manual `@/lib/sqlite` mock — no `...actual` spread, no `process.env.TZ`); sqlite **71/71**; targeted **118/118**; full **1106 pass / 4 skip / 1 fail** (1 = documented pre-existing `intelligence.test.ts` flake); tsc **46 = exact baseline (0 new)**; no migration; no new packages. **Deferred**: live `probe_time` DB check (local Postgres not running); durable fix = correct `TZ`/`UTC` env on Netlify. | Sep 10 2026 |
| [versions-v3.31.md](./changelog/versions-v3.31.md) | **v3.31.0** — **SQLite-first NSE read architecture + low-frequency Prisma sync (Plan 09)** (branch `fix/v3.29.1-header-watchlist`, on top of committed v3.30.0; code committed `9303bd7`→`653b617`, 9 commits; doc commit pending user): user directive "Plan limit is now MONTHLY 200K ops/mo (resetting 2nd)" + "Prisma calls ALLOWED ONLY at 3 moments: boot hydration, 6h SQLite→Prisma push, ONE hourly ops-usage write — zero between". **(1) sync_history ledger** (`9303bd7`) — durable `sync_history` table (`direction` `prisma_to_sqlite`|`sqlite_to_prisma`, `trigger`, `leaderGated`, `rowsSynced`, `durationMs`, `error?`) + `recordSyncHistory()` (prune-100) + `recentSyncs` in health; also fixed the SCHEMA_SQL stray-`;`-in-`--`-comment sql.js parse error (root cause of v3.30.0 boot noise — comments now `/* */`). **(2) Boot hydration on every instance** (`d9bda6b`) — `syncFromPrisma` opts (`reason` `boot|probe|admin`, `skipReconcile`, `leaderBypass`, `force`); `initSqliteBackup()` → `{reason:"boot", skipReconcile:true, leaderBypass:true}` (every instance pulls; reconcile only on probe/admin/force). **(3) NSE rate guard** (`56ee538`) — NEW `lib/services/nseRateGuard.ts` (`withSingleFlight` per method+path, `withThrottle` min-interval, burst cooldown ≥5 × 403/419/429 in 60s → 60s fail-fast) + NEW 6-test suite; wired `nse-client.ts` + `market-cache.ts`. **(4) `_sync_outbox` + 6h PUSH engine** (`a681a48`) — NEW `_sync_outbox` table + index; `recordSyncOutbox`/`getOutboxPending`/`pushSqliteToPrisma` (grouped latest-op-wins, chunk-200 sinks, awaits leader gate, `reconcileControlToPrisma` after); NEW `lib/sqlitePushSinks.ts` (per-table payload mappers, `daily_price`/`corporate_action` raw upsert SQL); 6h probe pivots PULL→PUSH. **Production bug found here**: `if (!isLeader("sqlite-sync"))` never fired — `isLeader()` returns a `Promise` (always truthy) → fixed to awaited. **(5) NSE captures SQLite+outbox only, auto-promote off** (`f7e56b5`) — removed `startNsePromoteFlush` call + all `flushNseToPrisma()` call sites; env-gated `NSE_PROMOTE_ENABLED=1`. **(6) Jobs write SQLite-first (recs/swing/perf)** (`ae44431`+`63736f5`+`0013dae`) — mirror schema parity (5 new tables) + 7 write-through helpers + pipelines swapped to mirror-first with Prisma fallback. **(7) Admin long-lived datasets SQLite-first** (`a2bffd9`) — +7 `SqliteFallback` helpers (`admin_announcement`/`alert`/`transaction`/`corporate_action`) + 4 admin CRUD routes flipped mirror-first. **(8) db-health wiring + push action + derived counts + hot-route headers** (`653b617`) — GET spreads `outboxPending`/`derivedCounts`/sync-history; POST `"push_to_prisma"` action (`leaderGate:false`); db-health UI Outbox + Derived-counts cards + "Push to Prisma" button + Direction/Trigger columns; 5 hot-route header comments document the SQLite-first read chain. **Tests**: sqlite.test.ts **68/68** (11 new P8); instrumentation +1; 2 pipeline suites re-pointed; NEW nseRateGuard 6; full **1105 pass / 4 skip / 1 fail** (1 = documented pre-existing `intelligence.test.ts` flake); tsc **46 = exact baseline (0 new)**. **Deferred (plan rev-v3 c/d)**: `query_cache` + db-health monthly-ops window NOT implemented. No schema change → no migration. |
| [versions-v3.30.md](./changelog/versions-v3.30.md) | **v3.30.0** — **Daemon control-plane cadence + SQLite mirror touch-freshness + `upsertCronJob` Date-binding fix + Netlify WASM staging** (branch `fix/v3.29.1-header-watchlist`, on top of committed v3.29.2 `1e907f1`; code committed `8af65cc`, Date-bind fix + docs in the v3.30.0 doc commit): user directive "i don't want to see this every time — defaults in `.env` on Netlify" — the recurring db-health noise traced to root causes fixed here. **(1) Cadence** — `leader.ts` `LEADER_STALENESS_MS` 5min→**15min** + `LEADER_HEARTBEAT_MS` 60s→**300s** (5-min staleness let a missed heartbeat hand the lock to a cold-start standby → repeated re-elections); `cron-daemon.ts` NEW `SWING_DRAIN_INTERVAL_MS = 900s` (swing drain off the fast 60s resync tick; drained queue stopped re-checking every tick); `worker-engine.ts` `REAP_INTERVAL_MS` 60s→**300s**. **(2) Mirror freshness** — NEW `SqliteFallback.touchControlMirror(table)` re-marks `control_write_at:<table>` = NOW (non-empty gate, best-effort never throws); `discoverPendingTask()` calls it on the ready-DB early-return so an idle system's mirror freshness tracks poll cadence, not task activity (no more silent Prisma fallback). **(3) Schema-init root cause** — SCHEMA_SQL stray `;` inside `--` comments → sql.js `near "Prisma": syntax error` on EVERY boot → SQLite never ready → ALL SQLite-first reads silently fell back to Prisma (the recurring db-health noise). **(4) Netlify WASM staging** — NEW `scripts/copy-sql-wasm-netlify.mjs` copies `public/sql-wasm.wasm` → `.next/sql-wasm.wasm` post-build (Netlify publish dir = `.next`; wired into quickbuild/build; non-fatal when missing). **(5) `upsertCronJob` Date-binding fix** (`lib/sqlite.ts`) — Prisma `CronJob` rows carry real `Date` instances; raw bind → locale string (`String(Date)`) breaks the ISO read-back `new Date(String(col))` in `reconcileControlToPrisma` (12h reconcile could write corrupt `nextRun`/`lastRun`); NEW `toIso(v)` applied to lastRun/nextRun/createdAt binds. Same latent pattern in `upsertWorkerTask`/`upsertWorkerStatus` noted, left untouched. **Tests**: sqlite mock INSERT regex `/INSERT(?:\s+OR\s+REPLACE)?\s+INTO\s+(\w+)/i` + OR REPLACE/ON CONFLICT remove-then-push; NEW Date-binding describe (2: ISO not locale — fails pre-fix; re-upsert same id replaces); daemon-sqlite-first +2 (touch); leader constants; targeted **96/96**; tsc **46 = exact baseline (0 new)**; no migration. Commits `8af65cc` (Phases 1–4) + doc commit (Phase 5 + docs); no push/merge. |
| [versions-v3.29.md](./changelog/versions-v3.29.md) | **v3.29.2** — **Admin AI model management: true built-ins permanently locked (`openrouter/free` + `openrouter/auto`), catalog + custom models stay removable + "Model Library" UI + 3-optgroup select** (branch `fix/v3.29.1-header-watchlist`, on top of committed v3.29.1 `6e8db23`; commit pending user): user directive "a built-in model cannot be removed" — built-ins are the `AI_FALLBACK_MODELS` foundation re-selected whenever the active model is removed/becomes unviable. **API** (`app/api/admin/ai/config/route.ts`): DELETE zod-validates `modelId` (NOT `id` — `{id}` → 400 "modelId is required"); **builtin → 400 "Cannot remove built-in models"** (nothing persisted); unknown → 404; catalog → hidden; custom → removed; ACTIVE-model removal resets `ai_config.model` → `DEFAULT_MODEL` (`openrouter/free`) + `resetLLM()`. `custom-models/route.ts`: add/remove + adding a hidden catalog id **restores via the catalog path** (CATALOG badge, no custom-collection dup). **Constants** (`lib/services/ai/config.ts`): NEW `BUILTIN_MODELS` + `BUILTIN_MODEL_IDS`; `AVAILABLE_MODELS` (8) contains no built-ins; sync-guard test ties `AI_FALLBACK_MODELS` ⊆ `BUILTIN_MODEL_IDS`. **UI** (`app/admin/ai/page.tsx`): Custom-model section → **"Model Library"** heading + locked-note subtitle; active-model select → **3 optgroups (Built-in / Available / Custom)**; per-model Remove list = catalog ∪ custom **minus builtins**. **Tests**: NEW `aiModelCatalog.test.ts` 4 + `adminAiConfigModelManagement.test.ts` 10 (builtin 400 regression, hide/remove/404/active-reset/restore, POST valid/invalid) = **14 new**; guard-run `modelChain.test.ts` (5 pre-existing) → **19/19**; tsc **46 = exact baseline (0 new)**; no migration. **Live-verified** (Playwright :3000 admin): builtin DELETE → 400; UI remove/re-add gpt-oss toasts + CATALOG badge persists across reload; combobox = all 11 options across 3 optgroups (earlier "7 options" was a11y-snapshot truncation); 0 console errors; no DB pollution. ⚠️ Working tree also carries unrelated pending v3.28.x-era sqlite/leader/worker changes — do NOT mix into the v3.29.2 commit. |
| [versions-v3.29.md](./changelog/versions-v3.29.md) | **v3.29.1** — **Header overflow fix + Watchlist logged-out infinite-skeleton fix (browser/DevTools visual-verification batch)** (on `main`, on top of merged v3.29.0 `d7e54cf`; commit pending user): **(1) Header overflow** (`app/Header.tsx`, CSS-only) — 372 DOM overflow checks + 9-width quick-check loop, 0 overflow @1440/@375 on watchlist/alerts/screener/advanced-screener; e2e **87 passed / 2 flaky / 0 failed**. **(2) Watchlist logged-out skeleton FIX** — found via user-requested browser + Chrome DevTools visual observation of `/watchlist` logged-out; `app/watchlist/page.tsx` :303 `if (status === "loading" || loading)` made the `unauthenticated` "Please sign in to view your watchlist." card dead code (local `loading` clears only inside `fetchWatchlists()` = authenticated-only) → eternal skeleton; fixed to `status === "loading" || (status === "authenticated" && loading)`; e2e missed it (watchlist spec logs in first). **(3) Live-verified** — isolated logged-out context → sign-in card, 0 skeletons; logged-in demo → "Demo AI Watchlist" (RELIANCE ● LIVE ₹1,310.90 / -23.90 (-1.79%), OHLC table, Analyze/+ Add/Delete), 0 console errors, no overflow @375×812 / @2696. **Tests**: NEW `app/watchlist/__tests__/page.test.tsx` **3/3** (unauthenticated sign-in + no skeleton [regression], loading skeleton, authenticated-empty CTA). tsc **46 = exact baseline (0 new)**; no migration. |
| [versions-v3.29.md](./changelog/versions-v3.29.md) | **v3.29.0** — **UI/UX audit fixes: backtest symbol-gate softening + AI-failure error surfacing + mobile-nav Alerts + `[object Object]` throw-site fix** (on top of v3.28.5 `6700076`; commit pending user — push carries v3.28.5 too): **(1) Backtest gate softened** — `app/api/backtest/run/route.ts` no longer 404s on unknown symbols; v3.28.5's `isBacktestSymbolAllowed(symbol, hasDbRecord)` **REMOVED** (dead code — `lib/services/symbolReference.ts` now exports only `mergeSymbolSuggestions`); the route always falls through to `getBacktestData(symbolUpper)` and derives `symbolSource = "known"|"unlisted"` (:83) from `prisma.symbol.findUnique` purely for labeling — the ONLY no-data failure is the pre-existing `barCount < 50 → 400` guard (live-verified: unlisted `RBLBANK` → 200, `symbolSource:"unlisted"`, 70 bars). `symbolReference.test.ts` **11 → 7** (4 gate tests removed with the helper). **(2) AI-failure surfacing** — NEW `lib/aiErrorMessage.ts` `extractErrorMessage(err, fallback)` (unwraps `Error.message` / `{error:{message}}` shapes); `AiActionButton` gains `error?: string | null` → small red status line (hidden while loading); `app/watchlist/page.tsx` passes `error={aiError}` and its throw-site (:254) normalizes `err.error || HTTP <status>` to a string — fixes both the swallowed `/api/ai/query` 500s AND the `[object Object]` render (bonus regression; live-verified with a simulated 500). **(3) Mobile nav Alerts** — `app/Header.tsx` logged-in quick-access `grid-cols-2` now includes **F&O Analytics + Alerts** (verified 375×812). **Tests**: NEW `backtestSymbolFallthrough.test.ts` 4 (node-env pragma) + `watchlistAiError.test.ts` 8 + `App/components/__tests__/AiActionButton.test.tsx` 4 (path deviation, plain `test(`). **Verification**: tsc **46 = exact baseline (0 new)**; targeted **23/23**; full suite **1043 pass / 4 skip / 1 fail** (1 = documented pre-existing `intelligence.test.ts` flake only); no schema change → no migration. Spec `.agents/specs/07-ui-ux-audit-fixes.md` + plan `.agents/plans/07-ui-ux-audit-fixes.md`. **Do not commit/push/merge without explicit user approval.** |
| [versions-v3.28.md](./changelog/versions-v3.28.md) | **v3.28.5** — **NSE scrip-list constant + consumer wiring** (on top of v3.28.4; commit pending user): user directive — NSE "Securities available for trading (Equity)" CSV → committed symbol constant "loaded for the Symbol references". Machine endpoint **`https://archives.nseindia.com/content/equities/EQUITY_L.csv`** (www page JS-guarded); **2,570 clean scrips** (EQ 2,288 / BE 254 / BZ 28; 8 fields each, no dupes). NEW `scripts/fetch-nse-scrips.ts` generator (`npm run fetch:scrips -- --write`, dry-run default, BOM strip, ≥2000-row guard) → **`lib/services/nseScripList.ts`** (464 KB: `NseScrip`, `NSE_SCRIPS`, `NSE_SYMBOL_SET`, `NSE_SCRIP_BY_SYMBOL`, `isNseSymbol` O(1) case-insensitive, `getNseScrip`, `searchNseSymbols` prefix-priority) + NEW `nseScripList.test.ts` **11/11** (ISIN `^IN[0-9A-Z]{10}$` — SME `IN9`; RELIANCE/TCS/INFY/20MICRONS fixtures). **Wiring increment**: NEW `lib/services/symbolReference.ts` (`mergeSymbolSuggestions` constant-first + `isBacktestSymbolAllowed`); symbols/search route constant-first merge (source `merged/constant/db`, NSE fallback only when merged empty); backtest route 404 gate now falls through listed-but-unsynced symbols; `nseIpoService` `IpoIssue.listed?` + "Listed" pill in IposTab; NEW `symbolReference.test.ts` 11/11 + `nseIpoService.test.ts` **15/15** (11 pre-existing v3.6.4 + 4 listed-enrichment). tsc **46 = exact baseline (0 new)**; no migration. |
| [versions-v3.28.md](./changelog/versions-v3.28.md) | **v3.28.4** — **Read-first recommendations route + edge-cache the heavy latest-run reads** (on top of v3.28.3): (1) **Key collision** — `app/api/recommendations/route.ts` wrote its serialized `responseBody` under the **service's** `LATEST_KEY` → clobbered the `LatestCacheEntry {runId, newestRunId, data}` → the fingerprint check read `cached.runId === undefined` → the heavy stocks-include query re-ran on **EVERY request** (db-health `recommendations.prisma` 14/14 misses); (2) heavy reads unedge-cached. **Fix**: route gains `ROUTE_CACHE_KEY = "recommendations:api:latest"` + `ROUTE_CACHE_TTL_SECONDS = 60` + typed `RouteRecommendationsCacheBody`, read-first memory fast path after the breaker (`servedFrom: "memory_cache"`, `recordRead` hit, zero Prisma); all 3 legacy `"recommendations:latest"` refs switched to the route key; the service's two heavy `findFirst` reads (`latestRun` include + `newestRun` select) wrapped in `withAccelerateCache({ttl:60, swr:30})`; **fingerprint probes stay uncached** (cross-instance guard); `findFirst`+`include` payload falls back to the bare model through the wrapper → re-used the existing `as RunWithStocks | null` cast at the `serializedStocks` usage. Test factory gains the pure `withAccelerateCache` stub (spread preserves keys; existing assertions still pass) + NEW regression (fingerprint calls lack `cacheStrategy`; heavy calls[2]/[3] carry `{ttl:60, swr:30}`) → `dailyRecommendationService.test.ts` **34/34**. **Suite** full **1004 pass / 4 skip / 2 fail** (2 = documented pre-existing `intelligence.test.ts` flake); tsc **46 = exact baseline (0 new)**; no migration. Commit pending user. |
| [versions-v3.28.md](./changelog/versions-v3.28.md) | **v3.28.3** — **Audit write-behind promotion fix (strip `queued_at` before Prisma `createMany`)** (on top of v3.28.2): the db-health "Recent DB Errors" ring was full of repeating `AuditLog createMany — Unknown argument queued_at` (~15-min spacing) — root-cause: `mapWbToPrisma` in `lib/sqlite.ts` (used by the write-behind flush) has a `default` branch that passes same-name wb-row columns verbatim into `auditLog.createMany`; `queued_at` is a **SQLite wb-only bookkeeping column** (auto-added by `enqueueWriteBehind`) absent from the Prisma `AuditLog` model → every ~15-min flush threw → audit rows never promoted (sticky rows re-failed every flush, db-health logged each failure). Pre-existing since v3.22.0 promotion model. Fix (surgical, `lib/sqlite.ts`): `mapWbToPrisma` now skips `queued_at` before `default:`. Only audit is ever promoted (server_log/api hard-refused) so no other wb column leaks. +1 regression (`sqlite.test.ts` 37 total — seeds an audit wb row, asserts every `createMany` data entry lacks `queued_at` + mapped fields arrive). **Suite** targeted `sqlite.test.ts` **37/37**; tsc **46 = exact baseline (0 new)**. No schema change → no migration. Commit pending user. |
| [versions-v3.28.md](./changelog/versions-v3.28.md) | **v3.28.2** — **Lost-leader engine stop — single-active-worker enforcement** (audit + fix, on top of v3.28.1): post-ship audit verified all persistence paths (AI-call tracking → SQLite write-behind + two-tier merge; Recommendations/Performance/IPO/Swing persist post-analysis; crons synced via syncFromPrisma pull + reconcileControlToPrisma push). Remaining defect: `instrumentation.ts` `onLost` callbacks only logged; combined with `acquireLeaderLock` fail-open on DB-unavailable, multiple instances could start a worker poll loop + cron daemon on a blip — on DB recovery losers' heartbeats failed but they **kept polling forever** → multiple active workers/tasks. Fix (instrumentation.ts only): worker onLost → `stopWorkerEngine()`; cron onLost → `stopCronDaemon()`; sqlite-sync onLost stays log-only (gated per-run by `isLeader`). NEW `lib/__tests__/instrumentation.test.ts` (5: starts when leader for all 3 roles; worker/cron onLost → stop called; not-leader; non-node runtime). **Suite 1003 pass / 4 skip / 1 fail** (5 new; 1 = documented pre-existing `intelligence.test.ts` flake — excluding it 72 suites / 1003/4/0); tsc **46 = exact baseline (0 new)**; 85/85 targeted. No schema change → no migration. |
| [versions-v3.28.md](./changelog/versions-v3.28.md) | **v3.28.1** — **SQLite partial-init self-heal + promote not-ready guard (fixes live "SQLite Not Ready" + "promoteNseToPrisma … no such table")** (on top of v3.28.0): root cause — `initSqliteBackup` set `state.db = db` BEFORE the schema loop; a throw there left `state.db` non-null + `ready:false`, and the `if (state.db) return` early-return made the `ensureSqliteBackup()` retry a **permanent no-op**, yielding BOTH live symptoms on the same state: dashboard "SQLite Not Ready" AND `promoteNseToPrisma` hitting "no such table: daily_price" (it only guarded non-null `state.db`, not `ready`). Fix (#1): init catch now nulls `state.db` + `_instance` so the next call genuinely REBUILDS (self-healing); Fix (#2): `promoteNseToPrisma`/`promoteTable` now require `state.ready` so a partially-built mirror is skipped (all-zero summary, no Prisma ops, no throw). +2 regressions (partial-init repair; promote not-ready return). **Suite 998 pass / 4 skip / 1 fail** (1 = documented pre-existing `intelligence.test.ts` flake — excluding it 71 suites / 998 pass / 4 skip / 0 fail); tsc **46 = exact baseline (0 new)**; sqlite/daemon/historical/dbOpTiering 67 pass. No schema change → no migration. |</think>

| [versions-v3.27.md](./changelog/versions-v3.27.md) | **v3.27.0** — **Prisma Postgres Migration Phase 1-3: `withAccelerate()` wiring + `cacheStrategy` at 5 query sites** (branch `v3.26.0-prod-failure-triage`, on top of v3.26.0 work): **why** — standalone Prisma Accelerate (`prisma+postgres://accelerate.prisma-data.net`) retires **Dec 1, 2026** (~3mo); the long-term fix is **Prisma Postgres** (edge caching built-in, Phase 0 = manual Console provisioning at deploy-time, not code). Phase 1-2 ship the intermediate win: activate the already-installed `@prisma/extension-accelerate` `withAccelerate()` on the runtime client + add explicit **`cacheStrategy`** to the 5 highest-frequency direct-Prisma **reads**. **Phase 1** (`lib/prisma.ts`): `new PrismaClient({accelerateUrl}).$extends(withAccelerate())` (extension order documented — `withAccelerate()` first so `$allOperations` wraps it); `type AccelerateClient = PrismaClient` top type (reverted from an intersection that would add tsc errors); `let prismaClient: any`; NEW **`withAccelerateCache(strategy)(args)`** boundary helper — the base `PrismaClient` model read-args hard-code `cacheStrategy: never`, so inline/spread fails to type-check and manual intersections don't override `never`; the helper preserves contextual typing via `Parameters<T>[0]` while injecting `cacheStrategy` at the query boundary. **Phase 2 (5 sites)**: `corporateAction.findMany` `{ttl:300,swr:60}`; `chartinkScreenerResult.findMany` `{ttl:900,swr:300}`; `recommendationTracker.findMany` ×2 `{ttl:600,swr:60}`; `dailyPrice.findFirst` ×2 `{ttl:60,swr:30}`; `marketCache.findUnique` `{ttl:300,swr:60}`. **Test coupling fixes**: `recommendationPerformanceService.test` + `chartinkScreenerService.test` mock `@/lib/prisma` with `{__esModule,default}` only, so the new named export was `undefined` → added the pure `withAccelerateCache` stub to both factories; chartink exact-args assertion → `expect.objectContaining`. **Verification**: tsc **46 = exact baseline (0 new)**; `prisma validate` valid + `prisma generate` ok; full suite **995 pass / 4 skip / 2 fail** (2 fails = documented pre-existing `intelligence.test.ts` async cache-flake, confirmed fails identically in isolation, `intelligence.ts` untouched — excluding it: 71 suites / 995 pass / 4 skip / 0 fail from these changes); no schema change → no migration; diff scoped to exactly 8 files. **Do not commit/push/merge without explicit user approval.** |
| [versions-v3.26.md](./changelog/versions-v3.26.md) | **v3.26.0** — **Prod-failure triage from DB Errors / deploy logs / Tasks panel** (on `main`, on top of v3.25.0): (1) **P2002 false-error reporting FIX** (`lib/prisma.ts`) — the DB Errors panel showed 3× `WorkerStatus create` P2002; root cause = benign leader-election "create-or-stand-by" races in `lib/services/leader.ts` (10+ cold-start instances contend for `leader-<role>`; losers throw P2002, handled gracefully) were recorded as DB faults → NEW `isBenignUniqueConflict(err)` (code `P2002`), `$allOperations` catch SKIPS `recordDbError` for them (error still propagates unchanged). (2) **Plan-limit breaker false-trip FIX (critical)** (`lib/prisma.ts`) — the breaker opened on ANY `isDbUnavailableError`, so a transient Accelerate-proxy `fetch failed`/connection blip on a HEALTHY DB tripped the 5-min global freeze (logs: 17:00:34 `fetch failed` → 17:04:36 "Plan limit circuit breaker open" → 17:28/17:29 repeat → "Swing analysis processor crashed" + "Cron daemon resync deferred" with ZERO Prisma access for the cooldown; the "half-open probe" is dead code while open). FIX: breaker trips ONLY on `isPlanLimitHoldError` (P6003/"hold on your account"/"planLimitReached"/query timeout); transient comms errors keep per-query graceful degradation without freezing the breaker. Removed unused `isDbUnavailableError` import. (3) **"a is not iterable" Daily Market Sync FIX** (`lib/services/worker/worker-service.ts`) — `executeMarketDataSync` (`market_data` behind `Daily Market Sync (System)`) iterated `for (const stock of getIndexStocks(indexName))` with NO null guard; `getIndexStocks` returns `null` on NSE error (lines 783/809) → `TypeError` minified to "a is not iterable" (prod 2/9 + 3/9 at 01:01). Added `if (!Array.isArray(stocks) || stocks.length === 0) throw new Error("No stocks fetched from NSE (market data sync)")` mirroring `executeStockSync`. (4) **12h→6h sync cadence (user directive)** (`lib/sqlite.ts`) — `PROBE_INTERVAL_MS` 12h→6h; the periodic probe now runs a full `syncFromPrisma()` (→`reconcileControlToPrisma`) on EVERY available tick (was down→up only), so the SQLite→Prisma control-plane reconcile reaches Prisma every 6h even on a healthy DB. Stale "12h" comments updated to 6h in sqlite.ts + swingRecommendationService + corp-actions/combined + recommendations routes (`IPO_ANALYSIS_CACHE_TTL_SECONDS` 12h cache TTL unchanged). (5) **Tests** — NEW db-utils breaker-trip regression (transient `fetch failed`/ECONNREFUSED/P1001/Accelerate-proxy/Bad Gateway → `isDbUnavailableError=true` but `isPlanLimitHoldError=false`; benign P2002 → both false). **Targeted suite**: db-utils 24 + daemon-sqlite-first + dbOpTiering + leader 58 + sqlite 34 pass; tsc 46 = exact baseline; no schema change → no migration. Docs/commit pending user. |
| [versions-v3.25.md](./changelog/versions-v3.25.md) | **v3.25.0** — **SQLite-Primary daemon control plane**: high-frequency worker/cron check-reads (30s poll + 5-min resync) and periodic task-status writes move off Prisma to the local SQLite mirror; Prisma written ONLY at the 12h `syncFromPrisma` reconcile. Per directive "write to prisma only during the 12hr sync job; if sqlite empty fetch from prisma but write to sqlite". `lib/sqlite.ts`: `ensureControlColumns` (idempotent ALTER ADD COLUMN for `worker_task.assigned_to/cron_job_id/payload` + `cron_job.config` — SQLite-only, NO schema/migration) + NEW SqliteFallback `upsertWorkerTask/upsertWorkerStatus/upsertCronJob/deleteWorkerTask/deleteCronJob/isControlMirrorFresh` (non-empty AND fresh `control_write_at:<table>` in `_backup_meta` → trust mirror else Prisma+seed) + `reconcileControlToPrisma(db)` at top of 12h sync (worker_status upsert, cron_job conditional updateMany, completed/failed worker_task updateMany — the ONLY Prisma writes). `worker-engine.ts`: NEW exported `discoverPendingTask()` SQLite-first (fresh mirror → highest-priority pending; fresh-but-empty trusted → null; else Prisma `findFirst` + seed); poll uses it; completion/failure `upsertWorkerTask` self-corrects mirror. `cron-daemon.ts`: `syncCronJobs` SQLite-first (`getCronJobs` + `parseConfig` active-filtered) else Prisma + reseed; `fireJob` stays Prisma. `task-orchestrator.ts`: `seedTaskMirror()` after task creates. **Cross-instance exclusions stay Prisma (user-confirmed)**: atomic claim, leader lock+heartbeat, reaper liveness reads, stateless heartbeat, `fireJob` re-fetch, admin routes. NEW `daemon-sqlite-first.test.ts` (7). **Suite 994 / 4 skip** (full; 2 fails = pre-existing `intelligence.test.ts` flake — excluding it 71 suites / 983 pass / 4 skip / 0 fail); tsc 46 = baseline; no schema change → no migration. Commit/push pending; `.env` must be restored to Accelerate URL before finish. |
| [versions-v3.24.md](./changelog/versions-v3.24.md) | **v3.24.0** — **Plan-limit breaker false-OPEN observability + leader-heartbeat renewal + AI-monitoring two-tier merge** (branch `fix/cron-breaker-leader`, PR #113): root-caused "crons not firing at all" — the in-process cron daemon fires on schedule but EVERY DB op is rejected by an **OPEN plan-limit breaker on a healthy DB**, so `fireJob` throws `Plan limit circuit breaker open`, nothing spawns, `nextRun` never advances. Two defects: (1) `openPlanLimitBreaker()` logged nothing about the tripping error (invisible false trip — likely a 120s query-timeout `PrismaQueryTimeoutError` whose name matches both `isDbUnavailableError` and `isPlanLimitHoldError`, or a broad message-substring match) → added throttled `logger.warn` at the open site with model/operation/error/type (`BREAKER_TRIP_LOG_THROTTLE_MS`=60s, `lib/prisma.ts`); (2) leader heartbeats NEVER renewed — `renewLeaderLock` had zero call sites, so every `worker_status` `leader-<role>` row went stale after 5 min and a standby claimed it → split leadership → duplicate crons/sync/worker on multi-instance bursts → NEW `startLeaderHeartbeat(role, onLost?)` renews every `LEADER_HEARTBEAT_MS`=60s via an `unref()`'d timer (leader.ts), wired for `worker`/`cron-daemon`/`sqlite-sync` (instrumentation.ts). ALSO fixed **AI-monitoring "0 of 0 calls shown"**: since v3.22.0 AI calls are write-behind to SQLite `wb_server_log` and only error/warn rows are promoted, so info-level SUCCESS calls never reached Prisma; NEW `getWriteBehindLogsBySource` (sqlite.ts) + two-tier merge in `getPersistedAiCalls` (Prisma + SQLite, newest-first, sliced). NEW `ai-monitoring.test.ts` (3). **Suite 989 pass / 4 skip / 0 fail** (was 986); tsc 46 = exact baseline; no schema change → no migration. PR #113 pending user merge/deploy. Related: Netlify secrets-scan fix PR #112. |
| [versions-v3.23.md](./changelog/versions-v3.23.md) | **v3.23.0** — **SQLite-primary READ tier during plan-limit breaker holds — hot routes fully servable from the SQLite mirror without touching a held Prisma account + DB-log & worker-log download/export UI + readTier telemetry**: when the plan-limit circuit breaker is OPEN, hot reads serve entirely from the SQLite mirror (recommendations `servedFrom:"sqlite_mirror"`, swing returns screener-only pending with no Prisma writes, screener `getChartinkScreeners()` gated, corp-actions `source:"sqlite_mirror"` — atomic `updateMany` claim remains sole writer exception); DB-log download/export UI (`getDbLogFiles()` filesystem-only, per-kind export + archived-files table); worker/task/cron logs downloadable (`readAllLogs()` bulk-concat + `?action=download` in monitoring Workers tab); Chartink cache TTL 5m→15m; NEW `lib/services/readTier.ts` zero-DB telemetry registry (globalThis `__readTier`, `ReadSource` = sqlite/memory/prisma/nse/filesystem/other, per-reader latency min/max/avg/hits/misses/rows, per-source aggregation, bounded long-query ring `MAX_LONG=15`, SQLite perf grid) instrumented across 6 call sites (sqlite mirror, recommendations Prisma, swing breaker-open, screener Prisma, corp-actions memory+Prisma); db-health GET returns `readTier` + `cache.metrics`; new **Cache & Read-Tier Utilisation** card (NodeCache hit-rates, HIGH-frequency sqlite/memory table, LOW-frequency Prisma warning, long/large-queries, SQLite latency grid). NEW `readTier.test.ts` (11). **Suite 986 pass / 4 skip / 0 fail** (was 975/4, +11); tsc 46 = baseline; no schema change → no migration. Commit pending user. |
| [versions-v3.22.md](./changelog/versions-v3.22.md) | **v3.22.0** — **Write-behind log store (SQLite-primary, Prisma promotion for important logs only) + leader election + audit-tag gap fill — Prisma ops target <1000/day**: leader election (`lib/services/leader.ts`, single-writer lock on `worker_status` `leader-<role>` + heartbeat + 5-min staleness, DB-down fail-open, create-vs-claim race reconcile — stops 5-instance boot duplication); **write-behind promotion model** (SQLite = primary durable log store 14-day TTL; `isWbImportant` filters api 5xx/rate-limited/anomaly/error + server_log error|warn + security/critical audit tags → ONE `createMany` (~1 op) promotes only important rows; bulk info/api logs stay SQLite-only; `pruneWriteBehind` + 15-min leader-gated `startWriteBehindFlush`; `lastPromoted`/`lastRetained` surfaced; double-count fix — `createMany`=1 op, removed `dbOpsCounter.writes += chunk.length`); audit-tag gap fill (`ADMIN_DB_SYNC/FLUSH_PRICES/FLUSH_LOGS/DEPLOY_PREP/BACKUP/RESTORE` + `ADMIN_RECOMMENDATION_RUN/PERFORMANCE_CHECK/SWING_PERFORMANCE_CHECK`); db-health UI kind-key fix (`pending`/`lastFlushCounts` keyed by kind not table + promoted-vs-retained split); cron-daemon heartbeat → local SQLite (`writeLivenessHeartbeat`, zero Prisma ops). NEW tests: leader.test (18) + sqlite promotion/regression + audit-actions → **suite 972 pass / 4 skip / 0 fail** (was 945/4, +27); tsc 46 = baseline; no schema change → no migration. Commit/PR/deploy pending user. |
| [versions-v3.21.md](./changelog/versions-v3.21.md) | **v3.21.0** — **Professional Equity Research Decision Engine** (deep upgrade of v3.18.0 AI Investment Intelligence): 8-level verdict (STRONG_BUY…AVOID) + conviction /10 + confidence /100; 12-section institutional memo (executive thesis, fundamental score with evidence labels, management DNA, valuation zones with current-price marker, technical structure incl. marketPhase, shareholding analysis, risk matrix with category/probability/impact/pricedIn, catalysts, bull/base/bear scenario, contrarian view + what-would-change-my-mind, portfolio action with positionSizing, invalidation zones); honest data-gap banner (missing fields surfaced, never fabricated); optional raw-text document ingestion (annual-report/concall pasted into company-page textareas, 50KB cap, appended to prompt as secondary-unverified sections); backward-compatible (legacy 3-verdict prompt/parser kept, all new `IntelligenceAnalysis` fields optional → no DB migration; legacy cached rows render via null-coalescing); new `VerdictCard` (8-verdict color/emoji + conviction bar) + 11 new sections + rewritten `IntelligencePanel`/`CompanyIntelligence`/`RiskCatalystMatrix`; `adapters.ts` sma200 best-effort via 280-day window; POST route Zod documents schema (50K cap, 400 invalid); audit metadata modelUsed/verdict/conviction/confidence/hasDocuments/partialData; `server-only` import dropped from normalize.ts (parent-path throw). NEW tests: `stock-analysis-prompt.test.ts` (21) + `document-normalize.test.ts` (9) + `intelligence.test.ts` +3 → **suite 915 pass / 4 skip** (was 883/4, +32), tsc 46 = baseline (0 new production errors) · **v3.21.1** — **DB Health ops visibility — SQLite ops-counter persistence + Total Operations/Plan Usage UI + sql.js WASM fix**: `/admin/utils/db-health` "SQLite Not Ready" fixed — sql.js is a native/WebAssembly module; `next.config.ts` `serverExternalPackages: ['sql.js']` + `resolveSqlWasm()` (`node_modules/sql.js/dist` → `public/`) into `initSqlJs({ locateFile })`. IO-count reconciliation (user-approved "Display + persist"): `lib/prisma.ts` exports `getIstDayKey`; `lib/sqlite.ts` adds `persistOpsCounter()`/`restoreOpsCounter()` (key `ops_counter` in `_backup_meta`, IST-day guard + `Math.max` merge, restore at init + after initial sync) + 60s `startOpsCounterPersistence()` booted from `instrumentation.ts`; `/api/admin/db-health` GET returns `totalOperations`/`planLimit` (env `DB_PLAN_LIMIT_OPS`, default 10,000)/`planOperationsRemaining` + persists (POST sync too); UI gains 6th "Total Ops Today" stat card + "Plan Operations Usage" bar (reads vs writes vs plan, remaining, footnote: Prisma dashboard authoritative, counter restored, resets on deploy) + "Plan Ops n% Used" warning badge >80%. Tests: `sqlite.test.ts` mock fixes (`getIstDayKey`, `exec()` column projection, `INSERT OR REPLACE` semantics) + 3 new tests → **suite 920 pass / 4 skip** (was 917/4, +3), tsc 46 = baseline (0 new production errors). Docs pending commit. **Follow-up increment — per-type DB-error summary + lazy SQLite re-init**: NEW `classifyDbError()` (`lib/db-utils.ts`, `DbErrorType` = plan_limit/timeout/accelerate_proxy/connection/write_budget/other, ordered checks) + per-type `dbErrorCounts` (`__dbErrorCounts` globalThis, lazy IST-day rollover) with `recordDbError()` classification + `getDbErrorCounts()`; `persistDbErrorCounts()`/`restoreDbErrorCounts()` (key `db_error_counts` in `_backup_meta`, IST-day guard + per-key `Math.max` merge, same 60s tick as ops counter); **NEW `ensureSqliteBackup()`** lazy on-demand init (`_initPromise` finally-reset — failed init retried next call, never stuck "Not Ready" after a temp WASM/DB hiccup) + `resetSqliteStateForTests()` in-place test hook; `/api/admin/db-health` GET returns `dbErrorSummary {day, counts}` (+ POST ensures init); UI per-type summary chips (plan_limit/connection red w/ ring >0, timeout/accelerate_proxy/write_budget amber, other gray) + error total + IST-day footnote above Recent DB Errors. Tests: `db-utils.test.ts` `classifyDbError` (7 cases incl. real prod Accelerate message, benign P2021/P2002/P2025→other, non-Error→other) + `sqlite.test.ts` 5 new (error-count roundtrip, stale-day via mocked `getIstDayKey` reassignment, Math.max merge, ensure-ready, re-init after reset) → **suite 932 pass / 4 skip** (was 920/4, +12), tsc 46 = baseline (0 new production errors). Docs pending commit of increment. · **v3.21.2** — **Stock-quote tiering (cache → SQLite → Prisma, zero after-hours DB writes) + TTL ms→s fix + SQLite backup/restore in db-health** (`7409616`, pushed): Fix A `stock-service.ts` `syncDailyPriceOnce` (market-open-only + seed-once per symbol per IST day via globalThis Set); Fix B `lib/sqlite.ts` `daily_price_snapshot` table + snapshot get/set + `syncFromPrisma` DISTINCT ON seed, closed-market chain hotCache → SQLite (zero Prisma) → on miss only 2-3 Prisma reads; Fix C `enhanced-cache.ts` NodeCache TTL ms→s (`Math.ceil(ms/1000)`) — entries lived ~1000× too long; Fix D `priceSyncService.ts` `cacheDailyPrice` gated by `isMarketAccumulationWindow()` + `cacheDailyPriceSnapshot` warm; Fix E db-health API `opsSnapshot` captured before probe/table-counts; SQLite backup/restore (`exportSqliteBackup`/`restoreSqliteBackup`: 50MB cap + magic header + required tables + live DB swap) + POST `backup`/`restore` + /admin/utils/db-health Backup & Restore card. NEW `dbOpTiering.test.ts` (9) → **suite 941 pass / 4 skip** (was 932/4, +9), tsc 46 = baseline. · **v3.21.3** — **Prisma OpenTelemetry tracing (opt-in) + Prisma Compute P1001 false-alarm diagnosis**: installed `@prisma/instrumentation@7.10.0` + 8 `@opentelemetry/*` (25 pkgs); NEW `lib/otel.ts` `otelSetup()` **strictly opt-in** (`PRISMA_OTEL_ENABLED=1` else no-op) — AsyncHooksContextManager + NodeTracerProvider (attrs env-overridable) + PrismaInstrumentation via registerInstrumentations + SimpleSpanProcessor → OTLP/HTTP exporter (console fallback), idempotent globalThis guard, try/catch never crashes; wired `lib/prisma.ts` module-top **before** singleton. `.env.example` documents the vars. NEW `otel.test.ts` (4 no-op guard tests) → **suite 945 pass / 4 skip** (was 941/4, +4), tsc 46 = baseline. **P1001 diagnosis (no code fix — user applies Console toggle)**: Netlify healthy (latest `main` deploy ready; build = prisma generate + quickbuild), the failing "Deploy" = Prisma Compute auto-schema-apply sandbox running `migrate deploy` in a network-isolated sandbox that can't reach direct-TCP `db.prisma.io:5432`; verified `migrate status` from local = 36 migrations up-to-date, **ZERO pending** → false alarm #21. FIX: Prisma Console → DB → toggle OFF "apply schema changes automatically"; future migrations via v3.20.5 runbook (`migrate deploy` + DIRECT_URL from an env with egress). BUGS.md #13. Commit pending user. |
| [versions-v3.20.md](./changelog/versions-v3.20.md) | **v3.20.0** — NSE resilience: all 22+ NSE-dependent routes return graceful empty/null instead of 500/502; MCP GET endpoint fix (shared handler) + MCP graceful empty; corporate-actions NSE decoupling + graceful empty; /api/news/market Prisma import fix + DB error catching; NIFTY_50 constants consolidated to lib/constants.ts; 2026 market holidays; netlify.toml stale extension removed; DB-down test verified (local) · **v3.20.1** — DB ops reduction (~22K→~4.2K ops/day: worker poll 5s→30s, cron resync→5min, heartbeat→15min, legacy scheduler removed, web-vitals writes removed) · **v3.20.2** — DB failure ring buffer (last 50 errors in `$allOperations`) + Daily Price Cache batch writer (market-hours in-memory accumulation → single 4pm IST bulk upsert to `daily_prices`) + DB Health API/UI enhancements (ops counter, price cache, error table, flush button) · **v3.20.3** — **Plan-limit hold resilience**: `isDbUnavailableError()` now recognizes the real prod hold (P6003 / `planLimitReached` / "hold on your account" / `PrismaQueryTimeoutError` / `PlanLimitOpenError`) so all 18+ graceful-degrade chains trigger; plan-limit circuit breaker (`PlanLimitOpenError` + `$allOperations` fail-fast when open, half-open probe auto-recovery, `PLAN_LIMIT_COOLDOWN_MS` 5min); fire-and-forget `createAuditLog()`/`logAPIRequest()` (non-blocking — ~50+ await sites never stall); worker `setTimeout` DB backoff (30s→5min cap, `workerStopped`); cron-daemon boot/per-tick DB-unavailable guard + downgrade; notifications route skips DB-unavailable `console.error` spam; NEW `lib/__tests__/db-utils.test.ts` (14) → **suite 883 pass / 4 skip** (was 869/4), tsc 57 = baseline · **follow-up skill (same branch)**: `playwright-debug` skill built (Inspector/UI Mode/Codegen/Trace/Report) + wired into 7 agents (qa, e2e-agent, bug-hunter, ux-designer, code-reviewer, tdd, build) via `.opencode/opencode.json` + profiles + matrix + AGENTS.md (tooling/docs only, no code change) · **v3.20.4** — **Plan-limit breaker false-positive FIX + missing `intelligence_cache` migration**: Playwright CI was RED — the v3.20.3 `isDbUnavailableError()` blanket `name` catch-all (`prismaclient`+`request`) classified EVERY `PrismaClientKnownRequestError` (benign P2021/P2002/P2025) as "DB unavailable" → `$allOperations` opened the global breaker → 5-min DB freeze → auth/login failed. Triggered by a missing `intelligence_cache` migration (v3.18.0 had applied it only via local `db push`; `migrate deploy` on CI/prod never created the table → `restoreIntelligenceCacheFromDB()` P2021). Fixes: removed the blanket + redundant `"exceeded"` matches so the breaker only trips on REAL hold/unavailability (P6003/connection/timeout), NOT benign Prisma request errors; added `prisma/migrations/20260828000000_add_intelligence_cache` (validated column/index-for-index identical to Prisma's `db push` output). NEW `db-utils.test.ts` +4 real-shape regression tests → **suite 917 pass / 4 skip** (was 915/4), tsc 46 = exact baseline (0 new) · **v3.20.5** — prod deploy/migration-runner FIX: Prisma Dashboard auto-apply sandbox P1001 (Accelerate proxy can't run DDL → schema engine resolved `db.prisma.io:5432` direct-TCP, unreachable); `prisma.config.ts` now supports `DIRECT_URL` (`getDirectUrl()` + conditional `directUrl`, CLI/DDL only, runtime unaffected, `.env.example` documents format); **`20260828000000_add_intelligence_cache` applied to PROD via `npx prisma migrate deploy`** (user-approved, purely additive) → ledger 36/36, table verified (0 rows); tsc 46 = exact baseline |
| [versions-v3.19.md](./changelog/versions-v3.19.md) | **v3.19.0–v3.19.3** — DB plan limit resilience (graceful degradation, op reduction, write budget guard, admin OTP, staticCache key fix) + SQLite backup layer (sql.js in-memory fallback, route fallback chains, Accelerate proxy error detection) + SQLite expanded schema (10 tables, 6 new: worker_status, server_log, audit_log, cron_job, cron_run, worker_task) + recovery sync (5-min background probe, auto-sync on Prisma recovery) + admin DB health dashboard (`/admin/utils/db-health`) + graceful degradation (web-vitals, portfolio, notifications, advance-decline 200-not-500, SQLite recovery probe bug fix) |
| [versions-v3.15.md](./changelog/versions-v3.15.md) | **v3.15.0** — Closed IPOs with current prices + IPO analysis TTL cleanup + pipeline redesign (HOLDs collapsible) |
| [versions-v3.14.md](./changelog/versions-v3.14.md) | **v3.14.0** — Swing signal persistence + performance tracking (45-day expiry, direction-aware targets/stops) + worker-logs tab + spec-driven development workflow |
| [versions-v3.13.md](./changelog/versions-v3.13.md) | **v3.13.0** — DB-backed Swing AI analysis job: durable `SwingAnalysisJob` replaces the volatile cache-only fire-and-forget (pre-scan DB lookup, atomic claim + supersede-abort, stale recovery, cron-daemon drain) |
| [versions-v3.12.md](./changelog/versions-v3.12.md) | **v3.12.0** — Swing tab prod failure FIX (request-time split — async AI analysis; Netlify 30s wall) + prod-stability batch (perf live-price fallback, prod `daily_prices` backfill 21,195 bars, heartbeat-aware reaper, Prisma query timeout, worker-logger tmpdir fallback, error serialization) |
| [versions-v3.11.md](./changelog/versions-v3.11.md) | **v3.11.0–v3.11.3** — in-process node-cron daemon + `daysTracked` 500 fix + carried v3.10.1 batch · no-fake-HOLD Today's Picks (partial persistence) · `recommendationsCache` globalThis singleton · full serverless purge (persistent-server reality, Blob logging removed, DataFetcher test un-skip) |
| [versions-v3.10.md](./changelog/versions-v3.10.md) | **v3.10.0** — historical-price sync into `daily_prices` (Swing indicators "—" fix) + `backtest_history` prod-gap FIX (lazy DDL) |
| [versions-v3.9.md](./changelog/versions-v3.9.md) | **v3.9.0–v3.9.1** — Swing Trading Signals tab (34 screeners, families, AI LONG/SHORT/OBSERVE) + NSE chart buttons + `analysisStatus` honesty fix |
| [versions-v3.8.md](./changelog/versions-v3.8.md) | **v3.8.0** — AI pre-flight gate + cron spawn dedup + stale-task reaping + cron-ledger dedupe + 8192 maxTokens default |
| [versions-v3.7.md](./changelog/versions-v3.7.md) | **v3.7.0–v3.7.3** — F&O Analytics UI + NSE option-chain-v3 + MCP `getOptionChain`/`getFoExpiries` · BUY/SELL-only broadcast + AI connection-test cron · Netlify secrets-scan fix + credential-literal masking |
| [versions-v3.6.md](./changelog/versions-v3.6.md) | **v3.6.0–v3.6.4** — password-reset auth flow + market-sync cron + dividend-cards fix · recs default sorts + perf price bridge + AI context · dividend TZ fix · page redesign + IPO issue size + NSE events + IPO report v2 |
| [versions-v3.5.md](./changelog/versions-v3.5.md) | **v3.5.0–v3.5.7** — perf tracking/archival · target/SL fix + SSE wiring · TV `change`=% fix · Playwright e2e + CI · AI-config/cron-ledger · Chartink capture → DB · Chartink 117-registry + TV fallback · auth join fix + server logs + credentials hygiene + llms.txt |
| [versions-v3.4.md](./changelog/versions-v3.4.md) | **v3.4.0–v3.4.3** — Telegram bot admin + Profile page · prod fixes (chunk timeout, top-50 cap, AI monitoring) · subsystem docs + versioned `.githooks` |
| [versions-v3.3.md](./changelog/versions-v3.3.md) | **v3.3.0–v3.3.1** — daily recommendations engine + AI agent + self-heal AI + audit logging |
| [versions-v3.2.md](./changelog/versions-v3.2.md) | **v3.2.0** — Telegram bot (@tradenext6Bot) + corp-actions price/yield fix |
| [versions-v3.1.md](./changelog/versions-v3.1.md) | **v3.1.0** — risk metrics (Sharpe, max DD, beta, win rate) + benchmark overlay |
| [versions-v3.0.md](./changelog/versions-v3.0.md) | **v3.0.0** — CSV export + portfolio value history + P&L timeline chart |
| [versions-v2.md](./changelog/versions-v2.md) | v2 per-minor index → [versions-v2.2.md](./changelog/versions-v2.2.md) · [versions-v2.1.md](./changelog/versions-v2.1.md) |
| [versions-v1.md](./changelog/versions-v1.md) | v1 per-minor index → [versions-v1.16.md](./changelog/versions-v1.16.md) … [versions-v1.8.md](./changelog/versions-v1.8.md) |
| [docs/screener.md](./docs/screener.md) | Screener & backtest deep-dive (v1.16.0 FilterBuilder/BacktestDialog, v1.10.0 enhancement, v3.5.x Chartink 117-registry + TV fallback) |
| [docs/corp-actions.md](./docs/corp-actions.md) | Corporate actions (dedup fix, NSE field fix, enhanced UI, management) |
| [docs/serverless-logging.md](./docs/serverless-logging.md) | Serverless logging (ServerLog/db-logger, Netlify Blobs, 502 fix) — HISTORICAL, superseded by v3.11.3 |
| [docs/security-workers.md](./docs/security-workers.md) | Security (cookies/sessions), cron/workers, historical sync, tested features |

---

## Full version history (older)

See [versions-v2.md](./changelog/versions-v2.md) + [versions-v1.md](./changelog/versions-v1.md) + feature deep-dives above.


### Plan 15 — Swing AI auto-generate-once (v3.40.4)
- Greenfield `lib/services/swingAutoSeedService.ts`: seed-once auto-generate guard. Exports: `PUBLIC_SWING_USER_ID`, `isSwingSeededForUser`, `hasStoredSwingTargetsForUser`, `SwingAutoSeedOptions`, `autoTriggerOnce`, `generateSwingTargetForSymbol`. Process-level seed-once set prevents double AI.
- `lib/audit.ts`: added `SWING_AUTO_SEED_TRIGGERED` + `SWING_AUTO_SEED_SKIPPED` to `AuditAction` union.
- `app/api/recommendations/swing/route.ts`: fire-and-forget `autoTriggerOnce` after serve (serve-first preserved).
