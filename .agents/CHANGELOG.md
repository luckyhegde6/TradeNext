# TradeNext Changelog

> Full version history + legacy feature docs (moved here from AGENTS.md 2026-08-06). **AGENTS.md keeps only a compact version table** — this directory is the source of detail.
>
> **Reading guidance**: load only the subfile you need (e.g. v3.x for recent work), never the whole directory.

## Index

| File | Contents |
|------|----------|
| [versions-v3.md](./changelog/versions-v3.md) | v3.10.0 — Historical-price sync into `daily_prices` (Swing indicators "—" fix): NEW `historicalPriceSyncService` (scope = NIFTY 50 ∪ 30d trackers ∪ live screener captures, capped 300; N-day EQ window via generateSecurityWiseHistoricalData; multi-row `$executeRawUnsafe` ON CONFLICT upserts chunked 200; 200ms NSE delay; `maxDurationMs` cap; per-symbol tolerance; dry-run default) wired into `worker-service.ts` (`historical_price_sync` case) + `run-cron-background.ts` (new `historical-price-sync` action AND step 4 of market-sync with `dryRun:false` 6-min budget) + NEW `scripts/backfill-daily-prices.ts`; suite 653 pass (was 638), tsc 71 baseline; local dry-run verified (TCS 5d → 4 EQ bars); `--apply` needs user permission; PLUS plan-only (user decision) for prod `backtest_history` gap → `.agents/docs/plan-backtest-history-prod-gap.md` + BUGS.md #11 — v3.9.1 — Swing `analysisStatus` honesty fix (live-verified prod bug — header lied "AI targets ready" over an all-failed AI batch; NEW pure `analysisStatusAfterBatch(stocks)`: "done" only when ≥1 stock carries AI targets; +3 tests, suite 638 pass) + live verification of v3.9.0 (Swing tab 20 picks / family chips / TV-fallback badges, chart buttons AXISBANK-EQ / NIFTY%20BANK, 0 console errors desktop+mobile) + prod findings (daily_prices 0–1 rows per pick → indicators "—" = data gap needing a price-sync job; `backtest_history` table missing on prod → MCP getHistoricalData 500s) · v3.9.0 → v3.0.0 (Swing Trading Signals tab — 34 swing-category Chartink templates via the unified runner + family segregation (momentum/breakout/trend/mean-reversion/crossover/bearish) + top-20 composite cap + 25-bar indicator enrichment + NEW `lib/services/ai/swing-agent.ts` AI LONG/SHORT/OBSERVE with direction-aware levels via `evaluateRecommendationLevels` + `GET /api/recommendations/swing` + SwingTab/SwingCard UI + daily run `excludeCategoryIds:["swing"]`; scope-aware cache-key fixes — unified runner `unifiedCacheKey(options)` + swing `${key}:ai|noai` split; suite 634 pass; + NSE candlestick chart icon buttons on Swing cards + Today's Picks cards + Markets index cards via `openNSEChart` (`lib/charting.tsx`, `?symbol=X-EQ` stocks / `?symbol=INDEX`, markets button `stopPropagation`-guarded); local daily_prices indicator limitation documented; v3.8.0 AI pre-flight gate — `runAiConnectionTest(120s)` ok/fallback/failed → skip-AI all-HOLD fast-fail; cron system-job dedupe — keep earliest per name; worker stale-task reaping — 16-min STALE_MS + `maybeReap` ≤1/min + exported `checkScheduledJobs`; cron spawn dedup — 90-min `DEDUP_WINDOW_MS` skip-spawn-but-advance-nextRun; maxTokens default 8192 + `getPromptTimeoutMs()` 120s in llm-provider + batch-budget clamping; credential-literal masking follow-up — `Lessons.md` + hook block-lists assembled at runtime, pushed direct to main; Netlify secrets-scan build-failure fix — `.githooks` omit path + app hygiene (example telegram token/chatId/code placeholders no longer look like secrets) + live-verify finding: tradenext6.netlify.app stale (v3.6.3/v3.7.x not deployed, deploy on hold); BUY/SELL-only Telegram broadcast + AI connection-test cron w/ fallback probing + audit/status + CI e2e fix; F&O Analytics UI complete + NSE option-chain-v3 migration + MCP getOptionChain/getFoExpiries — 28 functions; IPO Issue Size — shares per lot + ₹ per lot — + NSE events feed + AI IPO report v2 JSON + MCP/Telegram IPO commands; direction-aware target/SL evaluation — ITC SELL bug + Recommendations sidebar/IPOs-status-sections redesign; DividendCalendar timezone fix; password-reset-request auth flow + daily market-sync cron + zeroed-dividend-cards fix; auth join→approve→login fix + server logs `logs/` dir + **`DEFAULT_PASSWORD` env var + credential-masking git hooks + README rewrite + `app/llms.txt`/robots LLM-discovery**; Chartink 117-registry PRIMARY + TV fallback unified runner; Chartink template capture → DB: 3 Prisma models + Playwright capture tool + sync service; AI config plumbing fix + cron ledger fix + session memory infra, Playwright e2e suite, screener `change`=% fix, target/SL ₹0 fix + SSE live prices, recommendation performance & archival, Telegram bot, recommendations engine, risk metrics, CSV export) |
| [versions-v2.md](./changelog/versions-v2.md) | v2.2.0 → v2.1.0 (alert engine, admin alert config, 98 screener templates) |
| [versions-v1.md](./changelog/versions-v1.md) | v1.16.1 → v1.0.0 (advanced screener, agent handoff, MCP API, security, workers) |
| [changelog/screener.md](./changelog/screener.md) | Screener & backtest deep-dive (v1.16.0 FilterBuilder/BacktestDialog, v1.10.0 enhancement) |
| [changelog/corp-actions.md](./changelog/corp-actions.md) | Corporate actions (dedup fix, NSE field fix, enhanced UI, management) |
| [changelog/serverless-logging.md](./changelog/serverless-logging.md) | Serverless logging (ServerLog/db-logger, Netlify Blobs, 502 fix) |
| [changelog/security-workers.md](./changelog/security-workers.md) | Security (cookies/sessions), cron/workers, historical sync, tested features |

---

